# Агенда — який наступний хід

**Дата:** 2026-09-07 · **Спирається на брифінг:** `work/CTO-RESEARCH.md`, базис **`0afa280`**,
перезібраний сьогодні. На момент цієї агенди `HEAD` = `d793c8c`, тобто брифінг відстає на
**4 коміти**, і всі чотири — це роботи цієї ж сесії (T117 Гап 1, T110, реліз `0.3.0`), тож
жодна теза брифінгу від них не протухла, окрім прямо виправлених нижче.

> Агенда **перезбирається**, не дописується. Зроблене з попередньої лишається одним рядком.

---

## 🔴 Головне за цей перезбір: найстаріша розвилка репо **вже закрита**, і агенда цього не знала

Попередня агенда несла **Р1 — «Який рушій контуру лишається (T80), стоїть із 25 серпня,
12-й день»**. Це неправда з 2026-09-06: рішення продукту записане в шапці самої задачі й
закомічене в `63a0a31`.

> «C-подібного контуру немає — є магнітний у департаментах» ⇒ `cell-flood` **без замовника**,
> лишається `button-group` як єдиний рушій.

**Наслідок для агенди — не косметичний.** Найбільша агент-реді робота репо стояла під міткою
«чекає на продукт» і тому не потрапляла в кандидати **два дні поспіль**. Це рівно та пастка, яку
брифінг називає своїм же правилом: **статус у заголовку — не доказ**; тут він був не застарілий,
а протилежний істині — розвилку закрили, а мітку «чекає» лишили в двох інших документах.

Виправлено в цій агенді (Р1 знято), у брифінгу §7 п.1 і в самій задачі.

---

## Джерела: скільки кандидатів дало кожне

| Джерело | Кандидатів | Нотатка |
|---|---|---|
| Відкриті таски `work/tasks/` | **11** | T80 (тепер **імплементація**, не розвилка), T101, T102 блок Б, T106, T109, T112, T113, T114, T115, T116, T117 Гап 2 |
| `gh issue list` | **0** | трекер порожній і був порожній завжди (`docs/agents/issue-tracker.md`: беклог живе в `work/`) |
| Борг `work/tech-debt/` | **0** | одинадцять файлів, **перевірено по одному**: усі `closed` / `done` / `рішення змінено` / `not a bug` |
| «Ризики» брифінгу §6 | **17 живих** із 20 | 1–3 закриті. З нових (13–20) шість не є задачами: 13–14 переведені в правила §8, решта — межі вже зробленого |
| Невиконані чекбокси | **0 нових** | усі влучання — всередині вже перелічених відкритих задач; п'ять у `SPEC.md:809-813` — це чек-ліст **рев'ю**, не робота |
| Скіпнуті тести / вимкнені гейти / TODO | **3** | скіпнутих **0**, `TODO`/`FIXME`/`HACK`/`ponytail:` — **0** (заміряно `grep` по `packages/*/src` і `packages/core/src`). Лишаються: `oxfmt` налаштований і **не** гейт; п'ять HARNESS-спеків не виконуються ніде; **помилки сторінки збираються рівно на одній вкладці** |
| Продуктові розвилки | **6 живих** | T116 (сім), T117 Гап 2, T112, T56, T61, T67 Phase 2. **T80 звідси пішов** — закритий |

Кандидатів без джерела (`⚠️ без джерела`): **0**.

---

## Кандидати

### A. T113 — згорнуті діти лягають стрічкою, а не матрицею
**Джерело:** [`T113`](./tasks/T113-collapsed-children-should-be-a-matrix.md) · спостереження
користувача 2026-09-06, відтворено знімками на **двох** вкладках.
- **Розблоковує:** критерій T112 «матричне розміщення має щонайменше одну сцену серед шести»;
  і рядок паритету, який хост уже виконує, а ми ні.
- **Зворотність:** дешево — правило розкладки, за прапорцем сцени, `git revert` без наслідків.
- 🔑 **У хості це вже працює** (`matrix-group.ts`, `matrix-decision.ts`, `tree-layout.ts:37-154`,
  `matrixOnCollapse`) — правило треба **повторити**, не винайти. Спеку вже виправлено: дефект
  стосується **обох** діаграм, не лише org.

### B. T80 — прибрати `cell-flood`, бо в нього немає замовника
**Джерело:** [`T80`](./tasks/T80-contour-engines-ba-demo.md) шапка (рішення продукту 2026-09-06).
- **Розблоковує:** кожну майбутню фічу контуру — сьогодні вона пишеться **двічі**; частину T112.
- **Зворотність:** ⚠️ **дорого** — видалення. Радіус заміряно: **23 TS-файли** + `contour.rs`,
  `contourEngine` двічі в `docs/USAGE.md` ⇒ **вище порога пайплайна**, повний `spec-flow`.
- ⚠️ **Пастка, названа в задачі:** прибрати *рушій* ≠ прибрати *Rust-контур*. П'ять канонічних
  тестів Variant B міряють `computeAllContours` — Rust тримає **канон магнетизму**, не лише flood.

### C. T115 — у хоста немає еквівалента `window.__orgHierarchy`
**Джерело:** [`T115`](./tasks/T115-test-seam-parity-with-host.md) · `questions.md` п.20, де хост
називає це «**найнедооціненішою статтею витрат усієї міграції**».
- **Розблоковує:** e2e хоста після переходу — тобто саму можливість переходу.
- **Зворотність:** середньо — новий шов у публічній поверхні.

### D. T114 — `Flat orgs` дає чотири помилки сторінки на першому кадрі
**Джерело:** [`T114`](./tasks/T114-flat-orgs-null-on-first-paint.md), знайдено прогоном T113.
- **Розблоковує:** нічого напряму, **але** несе важіль, більший за себе: помилки сторінки
  збираються **рівно на одній** вкладці (`prod-smoke.spec.ts:30,33`, і лише на дефолтній);
  `demo-audit.spec.ts` слухає тільки `response`. Гейт треба **додати**, і він накриє всі вкладки.
- **Зворотність:** дешево. Задача сама каже: з T113 **не** пов'язано — інша причина, інший шлях.

### E. T102 блок Б — стеля 2 500 і квадратична вартість
**Джерело:** [`T102`](./tasks/T102-row-tree-depth-block-b.md) · `reports/row-tree-depth/spec.md` §3.
- **Розблоковує:** нічого сьогодні — гвардія тримає, жоден інпут більше не вбиває WASM.
- **Зворотність:** дешево на кроці **B2** (вимір: чи стеля в `tidy_tree`/`Drop`, чи наша).
- Був рекомендованим ходом попередньої агенди й **не взятий** — черга змінилась під нього.

### F. T116 — org-level reparent · **G.** T117 Гап 2 — move/merge/clone
**Джерело:** `positions-diagram-parity.md` п.24, п.29.
- **Обидва заблоковані продуктом**, див. розвилки. T116 має spec+plan, але **сім розвилок**
  і **не пройдену `plan-critique`**; T117 Гап 2 не має навіть відповіді, чи це справа SDK.

### H. T106 — два файли тримають майже все · **I.** T109 — `toggleStaffOrg` без відкоту · **J.** T101 — флаки, і задача більше не та, що написана
Без змін із попередньої агенди. T101 ⚠️ спростований власною гіпотезою (падає 2 з 3 і при
`--workers=1`), тож починається з переписування задачі, а не з коду.

### K. T112 — звести демо до шести вкладок
**Заблокована конфліктами** й тепер ще й зчеплена з двома іншими: T80 прибирає вкладку
`Staff · Flood`, а T111 лишив ризик — `Staff · Magnetic` кандидат на зняття, тоді як
`staffBrigade.ts` має **нуль** `gridCell`, тобто колізію посад не буде де показати.

### L–N. Ризики брифінгу без своєї задачі
Ризик 9 (список маркерів програмних рушіїв старіє мовчки) · 11 (критерій A2 не зелений у
кожному порядку прогону) · 12 (юніт-перф-тести брешуть під навантаженням) · 17 (T111 без e2e)
· 19 (вартість кадру превʼю на 1M не виміряна) · `oxfmt` не гейт · п'ять HARNESS-спеків не
виконуються ніде.

---

## Рішення людини, не агента

### ~~Р1. Який рушій контуру лишається (T80)~~ — ✅ **закрито 2026-09-06**, агенда це проґавила
Рішення: `button-group` єдиний, `cell-flood` без замовника. **Тепер це не розвилка, а
кандидат B** — і найбільший агент-реді обсяг у репо.

### ~~Р2. Версія SDK для накопиченого~~ — ✅ **вирішено 2026-09-07: `0.3.0`**

### Р3. Сім розвилок T116 (org-level reparent) — **найдорожче з відкритого**
**Питання:** `reports/org-reparent/spec.md` §Відкриті питання.
**Ціна мовчання:** spec+plan написані й лежать; поки розвилки відкриті, вони старіють проти коду.
**Рекомендація:** перш ніж відповідати на сім питань — прогнати `plan-critique` (4 лінзи,
тіром, відмінним від `opus`, який писав). Критика може **зняти** частину питань, і тоді людині
лишиться менше. Це дешевший порядок, ніж відповідати на все, а потім критикувати.

### Р4. Чи є move / merge / clone-structure справою SDK взагалі (T117 Гап 2)
**Варіанти:** (1) SDK дає свої мутатори — велика фіча, транзакційність за правилом T104;
(2) лишається хостовою логікою: хост складає з `reparentPosition`/`setData` і сам склеює історію.
**Рекомендація:** (2) — і **назвати це вголос у `docs/USAGE.md`**. Хост і так комітить ці три дії
поза draft-чергою й навмисно не в undo, тобто вже поводиться з ними як зі своїми.
Задача сама визнає це прийнятним результатом.

### Р5. Демо після чистки (T112) · **Р6.** T56 · **Р7.** T61 (макет) · **Р8.** T67 Phase 2
Без змін. ⚠️ Р5 подорожчала: рішення про вкладки тепер зачіпає **три** задачі (T80, T111, T112).

---

## 🎯 Наступний хід: **A — T113, матриця згорнутих сиблінгів**

Один хід, TDD, без `spec-flow`: правило розкладки не додає публічного імені й не змінює
задокументованої поведінки методу — поріг пайплайна не перетинається.

**Чому саме він:**

1. **Це єдиний кандидат, де користувач уже бачив поламане** і сказав про це — «flat orgs і 100k
   orgs працюють неправильно». Решта відкритого — борг, ризики й плани.
2. **Є еталон, а не чисте поле.** Хост реалізував це правило на GoJS і виніс рішення «матриця чи
   ряд» в окремий модуль. Ми повторюємо, і навіть шов уже підказаний.
3. **Дешевий відкат** — найдешевший серед P1.
4. Закриває критерій T112 про матричну сцену, тобто трохи розплутує Р5.

**Чому не інші:**

- **B (T80, прибрати `cell-flood`)** — найбільша знахідка цієї агенди, але **не перший хід**:
  вище порога пайплайна (повний `spec-flow`), і його радіус перетинається з нерозв'язаною Р5 —
  він прибирає вкладку `Staff · Flood`, поки доля решти вкладок не вирішена. Зробити видалення
  двічі дорожче, ніж зачекати на одне рішення. **Наступний після T113.**
- **C (T115)** — цінність висока, але поки **жодної інтеграції не існує** (архітектурний факт
  №5): будувати шов для споживача, який не прийшов, — ставка на форму, якої ніхто не приймав.
  Природне місце — після відповідей на Р3/Р4.
- **D (T114)** — дешевий і несе важіль (гейт на помилки сторінки для **всіх** вкладок), але
  P3 за симптомом, і власна задача каже, що з T113 він **не пов'язаний**. Найкращий кандидат
  «на додачу», якщо після T113 лишиться час; окремим ходом — ні.
- **E (T102 блок Б)** — був рекомендованим і лишається чесним, але це стеля, якої сьогодні ніхто
  не торкається: гвардія тримає, жоден інпут не вбиває WASM. Видимий дефект іде першим.

---

## Зроблено з попередньої агенди (2026-09-06)

- **T111** — drop у зайняту клітину посуває / міняє / питає → `31b98b3` (мердж), звіт
  `reports/seat-collision/report.md`
- **T117 Гап 1** — root-guard для `reparentPosition` + видима відмова → `68854d7`
- **B. T110** — тринадцять записів CHANGELOG, гейт свідомо не доданий → `a4edf01`
- **Р2** — версія `0.3.0` → `d793c8c`
- **Брифінг перезібрано** проти 16 комітів, архітектурний факт №5 (хост не споживає SDK) →
  `d404e81`
- **A. T102 блок Б** — рекомендований попередньою агендою, **не взятий**: черга змінилась під
  користувацькі дефекти й читання хоста
