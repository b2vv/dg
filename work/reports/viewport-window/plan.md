# plan — T88 · вікно, що їде за камерою

**Спека:** `spec.md` (GATE 1 закрито 2026-08-29, всі рішення Р3–Р8 прийняті)
**Гілка:** `cursor/t88-viewport-window` · **База:** `f6074de`

Як. Що і навіщо — у спеці, тут не повторюється.

---

## 1. Розміщення (за `.claude/standards.md` → `## Розміщення`)

| Що | Куди | Чому саме туди |
|---|---|---|
| колбек камери, `spliceData`, колбек пошуку, шар скелетонів | `packages/sdk` | публічна межа й Pixi-рендер — обидва там |
| арифметика вікна, реалізація колбеків, генератор 100k | `packages/demo` | Р3: хост володіє арифметикою |
| — | `packages/core` | **нічого.** Межа WASM↔TS не зсувається: розкладка отримує ті самі дані, лише інший їх зріз |

Рядок Конституції про WASM-межу проведено й він **не спрацьовує**: жоден тип у `types.rs`
не змінюється, тож `rust-types.ts` регенерувати не треба.

## 2. Reuse-first

Звірено з кодом і CRG-графом (`code-review-graph`), плюс тайп-граф `ttsc-graph` по межі пакетів.

### Переюзуємо

| Механізм | Де | Для чого тут |
|---|---|---|
| `mergePartial` / `mergeById` | `data/mergeData.ts:15,27` | половина `spliceData` — додавання — уже написана |
| `SearchIndexService.append` | `interaction/SearchIndexService.ts:61` | зливає індекс замість перебудови |
| `resolveStaffWindowStart` | `scenarios/scaleStaff.ts` | чиста арифметика старту вікна, вже є й покрита |
| `worldBoxToScreen`, `screenRectInView` | `render/promoteMath.ts:50,212` | «що зараз під камерою» в екранних координатах |
| дисципліна стабілізації промоуту (`settleMs`) | `react/createReactPromoteOverlay.ts` | той самий дебаунс обслуговує обидві речі — T88 сам це називає |
| нотифікатор зміни в'юпорту | `OrgHierarchyDiagram.ts:294-303` | **узагальнюємо, а не дублюємо** — див. нижче |
| ліс із кількома коренями (T65) | `layout/` | 100k після зняття переприв'язки — це ліс фрагментів |

### Нове — і чому наявне не підійшло

| Нове | Чому не переюзали |
|---|---|
| публічний колбек камери в `callbacks.ts` | у контракті **немає нічого** про камеру; `subscribePromoteSync` спрацьовує на потрібне, але його ім'я заявляє промоут. Тому: **перейменувати внутрішній нотифікатор у нейтральний і підписати на нього промоут**, а не заводити другий поряд. Один нотифікатор, два споживачі |
| `spliceData({ add, removeIds })` | `mergePartial` уміє лише додавати; `SearchIndexService` має `append`, але **не має видалення**. Половини механізму просто не існує |
| шар скелетонів | у репо **немає жодного** placeholder-рендера, крім медійних заглушок (це про картинки, не про вузли) |
| колбек пошуку поза вікном | `diagram.search()` шукає лише завантажене й **мовчить** про це |
| генератор 100k без переприв'язки | наявний свідомо будує зв'язне піддерево (`scaleOrgs.ts:73-78`); саме це й ламає суванння |

### 🔴 Дві величини на ім'я «pad» — іменуються по-різному

T88 попереджає прямо, і попередження виконується: у промоуті `pad` — це **екранні пікселі**
навколо в'юпорта для DOM-карток. Наша величина — **запас у екранах** для матеріалізації даних.
Різні одиниці, різний зміст. Наша називається **`reserveScreens`**, і слово `pad` у ній не
з'являється.

## 3. Кроки (порядок Р8: скелет наскрізь → м'ясо)

### Етап 1 — наскрізна смуга на `staff-1m`

1. **Узагальнити нотифікатор.** `notifyPromoteSync`/`subscribePromoteSync` →
   нейтральне ім'я (`notifyViewChanged`/`subscribeViewChanged`). Промоут стає одним із
   підписників. Чисте перейменування, поведінка не змінюється.
2. **Публічний колбек.** `onViewportChange?(t: ViewportTransform, meta: { settled: boolean })`
   у `callbacks.ts`. Кадрово скоалесцений (як `requestPaint`); `settled` стає `true` після
   періоду спокою. Хост рахує арифметику на кожній події (вона дешева) і матеріалізує на
   `settled` **або** коли з'їдено запас.
3. **`spliceData`.** `{ add?: TRaw; removeIds?: Partial<Record<keyof DiagramData, readonly string[]>> }`.
   Ключована форма, а не пласкій список: id унікальні лише всередині колекції.
   Потрібен `removeByIds` у `mergeData.ts` і `remove` у `SearchIndexService`.
4. **Скелетони.** `setSkeletonBoxes(boxes: readonly WorldBox[])` — SDK малює прямокутники й
   **не знає, що вони означають**. Окремий Pixi-шар під шаром вузлів.
5. **Вікно в демо.** Підписка на колбек → `resolveStaffWindowStart` → `spliceData`.
6. **Виміри — і вони визначають решту:** `reserveScreens`, поріг перебудови, ціна перебудови в мс.

### Етап 2 — пошук

7. `searchBeyondWindow?(query, page): Promise<{ hits, hasMore }>` у контракті; `diagram.search()
   ` питає хоста, коли власний індекс порожній. Демо відповідає арифметикою конгруенцій.

### Етап 3 — blur

8. Двостановий blur за швидкістю камери з гістерезисом (Р2). **Не робиться, поки етап 1 не дав
   вимір** — можливо, з готовим запасом він не потрібен.

### Етап 4 — 100k

9. Зняти переприв'язку в `buildScaleOrgsWindow`; вікно = ліс фрагментів.
10. Те саме суванння в matrix і row-tree.

## 4. Сайд-ефекти

| Ефект | Кого зачіпає | Пом'якшення |
|---|---|---|
| три нові члени публічного API | усіх хостів | **усі опційні** — наявний хост не помічає зміни |
| перейменування `subscribePromoteSync` | хости, що його викликають | метод **не в `docs/USAGE.md`** і не експортований у типах — але промоут-оверлей у SDK його вживає. Лишаємо старе ім'я як делегат на один реліз |
| `diagram.search()` стає асинхронно ширшим | хости з вікном | без колбека поведінка **не змінюється** |
| демо 100k показує ліс замість зв'язного піддерева | видима зміна демо | названо в звіті й на екрані; це прийняте рішення Р7 |
| пам'ять при суванні | `staff-1m`, `scale-100k` | виселення робить профіль **сталим**; без нього ріс би до 1M |
| скелетони малюються щокадру, поки вікно не готове | кадр під час перебудови | шар — прості прямокутники без тексту; ціна входить у вимір етапу 1 |

## 5. План міграції

Міграції даних немає — жодна схема не змінюється. Міграція **API**, і вона адитивна:

1. Нові колбеки й `spliceData` виходять як додавання; жоден наявний виклик не ламається.
2. `docs/USAGE.md` оновлюється **тим самим комітом**, що й сигнатура (Конституція).
3. `subscribePromoteSync` лишається як тонкий делегат із поміткою про заміну — прибирається
   окремою задачею, не цією.
4. Демо переходить на новий шлях повністю: старий перехід по `pos-N` і по кліку **лишається**
   (він і далі потрібен для стрибка), суванння додається поряд.

## 6. Rollback

| Крок | Як відкотити | Що rollback НЕ поверне |
|---|---|---|
| 1–4 (SDK) | revert комітів; API адитивне, споживачів поза демо немає | — |
| 5–6 (вікно в демо) | revert; демо повертається до `pos-N` + клік | — |
| 9–10 (генератор 100k) | revert | **нічого незворотного**: генератор синтетичний, даних користувача немає |

Незворотних ефектів немає взагалі: ні міграції даних, ні зовнішніх викликів, ні записів
у сховище. Найгірший наслідок відкату — демо повертається до сьогоднішньої поведінки.

## 7. Конституція (`.claude/standards.md` → `## Конституція`)

| Рядок | Проведення |
|---|---|
| Тести перед кодом, success + failure | кожен крок §3 іде через `tdd`; етапи 1–2 мають чисті функції, які тестуються без Pixi |
| Ієрархія KISS → SOLID → … | **спрацював на Q1:** відхилено «механізм вікна цілком у SDK» — абстракція не доведена другим споживачем. Взято мінімальну межу |
| GoF лише за ≥2–3 повтореннями | патернів не вводиться; `spliceData` — метод, не фабрика |
| Межа WASM↔TS | **не зсувається** — `types.rs` не чіпається, регенерація не потрібна |
| Публічний API не змінюється мовчки | `docs/USAGE.md` у тому самому коміті — рядок 2 плану міграції |
| Pocock лише TS | увесь дифф — TS; на Rust не поширюється |

## 8. Приймальні сценарії

Три групи представлені всі. Рівень перевірки названий у кожному рядку; **ручних рядків — 2**
(21 і 22), і вони лишаються ручними назавжди, бо міряють кадр на залізі, а не поведінку коду.

### Happy path

| # | Сценарій | Передумова | Дія користувача | Очікуваний спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 1 | пан до краю довантажує | `Staff · 1M`, фокус за замовчуванням, вікно 600 матеріалізовано | панорамує вправо, аж поки правий край вікна пройде центр екрана | у в'юпорті є картки з індексом **більшим** за максимальний на момент початку жесту; жодного кліку й вводу не було | e2e `e2e/staff-1m.spec.ts` |
| 2 | суванння виселяє | те саме, після дії рядка 1 | продовжує панорамувати в той самий бік | число вузлів у сцені лишається в межах ±10% від стартового (не росте монотонно) | e2e `e2e/staff-1m.spec.ts` |
| 3 | пошук за іменем поза вікном | `Staff · 1M`, вікно на pos-350000 | вводить у пошук «Morgan Blake» | статус називає **число** знайдених і камера стоїть на картці з цим іменем; рядок «try pos-N» не показується | e2e `e2e/staff-1m.spec.ts` |
| 4 | 100k суванння в matrix | `100k orgs`, Collapse all (matrix) | панорамує вправо за край вікна | у в'юпорті є картки з індексом більшим за стартовий максимум | e2e `e2e/scale-100k.spec.ts` |
| 5 | 100k суванння в row-tree | `100k orgs`, режим за замовчуванням | панорамує вниз за край вікна | те саме спостереження, що й у рядку 4 | e2e `e2e/scale-100k.spec.ts` |

### Межі

| # | Сценарій | Передумова | Дія користувача | Очікуваний спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 6 | край адресного простору | `Staff · 1M`, фокус на pos-999900 | панорамує далі вправо | сцена не порожніє: останні картки лишаються на екрані, скелетони **не** малюються, статус не міняється | e2e `e2e/staff-1m.spec.ts` |
| 7 | початок адресного простору | фокус на pos-0 | панорамує вліво | те саме спостереження, що й у рядку 6, з протилежного краю | e2e `e2e/staff-1m.spec.ts` |
| 8 | вікно ширше за простір | тотал менший за розмір вікна (fixture) | відкриває вкладку | матеріалізовано рівно `total` карток, скелетонів немає | unit `packages/demo/src/scenarios/scaleStaff.test.ts` |
| 9 | нульове виселення | `spliceData` з порожнім `removeIds` | — (API) | число вузлів = старе + додане; жоден наявний вузол не зник | unit `packages/sdk/src/data/mergeData.test.ts` |
| 10 | виселення всього | `spliceData` без `add`, `removeIds` = всі поточні id | — (API) | `getData()` повертає порожні колекції; пошук по будь-якому запиту дає 0 | unit `packages/sdk/src/OrgHierarchyDiagram.data.test.ts` |
| 11 | дублікати в `add` | `add` містить id, який уже в сцені | — (API) | вузол один, а не два; поля взяті з патча | unit `packages/sdk/src/data/mergeData.test.ts` |
| 12 | id одночасно в `add` і `removeIds` | обидва списки містять один id | — (API) | вузол **присутній** (додавання після видалення), і це задокументовано в `docs/USAGE.md` | unit `packages/sdk/src/data/mergeData.test.ts` |
| 13 | пошук дає 10 000 збігів | «Morgan Blake» у демо-арифметиці | вводить запит | показано першу сторінку і **число всіх** збігів; UI не вішається | e2e `e2e/staff-1m.spec.ts` |
| 14 | максимальний зум-аут | `Staff · 1M`, зум до `far` | зумить максимально назовні | вікно **не** намагається матеріалізувати весь екран: число вузлів не перевищує стелю вікна | e2e `e2e/staff-1m.spec.ts` |

### Відмови

| # | Сценарій | Передумова | Дія користувача | Очікуваний спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 15 | швидкий жест обганяє дані | `Staff · 1M` | кидок камери на кілька екранів за один рух | у смузі попереду видно **скелетони** на місцях майбутніх карток; порожньої області без нічого немає | e2e `e2e/staff-1m.spec.ts` |
| 16 | стрибок `pos-N` | вікно на pos-100 | вводить `pos-900000` | до появи даних видно скелетони, після — картки на **тих самих** місцях (геометрія не стрибнула) | e2e `e2e/staff-1m.spec.ts` |
| 17 | колбек пошуку кинув помилку | хост-колбек кидає | вводить запит | статус називає, що зовнішній пошук недоступний; локальні збіги все одно показані; консоль без необробленого reject | unit `packages/sdk/src/OrgHierarchyDiagram.search.test.ts` |
| 18 | колбек пошуку не заданий | хост не передав колбек | вводить запит поза вікном | поведінка **точно як сьогодні**: локальний результат, без падіння | unit `packages/sdk/src/OrgHierarchyDiagram.search.test.ts` |
| 19 | два суванння наздоганяють одне одного | перше `spliceData` ще не завершилось | панорамує далі, поки триває перебудова | у сцені лишається **один** консистентний діапазон; дублікатів немає, «дірок» посередині немає | unit `packages/demo/src/app/viewportWindow.test.ts` |
| 20 | камера рухається після `destroy()` | діаграму знищено | — (API) | колбек камери більше не викликається; помилки в консолі немає | unit `packages/sdk/src/OrgHierarchyDiagram.lifecycle.test.ts` |

### Виміри — вердикт, а не спостереження

| # | Сценарій | Передумова | Дія | Очікуваний спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 21 | ціна кадру з blur без GPU | `Staff · 1M`, програмний WebGL, blur увімкнено | жест на кілька екранів | p50 кадру **≤ 33 мс**; гірше 30 fps не проходить (вердикт з T88 Р2) | **вручну**, стенд `e2e/t87-motion.spec.ts` |
| 22 | панорамування всередині запасу не подорожчало | той самий жест до і після фічі | жест у межах готового запасу | p50 кадру не більший за дофічевий більш ніж на 10% | **вручну**, той самий стенд |

## 9. Таски

*(після GATE 2)*
