# tasks — T88 · вікно, що їде за камерою

**План:** `plan.md` ревізія 2 · **Гейти:** обидва зелені (`critique.md`, `plan.md` §9)
**Позначення:** `[P]` — можна робити паралельно з попереднім; решта строго по порядку.

Номери рядків — це рядки приймальної таблиці `plan.md` §8.

---

## Етап 1 — наскрізна смуга на наявному API

### T88.0 — відновити стенд і зняти дофічеву базу 🔴 **перший, до будь-якої правки коду**
- **Файли:** `e2e/t87-motion.spec.ts` (відновити з `7b79211`), `work/reports/viewport-window/report.md` (створити)
- **Робить:** відновлює стенд руху; знімає p50/p95 на `Staff · 1M` тим самим жестом, що міряв T87
- **Готово, коли:** числа записані у звіт із назвою середовища (апаратний / програмний WebGL)
- **Закриває рядки:** 25
- ⚠️ Після цього таска база вже недосяжна — будь-яка правка коду її змінює

### T88.1 — абсолютна сітка staff
- **Файли:** `packages/demo/src/scenarios/scaleStaff.ts`, `packages/demo/src/scenarios/scaleStaff.test.ts`
- **Робить:** `col = i % STAFF_SCALE_COLS`, `row = floor(i / STAFF_SCALE_COLS)`; голова блоку й `reportLines` не залежать від `range.start`
- **Готово, коли:** `gridCell` посади pos-X однаковий для двох різних `range.start`
- **Закриває рядки:** 4, 7
- **Підстава:** GATE 2, збіг З4 (2 лінзи)

### T88.2 — публічний колбек камери
- **Файли:** `packages/sdk/src/callbacks.ts`, `packages/sdk/src/OrgHierarchyDiagram.ts`, `docs/USAGE.md`, тест `OrgHierarchyDiagram.lifecycle.test.ts`
- **Робить:** `onViewportChange?(t, meta: { settled, reason: 'camera' | 'resize' })`; джерело — `PixiHost.setOnViewportChange` **і** `setOnResize`; rAF-коалесцування + settle-таймер; `destroy()` знімає підписку й гасить таймер
- **Готово, коли:** колбек не спрацьовує після `destroy()`; ресайз дає `reason: 'resize'` з тим самим трансформом
- **Закриває рядки:** 21
- ⚠️ Нотифікатор промоуту **не чіпається** (GATE 2, збіг З2)

### T88.3 — арифметика вікна (чиста) `[P]` після T88.1
- **Файли:** `packages/demo/src/app/viewportWindow.ts` (новий), `viewportWindow.test.ts`
- **Робить:** з екранного прямокутника + `reserveScreens` → діапазон індексів і `windowSize`; дебаунс серії подій
- **Готово, коли:** більший екран дає більший `windowSize`; серія ресайзів дає одну перебудову
- **Закриває рядки:** 9, 12

### T88.4 — підключити вікно до `setData`
- **Файли:** `packages/demo/src/app/App.ts`
- **Робить:** підписка → арифметика → `buildScaleStaffWindow` → `setData`; серіалізація (черга, остання виграє); деградація при відмові воркера/WASM — сцена лишається на попередньому вікні, статус називає причину, черга скидається
- **Готово, коли:** пан за край довантажує; жодна колекція `getData()` не росте монотонно
- **Закриває рядки:** 1, 2, 15, 23

### T88.5 — спостережуваний сигнал вікна
- **Файли:** `packages/demo/src/app/App.ts`, `e2e/demoBridge.ts`
- **Робить:** статус `window <start>…<end> / total` + `data-window-start` на монтуванні
- **Готово, коли:** e2e може дочекатись перебудови, а не спати на константі
- **Закриває рядки:** розблоковує 1, 2 (GATE 2, N15)

### T88.6 — стрибок `pos-N` без `reload()`
- **Файли:** `packages/demo/src/app/App.ts`
- **Робить:** `runSearch` для `pos-N` іде через `setData`, а не `disposeDiagram() + create()`
- **Готово, коли:** після стрибка полотно те саме, камера не скинулась у fit
- **Закриває рядки:** 14
- **Підстава:** GATE 2, збіг З7 (2 лінзи)

### T88.7 — межі й перемикання `[P]` після T88.5
- **Файли:** `e2e/staff-1m.spec.ts`, `e2e/demo-audit.spec.ts`
- **Закриває рядки:** 5, 6, 8, 10, 22

### T88.8 — вимір 🔴 **гейт для всього далі**
- **Файли:** `work/reports/viewport-window/report.md`
- **Робить:** `reserveScreens`, поріг перебудови, ціна `setData` на вікні в мс — **окремо з перекриттям id і без** (перекриття вмикає повну `rebuildForScale`); порівняння з базою T88.0
- **Готово, коли:** три числа названі, і за ними винесено рішення про T88.11–T88.13
- **Закриває рядки:** 26

## Етап 2 — пошук (рішення Р5, лишене користувачем)

### T88.9 — колбек пошуку в SDK
- **Файли:** `packages/sdk/src/callbacks.ts`, `OrgHierarchyDiagram.ts`, `docs/USAGE.md`, `OrgHierarchyDiagram.search.test.ts`
- **Робить:** `searchBeyondWindow?(query, page): Promise<{ hits, total, hasMore }>`; матеріалізація хіта до фокуса; «остання виграє»; перевірка `destroyed` **після** await; валідація пейлоада → відмова, не «нуль збігів»
- **Готово, коли:** усі шість рядків відмов зелені
- **Закриває рядки:** 16, 17, 18, 19, 20, 24
- ⚠️ Форма повернення `search()` росте — **неадитивна** зміна, `USAGE.md` тим самим комітом

### T88.10 — пошук за іменем у демо
- **Файли:** `packages/demo/src/app/App.ts`, `packages/demo/src/scenarios/scaleStaff.ts`
- **Робить:** реалізація колбека через конгруенції (`FIRST.indexOf` з коду, не з тексту спеки); сторінка, не список
- **Готово, коли:** «Morgan Blake» дає число збігів і камеру на картці
- **Закриває рядки:** 3, 11

## Етап 3 — умовні (входять лише за числами T88.8)

### T88.11 — скелетони · **умова:** вимір показав видиму порожню смугу
- Першим варіантом хостовий оверлей на публічних `worldBoxToScreen`/`screenRectInView`; origin з `listPromoteBoxes`; стеля числом; гасіння при успішному `setData` і в гілці помилки
- **Закриває рядки:** 13

### T88.12 — `spliceData` · **умова:** `setData` не вклався в бюджет кадру
- Тоді ж доведеться розв'язати те, що `setData` закриває безкоштовно: видалення ребер без id, прунінг виділення й розгорток, гонка індексу, інкрементальний білдер у демо

### T88.13 — blur · **умова:** запас не закрив швидкий жест
- Не входить → A6 закривається записом «blur не реалізовано, підстава — X мс»
- **Закриває рядки:** 27

## Поза циклом

### T88.14 — 100k → окрема T-задача `work/tasks/T95-scale-100k-viewport-window.md`
- **Умова входу:** числа A2/A3 з T88.8
