# T115 крок 2 — якір експандера. Звіт

**Цикл:** повний spec-flow, `a8043a0` → `ed74790` (18 комітів, 33 файли, +1823 / −161)
**Реліз:** `0.5.0` · **Спека:** [`spec.md`](./spec.md) · **План:** [`plan.md`](./plan.md) ·
**Кроки:** [`tasks.md`](./tasks.md)

---

## 1. Що зроблено

Кнопка розгортання org-картки дістала власний тест-якір. До цього «клікнути саме по chevron, а не
по картці» не виражалось **узагалі**: кут кнопки залежить від режиму сцени, тож ззовні з боксу
картки його не обчислити. Єдиний обхід — програмне розгортання, а воно обходить рівно той шар, де
живе вада, і лишається зеленим навіть коли кнопка **перекрита карткою**.

| Крок | Що поїхало |
|---|---|
| К1 | бокс кнопки віддає **той, хто її монтує** — `mountOrgNodeChrome` → `OrganizationNodeView.expanderBox()`; плоскі числа в координатах картки, не контейнер і не `getBounds()` |
| К2 | `DiagramRenderer` зсуває його в **світові** координати; `hasChildren` — лише деревна гілка |
| К3 | публічна пара полів у `TestAnchorCandidate`; promote читається **в момент виклику** |
| К4 | `toggleOrgExpand(orgId): Promise<boolean>` на фасаді + `USAGE.md` §7 |
| К5 | другий якір `node-<testId>-expander` і **окрема** гілка кліку |
| К6 | `flat-orgs` злазить із містка; новий `e2e/expander-anchor.spec.ts` |
| К7 | `USAGE.md` §14: два поля, сім станів, `orgTreeChrome`, дві названі межі |
| К8 | CHANGELOG, `0.5.0`, `package-lock.json` |

**Публічна поверхня:** два **необов'язкові** поля (`expander`, `hasChildren`), один новий метод
(`toggleOrgExpand`), один другий якір у DOM. Плюс одна **ламальна** зміна, знайдена рев'ю:
`TestAnchorOverlayDiagram` дістав обов'язковий метод — див. §7.

**Асиметрія кліку — це і є фіча.** Якір вузла кличе `focusByTestId` і **виділяє**; якір
експандера кличе `toggleOrgExpand` і селекції **не чіпає**. Без цієї різниці тест «клік по кнопці
не виділяє вузол» не можна написати взагалі — а він і був причиною циклу.

---

## 2. Протестовані сценарії

| Гейт | Було | Стало |
|---|---|---|
| юніт sdk | 978 | **1004** |
| юніт demo | 104 | 104 |
| e2e | 78 | **81** (звірено `--list`) |
| Rust | 24 | 24 (не чіпався) |
| `test:scripts` | 19 | 19 |

Усі зелені: typecheck, oxlint, `check:docs`, `test:scripts`, sdk, demo, rust, e2e.

⚠️ **Число — це твердження і про машину теж.** Повний прогін робився, коли поруч нічого не
молотило. Один раз за цикл `flat-orgs.spec.ts:76` (сітка T113) упав **одразу після** важкого
фонового прогону; наступні три прогони поспіль — 23/23. Це вже названий клас T101, і саме цей
тест у ньому вже перелічений.

---

## 3. Неочевидні знахідки

### 3.1 Чотири тести були зелені без фічі — і механізм щоразу той самий

Це головна знахідка циклу, і вона про метод, а не про код. **Асерт задовольнявся хибним станом:**

| Де | Асерт | Чому був істинний і без фічі |
|---|---|---|
| К1 | `x + width <= style.width` | нерівність терпить **лівий** край: мутація «обнулити x» лишала 32/32 зеленими |
| К2 | «бокс усередині картки» | терпить **локальні** координати: зсув у світові знімався безкарно |
| К5 | `unhandledrejection` порожній | подія в цьому середовищі **не приходить ніколи** — порожнє порівнювалось із порожнім |
| К6 | «після кліку видно `node-org-2`» | вузол видно **й до** кліку: вкладка відкривається мінімумом (T97), а не матрицею |

🔑 **Читання цього не ловить за побудовою.** Очима видно, що асерт правдивий; питання інше —
*чи існує стан, у якому він правдивий і **без** фічі*. Мутація спіймала всі чотири.

Формулювання другої сесії, яке варте того, щоб пережити цикл: **тест, який не червонів, доводить
лише те, що його написали.**

### 3.2 Тест, що бере ту саму форму ключа, що й реалізація, погоджується з нею

`listTestAnchors` порівнював promote **точним** збігом `promoted.has(box.id)`. Я перевірив цю
гілку читанням — пройшов `listPromoteBoxes` → `listNodeBoxes` → `listBoxes`, побачив, що оверлей
бере id з тих самих боксів, і закрив питання. Висновок був **істинний для тієї множини, яку я
знав**: внутрішнього React-оверлея.

Але `setPromotedNodeIds` **публічний**, і найкоротший шлях хоста — голий `'root'`, тоді як реєстр
кладе `organization:root`. Друга сесія довела прогоном: в'юха схована, а якір далі показує на
кнопку, якої на екрані немає. Мій тест цього не бачив **за побудовою** — він клав той самий
типізований ключ, що й реалізація.

**Правило:** якщо в системі існують дві форми одного ключа, тест **зобов'язаний** узяти ту, якої
реалізація не бере.

### 3.3 Крок вніс мовчазну регресію, і жоден гейт не міг її побачити

Chevron ліг поверх якоря картки з `pointer-events: auto` і взяв лише `click`. Слухач
`contextmenu` лишився на картці — тож на ділянці кнопки меню діаграми не відкривалось,
`preventDefault` ніхто не кликав, і користувач діставав **браузерне** меню поверх канви.

Тесту на праву кнопку по chevron не існувало, бо **до цього кроку там не було чого натискати**.
Тобто клас помилок, який виникає рівно в момент появи нового шару.

**Правило:** новий шар над старою ділянкою екрана мусить перенести на себе **всі** її обробники,
а не лише той, заради якого його додали.

### 3.4 «Випадок не будується» виявився помилкою вимірювання, а не фактом

Охоронець `if (!org)` у `toggleOrgExpand` пережив мутацію. Я записав це в код як прийнятне: мовляв,
випадок «id невідомий, але діти є» не будується, бо висячий `parentOrgId` відсікає валідатор.

Спроба написати тест **упала на валідаторі** — і я прочитав падіння як підтвердження гіпотези.
Насправді `validateOrgHierarchy` кличе лише **деревна** розкладка, а сцену обирає
`positions.length > 0`. Досить було додати одну посаду. Тобто експеримент був не просто невдалий,
а **упереджений**, і я не помітив, бо результат збігся з очікуванням.

Побічний факт заведено окремо — **T124**: штатна сцена не валідує org-ієрархію взагалі, тож цикл
у `parentOrgId` там не ловить ніщо.

### 3.5 Виправлення, ухвалене в одному місці, не доїжджає до переліку — **п'ять** разів за цикл

План (§6a проти виконуваних секцій) · брифінг (шість таблиць описували видалений рушій) ·
`docs/USAGE.md` (рядок недосяжного стану) · `spec.md` (чотири приймальні критерії) ·
`plan.md` §8 (рядок 6).

Механізм щоразу той самий: нова секція пишеться свідомо, а перелік виглядає перевіреним **саме
тому, що детальний**. Реалізатор бере рядок таблиці, а не абзац під нею.

**Практичне:** приймальну таблицю перечитувати на **кожному** GATE і після **кожного** рішення —
а не лише тоді, коли `grep` по імені спрацював. При **видаленні** grep не спрацьовує взагалі:
стара правда сидить у тезі, не в ідентифікаторі.

### 3.6 Перелік станів мусить бути переліком станів **того API, у чиїй доці він стоїть**

Мій перелік мав вісім рядків, і один із них — «хост не підписався на жоден з трьох обробників» —
через `OrgHierarchyDiagram` **недосяжний**: фасад передає `onOrgContextMenu` безумовно. Тобто я
описував хостові стан, якого він не вміє відтворити. Для хоста станів **сім**; восьмий лишився
окремим абзацом із поміткою «лише через прямий `DiagramRenderer`».

---

## 4. Скріншоти

Не робились: зміна не має власного вигляду. Chevron малюється як і до циклу — усе нове живе в
**невидимому** шарі якорів (`opacity: 0.001`) і в даних. Візуальну частину стережуть наявні
геометричні тести, які цикл не правив.

---

## 5. Проходка по user flow

Хост, що мігрує з GoJS, писав так:

```ts
await page.evaluate(() => window.__orgHierarchy.expandOrg('org-7'));   // обхід
```

Тепер так:

```ts
await page.getByTestId('node-org-7-expander').click();
await expect(page.getByTestId('node-org-9')).toBeVisible();
```

І два тести хоста, заради яких усе робилось, стають виразними: клік по кнопці **не** виділяє
вузол і **не** відкриває сайдбар. Наш власний `e2e/expander-anchor.spec.ts` доводить перше з них
у браузері — з оракулом, оживленим у тій самій пробі.

---

## 6. Ризики й обмеження

| # | Обмеження | Статус |
|---|---|---|
| 1 | `interactive: true` — прод-небезпека, і цикл **подвоїв** її вагу: якорів тепер два на вузол, і другий лежить саме на кнопці | стереже лише дока (§14); машинної перевірки немає й не планується — T115 крок 4 |
| 2 | Якорі описують **останній успішний** кадр або жоден | названо в §14; лікувати означало б не чистити реєстр на вході кадру |
| 3 | Однакові `testId` дедуплікуються **мовчки** | названо в §14 |
| 4 | Шар перебудовується цілком на кожен sync — handle після руху камери detached | названо в §14; ліки на боці тесту (брати локатор заново) |
| 5 | `toggleOrgExpand` успадкував **нетранзакційний** шлях `expandOrg`/`collapseOrg` | названо в §7 і в докстрінгу; приведення до T104/T109 — окремий цикл |
| 6 | Gojs-експандер: ручний фолбек хіт-тесту не працює **взагалі** | [T123](../../tasks/T123-gojs-expander-has-no-manual-hit-fallback.md), окремо; цикл цього не вносив і не погіршив |
| 7 | Штатна сцена не валідує org-ієрархію | [T124](../../tasks/T124-staff-scene-does-not-validate-org-hierarchy.md), окремо |

---

## 7. Що дало рев'ю

Три незалежні джерела, жодне — не автор.

**Друга сесія (`Dg 2`), покроково + цикл цілком.** Дала єдину **блокуючу** знахідку коду за весь
цикл (promote, §3.2) і єдину **регресію** (contextmenu, §3.3) — обидві виміром, не читанням.
Плюс: не заповнювати `hasChildren` у штатному шляху (аргумент про **пару** сильніший за моє
питання); не додавати містку `getSelection()` (був би дворядковим форвардером до публічного
`getSelections()` — та сама форма, яку T119 крок 0 щойно видалив); рядок 2 таблиці станів
(§3.6); мертвий міст, що лишився стояти після К6; чотири анотації в приймальній таблиці.

**Спекова вісь (`code-review`, sonnet).** Реалізація контракту — без зауважень; **дірки у
покритті**: п'ять сценаріїв §8 не пінило ніщо, і серед них №14, який спека сама назвала
вирішальним словами «при `scale === 1` хибний підхід теж зелений». Закрито `ed74790`.

**Стандартна вісь (`code-review`, sonnet).** Одне тверде порушення — `work/TDD.md` §4: шість
кроків імплементації везуть тест і код **одним** комітом, тож RED ніде не доведений комітом.
Приймаю: RED був, я його прогонив і називав числом у листуванні, але **артефакт** — це коміт, і
його немає. Фікс-коміти (`c74bf30`, `ecb917a`, `27dc8a5`) показують справжній test-first.
Знахідка, що коштувала CHANGELOG-рядка: `TestAnchorOverlayDiagram` дістав **обов'язковий** метод —
ламальна зміна публічного типу, якої `check:docs` не бачить за побудовою.

---

## 8. Post-deploy

⚠️ **Виконується після пушу — `origin/main` відставав на 12 комітів, коли цикл був готовий.**
Pages деплоїться з `push` у `main`, тож без пушу прод показував би позаминулий тиждень.

Веде друга сесія. Перевірки, узгоджені заздалегідь:

1. `prod-smoke` за наявним конфігом.
2. 🔴 **На звичайній сторінці якорів немає взагалі** —
   `document.querySelectorAll('[data-testid^="node-"]').length === 0` без `?e2e=1`. Це
   архітектурний факт №7 п.2, і цикл подвоїв його вагу.
3. З `?e2e=1`: `node-<id>-expander` існує; клік розгортає й **не** виділяє (у `#status` немає
   `selected`); клік по якорю вузла виділяє — той самий оракул із живістю, що й у К6.
4. Права кнопка на chevron відкриває меню діаграми, а не браузерне (регресія §3.3 — у jsdom вона
   виглядає інакше, ніж у браузері).
5. Консоль без помилок, зокрема без необробленого reject із гілки кліку.
