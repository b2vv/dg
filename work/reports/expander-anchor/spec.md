# T115 крок 2 — якір експандера · spec

**Дата:** 2026-09-10 · **Базис:** `main` @ `dd2a095` · **Задача:**
[T115](../../tasks/T115-test-seam-parity-with-host.md) крок 2 · **Рішення:**
[`test-seam/decisions.md`](../test-seam/decisions.md) §Рішення 1 · **Цикл:** повний `spec-flow`

**Поріг пайплайна спрацював** ([`.claude/standards.md` §Поріг](../../../.claude/standards.md)):
змінюється **публічний API SDK** — тип `TestAnchorCandidate` експортується з кореневого барелю, а
`listTestAnchors` описаний у `docs/USAGE.md` §14 з T115 кроку 3. Решта критеріїв не спрацювала:
Rust не чіпається, межа WASM ↔ TS і контракт `main ↔ worker` не рухаються.

---

## Проблема

Кнопка розгортання — це Pixi-контейнер усередині картки. Ззовні її не видно **ніяк**: ні в
DOM-якорях оверлея, ні в `listTestAnchors()`, ні в жодному публічному методі. Хост, маючи бокс
вузла, обчислити її не може: зсув кнопки залежить від режиму сцени.

### Це не гіпотеза про хост — потребу він довів своїм виміром

2026-09-10 сесія, що веде e2e в `cassiopeia-admin-ui`, назвала **два наявні тести**, які без
якоря перестають існувати:

| Тест | Що доводить |
|---|---|
| `tests/organization-hierarchy-diagram.e2e.ts:312` | клік по експандеру перемикає дітей **без вибору вузла** |
| `tests/organization-hierarchy-diagram.e2e.ts:328` | розгортання/згортання **ніколи не відкриває сайдбар** |

🔑 **Аргумент, який робить це не питанням зручності.** Обидва тести доводять **негативне** — що
клік по кнопці **не** виділяє вузол і **не** відкриває сайдбар. Виклик API (`expandOrg`,
`focusByTestId`) розгортає сцену, **обходячи рівно той шар**, у якому живе можлива вада:
попадання в кнопку проти попадання в картку. Проба, яка розгортає програмно, лишається зеленою
навіть на коді, **де кнопка перекрита карткою**.

Отже «нічого не робити» коштує не зручності, а **єдиного гейта** на цю поведінку.

### І ми платимо за ту саму дірку самі

`e2e/flat-orgs.spec.ts:72` клікає вузол через DOM-якір і працює. Розгортання в тому ж файлі йде
через `expandOrg(page, 'org-1')` (`e2e/demoBridge.ts:5-9`), тобто через міст `window.__demoE2e` —
рівно той обхід, який T115 хоче прибрати в хоста. Дірка не теоретична.

### 🔴 Чому наївне `expander?: WorldBox` не годиться

Кнопки може не бути з **чотирьох** різних причин, і лише дві з них — властивості моделі:

| Причина | Природа | Джерело |
|---|---|---|
| немає дочірніх організацій (дерево) | **модель**, стабільна | `DiagramRenderer.ts:1118-1127` |
| немає посад під карткою (штат) | **модель**, стабільна | `DiagramRenderer.ts:1145-1157` (з'явилось `0.4.1`) |
| `lod === 'far'` — chrome не монтується | **кадр**, залежить від зуму | `OrganizationNode.ts:246` |
| `staff-expand && gojsTree` — ранній вихід | **кадр**, залежить від конфігурації сцени | `OrganizationNode.ts:251-253` |

Якщо віддати лише «бокс або `undefined`», то `undefined` означатиме всі чотири одразу. Наслідок
конкретний: тест «у листа немає розгортання» **пройде й тоді, коли камера просто від'їхала**. Це
той самий клас «зелений із хибної причини», який у цьому репо вже коштував і хибної спеки, і
непотрібно запланованої роботи.

---

## Користувацькі сценарії

**С1. Хост клікає саме по кнопці.** e2e бере `page.getByTestId('node-<id>-expander').click()`,
Playwright сам чекає на стабільність і б'є в центр елемента. Тест `:312` доводить, що вузол при
цьому **не** виділився — і доводить це на тому ж шарі, де живе вада.

**С2. Хост доводить, що лист не має розгортання.** Питає `listTestAnchors()`, бачить у вузла
`hasChildren === false` і **відсутність** бокса експандера. Твердження не залежить від зуму: якщо
камера від'їхала, `hasChildren` лишається `false`, і тест не проходить «випадково».

**С3. Хост відрізняє три стани, а не два.** `hasChildren === false` — лист; `hasChildren === true`
без бокса — кадр не показує кнопку (привід підвести камеру); **підписки немає** — кнопки не буде
ніколи, і жодна камера цього не змінить. Третій стан додано після GATE 1, див. Г2.

**С4. Наші власні e2e позбуваються моста.** `flat-orgs.spec.ts` перестає кликати `expandOrg`
через `window.__demoE2e` і клікає по якорю — тобто ми перестаємо платити за дірку, яку описуємо
хосту.

**С5. Хост, який нічого не міняв, не помічає нічого.** Нове поле необов'язкове; наявний код, що
читає `testId`/`kind`/`ref`/`world`, компілюється й поводиться як раніше.

---

## Acceptance criteria

🔴 **Чотири рядки цієї таблиці цикл переписав рішеннями, і таблиця лишилась первісною.**
Позначено 2026-09-11 після рев'ю циклу другою сесією. **Заднім числом не переписую** — анотую
тим, що їх замінило: таблицю читають як контракт, і слід рішення тут цінніший за охайність.

Це **п'ятий** випадок тієї самої форми за один цикл: виправлення, ухвалене в одному місці, не
доїхало до переліку, який виконують. Ловили в плані (§6a проти виконуваних секцій), у брифінгу
(шість таблиць описували видалений рушій), у `docs/USAGE.md` (рядок 2 недосяжного стану) — і ось
у спеці. **Практичне:** приймальну таблицю треба перечитувати **на кожному** GATE і після
**кожного** рішення, а не лише перед стартом.

| # | Критерій | Чим перевіряється |
|---|---|---|
| A1 | ~~`hasChildren: boolean` **обов'язкове**~~ → **необов'язкове й лише для організацій.** Змінено Г3 у цьому ж файлі: `listTestAnchors` будує кандидатів з усіх боксів, а для персони питання «чи є діти» відповіді не має — `false` там читалося б як доведене «немає» | `npm run typecheck`; наявний код-споживач не правиться |
| A2 | ~~юніт: бокс кнопки лежить **усередині** боксу картки~~ → **рівність до правого краю.** Змінено на К2: «всередині» **виміряно як недискримінуюче** — мутація «зняти зсув у world» лишала тест зеленим, бо локальна координата теж потрапляє в діапазон картки | юніт на рівність `e.x + e.width === card.x + card.width - 4` |
| A3 | Дерево: у вузла з дітьми бокс є, у листа немає, `hasChildren` відповідає | юніт на `listTestAnchors` |
| A4 | ~~Штат: `hasChildren` чесний і там — джерело `hasStaff`~~ → **у штаті немає ЖОДНОГО з двох полів.** Змінено рев'ю К2/К3: поле чесне саме по собі, але хост читає **пару**, а пара «ознака є, бокса немає» означає «підведи камеру» — у штатній сцені це неправда двічі (chevron на екрані, камера ні до чого) | юніт |
| A5 | 🔴 `lod === 'far'`: бокса немає, але `hasChildren` **не змінюється** | юніт — саме цей кейс відрізняє модель від кадру |
| A6 | Оверлей вішає другий якір `data-testid="node-<testId>-expander"` для **org-дерева** там і лише там, де є бокс | юніт на `createTestAnchorOverlay` |
| A7 | Якір експандера **не з'являється** для листа | юніт |
| A8 | Наш e2e розгортає через якір, а не через `window.__demoE2e` | `e2e/flat-orgs.spec.ts` зелений без `expandOrg` |
| A9 | Клік по якорю експандера **не виділяє** вузол — те, що доводить `:312` хоста. ⚠️ Нинішній оверлей робить протилежне (Г1), тож це критерій **на нову гілку кліку**, а не на наявну | e2e демо |
| A10 | Геометрія карток і контурів не змінилась | наявні тести рендера **без правок** |
| A11 | `docs/USAGE.md` §14 описує обидва поля й правило «модель проти кадру» | `check:docs`; рев'ю |
| A12 | CHANGELOG має запис; версія піднята (`0.5.0` — барель росте) | `packages/sdk/package.json` |

**Мірник «нічого не зламалось»:** повний `npx rstest run` (підсумкове число), `npm run typecheck`,
`npm run lint`, `npm run check:docs`, повний e2e.

⚠️ **Правка 2026-09-10: попередня редакція цього абзацу стояла на спростованій підставі.**
Тут було написано, що прогін уміє мовчки не виконати шість тестів, тож e2e-критерії вимагають
звіреного числа. **Режиму не існує** — п'ятнадцять прогонів його не відтворили
([T122](../../tasks/T122-e2e-can-under-run-silently-and-stay-green.md)). Ті шість «зниклих»
тестів **упали**, а сховала їх команда виміру: `| tail` відрізав блок падінь, який Playwright
друкує **над** рядком `passed`, і пайплайн підмінив код виходу власним нулем.

**Що з цього лишається чинним для A8, A9 і A13:**

1. 🔴 **Прогін не проганяється через `| tail` / `| head` / `| grep`.** Повний вивід — у файл,
   і читається файл. Це правило, а не побажання: воно вбило шість видимих падінь у двох різних
   сесіях за один день.
2. **Число звіряється з `npx playwright test --list`** — але **не** тому, що прогін уміє мовчати
   (не вміє), а тому, що специфікація може перестати **знаходитись**: за цей тиждень число вже
   рухалось (79 → 78, коли T80 забрав flood-тест). Мовчазне «файл більше не матчиться» — клас,
   якого ніщо інше тут не ловить.
3. **Флак під навантаженням реальний і видимий** — 3 падіння з 15 прогонів на трьох різних
   тестах. Тобто e2e-критерій вважається виконаним лише тоді, коли падіння **немає**, а не коли
   його **не видно** в обрізаному хвості.

---

## Out of scope

- **Якір кнопки меню (`org-menu`).** Той самий механізм, але потреби ніхто не заявляв. Додати
  пізніше буде дешево саме тому, що шлях бокса до якоря вже буде прокладений.
- **Якір штатного chevron.** Виміряно 2026-09-10: у хоста немає ні містка, ні тесту, ні згадки.
  Робити його означало б будувати другого споживача-привида поряд із першим. Шлях лишається
  прокладеним — якщо замовлять, це буде правка на кілька рядків, а не новий цикл.
- **Гейт середовища (T115 крок 4).** Абзац доки; іде **всередині** цього циклу як частина A11, а
  не окремим ходом — але поведінки не міняє.
- **Перейменування `initContourWasm`** та інший борг імен — не тут.
- **Зміна форми `world`.** Координати лишаються сценовими; хост переводить їх сам через
  `worldBoxToScreen`, як описано в §14.

---

## Правки після GATE 1 (`Dg 2`, 2026-09-10) — три блокуючі, усі реальні

Гейт виносила **інша сесія**, не автор. Кожну знахідку перевірив сам; жодну не зняло звуження
обсягу після відповіді хоста.

### Г1 🔴 Клік по якорю експандера **виділив би вузол** — саме те, що A9 забороняє

Обробник кліку в оверлеї **один на всі якорі** й кличе `focusByTestId`
(`react/createTestAnchorOverlay.ts:82-87`) → `revealPath` → `focusNode` → `applySelection`
(`OrgHierarchyDiagram.ts:1486`). Демо монтує оверлей із `interactive: true` (`App.ts:604`).

Тобто A8 недосяжна, а A9 стверджувала протилежне до наявного коду — і це **рівно той тест хоста
`:312`**, заради якого фіча існує. Якір, що виділяє вузол, доводив би зворотне до потрібного.

**Потрібен другий шов**, якого спека не мала: окрема гілка кліку для якоря експандера.

⚠️ **Поправка до оцінки гейта:** він написав, що поріг спрацьовує «двічі, не раз». Перевірив —
**ні**: `expandOrg` і `collapseOrg` **уже публічні** (`OrgHierarchyDiagram.ts:843`, `:861`) і вже
описані в `docs/USAGE.md`. Нового публічного **методу** не треба.

Лишається вибір форми, і це рішення **плану**, не спеки:
- **(а)** якір несе `expanded`, оверлей сам обирає `expandOrg`/`collapseOrg` — росте публічний
  тип, який ми й так міняємо;
- **(б)** з'являється `toggleOrg(orgId)` на фасаді — новий публічний метод, зате знімає наявну
  асиметрію (у штату тогл є, у дерева немає).

### Г2 🔴 Причин **п'ять**, і п'ята — не модель і не кадр

`orgTreeCardOptions` не створює chrome **взагалі**, якщо хост не дав жодного з
`onOrgContextMenu` / `onOrgExpand` / `onOrgCollapse` (`DiagramRenderer.ts:1099-1105`), і навіть із
дітьми вимагає `onOrgExpand || onOrgCollapse` (`:1119`).

Це **підписка хоста** — стала на весь час життя діаграми. Сценарій С3 радив у такому разі
«підвести камеру»; для цього стану це **вічно хибна порада**. Отже станів три, а не два:

| Стан | Що означає | Що робити |
|---|---|---|
| `hasChildren === false` | лист — кнопки й не повинно бути | нічого |
| `hasChildren === true`, бокса немає, підписка є | кадр не показує кнопку | підвести камеру |
| **підписки немає** | кнопки не буде **ніколи** | підписатись на `onOrgExpand`/`onOrgCollapse` |

### Г3 🔴 `hasChildren` обов'язкове на кандидаті, який буває персоною й посадою

`listTestAnchors` будує кандидатів з **усіх** боксів сцени, а `NodeWorldBox.kind` — це
`person | organization | position` (`SceneRegistry.ts:5-12`). Для персони питання «чи є діти» не
має відповіді; для посади воно означає інше (адмінпідлеглі). `false` для персони читалося б як
**доведене** «дітей немає».

**Рішення:** поле **необов'язкове** й **визначене лише для організацій**. Тоді
`kind === 'organization' && hasChildren === false` — однозначний лист, а відсутність поля при
іншому `kind` означає «питання не стосується», і це видно з `kind`, а не вгадується.

### Г7 (поза списком) — promote

У режимі `near-visible` Pixi-в'юха схована (`setPromotedNodeIds`), бокси лишаються, а шар якорів
(`zIndex 6`) стоїть над шаром promote (`5`). Хост дістав би бокс кнопки, **під яким кнопки
немає** — там чужий HTML-компонент. Демо вмикає promote штатно.

**Рішення:** критерій, що для промотованого вузла бокс експандера **не віддається**. Інакше
з'явиться якір, який мовчки клікає не туди.

### Що гейт підтвердив як чисте, з корисними уточненнями

- **Бокс придатний і доступний до першого paint** — chrome монтується синхронно в
  `OrganizationNodeView.create`, `rememberBox` у тому ж проході.
- ⚠️ **Але не через Pixi-`getBounds()`/`toGlobal()`**: вони дають координати **після** камери, а
  `world` — до неї. Джерело — `card.x + btn.x` плюс локальні розміри.
  Геометрій **дві**: icon 22×22 вгорі-праворуч (`orgNodeChrome.ts:105-118`) і gojs 26×26
  внизу-праворуч (`:50-52`).
  🔑 **A2 треба міряти на зумі ≠ 1** — при `scale === 1` хибний підхід теж зелений.
- **A5 пишеться як описано**, але доводиться на `listTestAnchors()`, а **не** на DOM: оверлей
  викидає якорі поза екраном (`createTestAnchorOverlay.ts:60`).
- **Геометрія й z чисті.** Ризик інший: утримане посилання на `expandButton` після наступного
  рендера — це об'єкт **минулого кадру** (`SceneRegistry.clear()` на вході). ⇒ бокс рахувати **в
  момент монтування**, а A10 має включати тест на **два послідовні рендери**.
- **Out of scope тримається сильніше:** `mountOrgNodeChrome` уже повертає **обидва** контейнери
  (`orgNodeChrome.ts:135`), і обидва відкидаються там само. Щойно бокс рахується при монтуванні —
  і меню, і штатний chevron додаються одним рядком.

### Додані критерії

| # | Критерій | Чим перевіряється |
|---|---|---|
| A13 | Клік по якорю експандера **розгортає/згортає й не виділяє** вузол | юніт на оверлеї + e2e демо |
| A14 | ~~Без **підписки хоста** бокса немає, і §14 називає це **третім** станом~~ → **керує опція `orgTreeChrome`, і станів для хоста сім.** Змінено Г2′ і К7: підписка через `OrgHierarchyDiagram` недосяжна — фасад передає `onOrgContextMenu` безумовно (`:778-780`), тож ранній вихід у рендерері не спрацьовує ніколи. Стан лишився в §14 окремим абзацом як «лише через прямий `DiagramRenderer`» | юніт; рев'ю доки |
| A15 | `hasChildren` присутнє **лише** для `kind === 'organization'` | юніт |
| A16 | Для промотованого вузла бокс експандера не віддається | юніт |
| A17 | Бокс правильний на **зумі ≠ 1** і після **двох** послідовних рендерів | юніт |

---

## Відкриті питання — В1–В3 закриті виміром хоста 2026-09-10

Усі три відповіді прийшли з адресами в коді хоста, тож нижче не переказ, а посилання.

**В1. Точка чи бокс? — ✅ бокс годиться.**
`OrgHierarchyTestApi.expanderCenter` віддає `{x, y} | null`
(`use-org-hierarchy-diagram.ts:59`, обгортка над `engine.expanderCenter` `:171`), а споживач —
`page.mouse.click(expander.x, expander.y)` (`organization-hierarchy-diagram.e2e.ts:317`, `:341`).
Тобто хост бере **центр і нічого з нього не рахує далі**. Бокс йому підходить: центр виводиться
тривіально, зворотне — ні. **Віддаємо бокс.**

**В2. DOM-якір, координати чи обидва? — ✅ DOM.**
Для `:312`/`:328` потрібен **справжній клік у кнопку**, а не програмне розгортання, і
`getByTestId(...).click()` дає його без арифметики. Координати хосту потрібні лише там, де вже є
`page.mouse` — і це кліки по **вузлу**, не по експандеру
(`edit-org-freshness-live.e2e.ts:87`, `breadcrumbs-trail.e2e.ts:195`).

⚠️ **Наслідок, який варто назвати чесно:** бокс експандера в `listTestAnchors()` **не має
споживача в хоста**. Він лишається в типі не «про запас», а тому, що **його споживач — наш
власний оверлей**: якір позиціонується саме з нього. Це та сама перевірка «хто мав би це кликати»,
через яку сьогодні пройшли `getLastContextMenu` (нуль викликачів → видалено) і `resolveTestId`
(викликач поза деревом → лишено). Тут викликач є, і він у дереві.

**В3. Дерево, штат чи обидва? — 🔴 лише дерево, і це перевертає мій дефолт.**
Місток `positions` (`use-positions-diagram.ts`) **експандера не має взагалі**; жоден
positions-e2e слова «expander» не містить. Усі експандер-тести хоста — у двох файлах
org-ієрархії. **Штатний chevron не замовлений.**

Спека раніше казала «за замовчуванням робимо обидва, бо асиметрія була б новим боргом». Це
відкинуто: **не будувати того, чого ніхто не просив**, важить більше — саме за це правило репо
двічі заплатив цього тижня. Обсяг зменшується вдвічі.

Що лишається симетричним: **`hasChildren` чесний в обох сценах**, бо джерело є з обох боків
(`hasChildren` у деревному chrome, `hasStaff` із `0.4.1`) і коштує нуль.

**В4. Ім'я поля.** Не блокує; вирішується на плані.

### Що хост додав понад питання

- **`hasChildren` він бере, і причина сильніша за мою.** Їхній `expanderCenter` віддає `null` для
  чотирьох станів **однаково**, і тест `:312` відрізняє «лист» від «камера від'їхала» лише тому,
  що спершу викликає `nodeCenter` того самого ключа як контроль (`:316`). Ознака моделі робить
  явним те, що зараз тримається на побічному ефекті.
- **Їхній місток уже гейтований `import.meta.env.DEV`** (`:168`, знімається в cleanup `:181-183`).
  Тобто порада §14 про `interactive: true` ляже в місце, де в хоста вже є звичка — ризик, який
  спека вважала відкритим, насправді менший.
