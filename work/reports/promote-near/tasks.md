# tasks — DOM-картки на `near` (промоут)

**План:** `plan.md` (GATE 2 зелений: 0 blocking, 0 дірок, 0 порушень)
**Гілка:** `cursor/promote-near-dom` · **PR:** у `main`

Порядок — за залежностями. Кожен таск називає рядки приймальної таблиці (`plan.md §8`).
За `work/TDD.md` тест пишеться **перед** кодом.

---

## T87.0 — Зміряти справжній `sync()` [блокує три рішення]

**Файли:** тимчасовий стенд (видаляється), число → `report.md`

🔴 **Це не тест, а вимір**, і він передує рішенням про дебаунс / трансформ шару / кеш.
Причина: мій попередній вимір рухав статичні `div`, а реальний `sync()` на **кожен кадр жесту**
обходить усі бокси сцени з лінійними `.find()` (`nodeRefs.ts:64,69,74`) і рендерить 44 компоненти.

- [ ] час одного `sync()` на кадр пана, вкладка `Staff · 1M`, 44 кандидати
- [ ] те саме без GPU
- [ ] обраний варіант (дебаунс / трансформ / кеш) **із причиною** в `report.md`

**Закриває:** передумову до T87.1–T87.3 · **рядки:** підготовка до 9, 9а

## T87.1 — Режим `'near-visible'` як гейт по LOD

**Файли:** `packages/sdk/src/render/promoteMath.ts`, `promoteMath.test.ts`

- [ ] тест: на `near` гейт відкритий, на `mid`/`far` закритий
- [ ] тест: дефолтний режим поводиться як раніше (регресія)
- [ ] код

**Закриває рядки:** 1, 2

## T87.2 — Порядок у `sync()`: фільтрувати → ховати

**Файли:** `packages/sdk/src/react/createReactPromoteOverlay.ts` + тест

🔴 Сьогодні `setPromotedNodeIds(ids)` іде **до** `screenRectInView` — з «усіма кандидатами» це
дірки в сцені.

- [ ] тест: нода за екраном **повертається канвасною**, а не зникає
- [ ] тест: на екрані піднімається все видиме
- [ ] код

**Закриває рядки:** 1, 5

## T87.3 — Предикат `shouldPromote`

**Файли:** `createReactPromoteOverlay.ts` + тест

Виконується **до** `setPromotedNodeIds`. Не заданий → усі кандидати.

- [ ] тест: `false` для ноди → вона на канвасі, дитини в шарі немає
- [ ] код

**Закриває рядок:** 11

## T87.4 — `pickNearestToCenter` + межі `maxPromoted`

**Файли:** `promoteMath.ts` (нова чиста функція), `promoteMath.test.ts`, оверлей

- [ ] тест: з 44 лишається 5, і саме центральні
- [ ] тест: `0` → жодної; `-1`, `NaN`, `Infinity` → без стелі; `2.5` → 2
- [ ] код

**Закриває рядок:** 6

## T87.5 — Error boundary на слот

**Файли:** `createReactPromoteOverlay.ts` + тест

🔴 Один `createRoot`, error boundary в SDK **немає жодного** — кидок гасить усі 44.

- [ ] тест: один компонент кидає → решта на місці, збійна нода **видима на канвасі**
- [ ] код

**Закриває рядок:** 10

## T87.6 — Гарантії відкладеної роботи (за рішенням T87.0)

**Файли:** `createReactPromoteOverlay.ts` + тести

- [ ] тест: `dispose()` під час відкладеного sync нічого не рендерить
- [ ] тест: sync з в'юпортом A, потім B → DOM відповідає **B**
- [ ] тест: два послідовні `sync()` → ідентичний DOM
- [ ] код

**Закриває рядки:** 8, 9а

## T87.7 — Геометрія картки в слоті

**Файли:** `promoteTypes.ts`, `OrgHierarchyDiagram.ts`, оверлей

- [ ] тест: слот несе радіус і відступи, різні для `organization` і `position`
- [ ] код

**Закриває рядок:** 3 (передумова)

## T87.8 — Демо: три способи вмісту

**Файли:** `packages/demo/src/**`

Зображення `cover` на всю ноду · `contain` по центру · текстовий фолбек, галуження за `entityType`.

- [ ] візуальний знімок (бейзлайн — із раннера CI, не локально)

**Закриває рядок:** 3

## T87.9 — e2e браузерні рядки

**Файли:** `e2e/promote-near.spec.ts` (**єдиний новий файл**)

- [ ] рядок 1: на `near` дітей = кількості видимих нод
- [ ] рядок 2: нижче порога — 0
- [ ] рядок 4: більший екран — більше карток
- [ ] рядок 5: вихід за екран → канвасна повертається
- [ ] рядок 8: поріг ×10 → вузли не накопичуються

**Закриває рядки:** 1, 2, 4, 5, 8

## T87.10 — Вимір ціни **в русі**

**Файли:** скрипт-вимір, число → `report.md`

- [ ] пан із піднятим шаром, 44 картки, FullHD, без GPU
- [ ] перетин порога 1.2 туди-сюди
- [ ] **гірше 30 fps не проходить** — і тоді це розмова, а не тихе зменшення ліміту

**Закриває рядок:** 9

## T87.11 — `docs/USAGE.md`

Шість пунктів переліку з `plan.md` К7 — режим, `shouldPromote`, `maxPromoted` із межами,
«два оверлеї не підтримуються», тиха поведінка після зняття режиму, експорт Pixi-only.

- [ ] док іде **тим самим комітом**, що й зміна сигнатури (конституція)

---

## Покриття приймальної таблиці

| Рядок | Таск | | Рядок | Таск |
|---|---|---|---|---|
| 1 | T87.1, T87.2, T87.9 | | 8 | T87.6, T87.9 |
| 2 | T87.1, T87.9 | | 9 | T87.10 |
| 3 | T87.7, T87.8 | | 9а | T87.6 |
| 4 | T87.9 | | 10 | T87.5 |
| 5 | T87.2, T87.9 | | 11 | T87.3 |
| 6 | T87.4 | | 12 | T87.6 |
| 7 | T87.2 | | | |

**Усі 13 рядків мають таск. Тасків без рядка немає** (крім T87.0 — це вимір, і він названий як вимір).
