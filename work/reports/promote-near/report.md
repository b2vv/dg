# report — DOM-картки на `near` (промоут)

**План:** `plan.md` · **Таски:** `tasks.md` · **Гілка:** `cursor/promote-near-dom`
Звіт — **дельта** до плану. Збіги згорнуті в посилання; розгорнуто лише те, що розійшлось.

---

## 1. Що зроблено

Кроки К1–К7 виконані **як у плані** — `plan.md §3`. Нижче розгорнуто лише те, що **розійшлося**
з планом або з'явилось поза ним.

### Розійшлося з планом

| # | План казав | Сталося | Чому |
|---|---|---|---|
| 1 | К3: кеш резолву (`id → node`) як ймовірне лікування | **кеш не потрібен** — замінено **порядком** | вимір К0: резолв 2.1 мс проти геометрії 0.005 мс. Фільтруємо по геометрії, резолвимо лише видимих. Дешевше за кеш і без інвалідації |
| 2 | К2: порядок «фільтрувати → ховати» | **«фільтрувати → резолвити → ховати»** | той самий вимір: резолв усіх 639 нод і був усією ціною фази |
| 3 | рядок 3 закривається **візуальним знімком** | закритий **e2e-твердженням** про три режими | знімок із раннера зафіксував би бейзлайн середовища, а не поведінку. Твердження «три різні режими» доводить рівно те, що обіцяно |
| 4 | `maxPromoted` — просто стеля | стеля **не обрізає сфокусовану картку**, але та займає одне з N місць | конфлікт виявився при реалізації: рядок 6 ламав би рядок 10а, забираючи фокус і незбережений ввід |

### З'явилося поза планом

| Що | Підстава |
|---|---|
| `settleMs` + трансформ шару | наслідок рішення GATE 1 «перемальовуємо на зупинці» (`spec.md`, Q1) |
| `shouldPromote` | К2а — рішення GATE 1 №2 не мало механізму |
| `onSlotError` + `SlotBoundary` | К3а; обидва дослідження React Flow підтвердили, що там boundary немає й одна нода валить усе |
| `chrome` у слоті, `getPromoteChrome` | К4 + знахідка `inventory-3-rendering.md` §7 про підлогу ширини 120 px |
| `getScreenSize`, `setOnResize` | §13.5 (forced layout у `sync`) і рядок 4 (ресайз не переобчислював набір) |
| `listPromoteBoxes` | наслідок №2 вище |
| «картка з фокусом не депромоутиться» | §13.1 → рядок 10а, знахідка дослідження React Flow |
| `setMaxPromoted`, `getPromotedNodeIds` | 🔴 **без замовника в плані**: перший знадобився, щоб виразити тест на конфлікт стелі з фокусом, другий — щоб e2e рядка 5 доводив **повернення на канвас**, а не лише зникнення з DOM |

**T87.0 — вимір (закритий).**

Стенд: `packages/demo/src/app/e2eBridge.ts` + `e2e/t87-measure.spec.ts`, коміт **`e488a28`**
(видалений наступним комітом — відновлюється з цієї sha). Стенд робив рівно те, що робитиме
майбутній `sync()`: справжній `listPromoteCandidates()`, справжній `setPromotedNodeIds()`,
справжній React-рендер справжніх `DefaultPromoteCard` і примусовий flush лейауту.

**Умови:** вкладка `Staff · 1M`, вікно 1920×1080, 639 нод у сцені, 40 ітерацій на точку,
перші 5 відкинуті. Прогони з GPU і з `SOFTWARE_GL=1`.

## 2. Протестовані сценарії

Приймальна таблиця з `plan.md §8`, плюс фактичний результат і вердикт.

| # | Сценарій | Фактично | Вердикт |
|---|---|---|---|
| 1 | На `near` піднімаються всі видимі | e2e: кількість карток у DOM = кількості схованих у Pixi | ✅ збіглося |
| 2 | Нижче порога — жодної | e2e: 0 карток і порожній набір схованих на зумі 0.8 | ✅ збіглося |
| 3 | Хост малює за типом сутності | e2e: три режими `cover` / `contain` / `text` від одного компонента | ✅ збіглося |
| 4 | Більший екран — більше карток | e2e: 1024×720 проти 1920×1080. **Знайшло дірку**: ресайз не переобчислював набір | ✅ після правки |
| 5 | Поза екраном — повертається канвасною | e2e: id більше **не в наборі схованих** → Pixi малює знову | ✅ після правки на рев'ю |
| 6 | `maxPromoted` як важіль | unit: з 44 лишається 5 центральних; чотири позадіапазонні значення | ✅ збіглося |
| 7 | Порожня сцена | unit: порожній шар, `setPromotedNodeIds([])`, нуль помилок | ✅ **після рев'ю** — тесту не існувало |
| 8 | Поріг ×10 — вузли не накопичуються | e2e: 10 циклів, кількість повертається до вихідної | ✅ збіглося |
| 9 | Ціна кадру, вердикт 30 fps | **+2 мс** p50, **+4 мс** p95, **0** кадрів > 33 мс на GPU | ⚠️ **збіглося з поясненням** — на перетині порога 9 кадрів > 33 мс, але без оверлея їх стільки ж і більше (сплески Pixi, не наші) |
| 9а | Перерахунки не ростуть із кадрами | unit: під час руху шар несе трансформ, `listPromoteCandidates` **не викликається** | ✅ збіглося |
| 10 | Компонент хоста кинув | unit: решта карток стоять, збійна нода **повертається на канвас** | ✅ збіглося |
| 10а | Картка з фокусом не депромоутиться | unit: фокус і текст на місці після виходу за екран | ✅ збіглося |
| 11 | Хост виключив ноду | unit: нода на канвасі, дитини в шарі немає, у Pixi не схована | ✅ збіглося |
| 12 | Нода зникла з даних | unit: картка **спершу піднята**, потім нода зникає → привида немає | ✅ **після рев'ю** — попередній тест стартував із порожніх даних |

**Не перевірено: 0 рядків.** Два рядки (7, 12) були заявлені закритими **без тесту** й закриті
на рев'ю; один (9) закритий **з поясненням**, а не бездоганно.

## 2а. Виміряне

### 2.1 Ціна одного `sync()`, мс (p50, усталений стан)

| Зум | LOD | Карток видно | `listPromoteCandidates` | `setPromotedNodeIds` | React-рендер | Лейаут | **Увесь `sync()`** |
|---|---|---|---|---|---|---|---|
| 2.14 | near | 28 | 2.1 | 0.7 | 0.1 | ~0 | **2.9** |
| 1.71 | near | 54 | 2.1 | 1.2 | 0.2 | ~0 | **3.4** |
| 1.37 | near | 66 | 2.1 | 1.4 | 0.3 | ~0 | **3.7** |
| 1.20 | near | 78 | 2.1 | 1.6 | 0.3 | ~0 | **3.9** |
| 0.52 | mid | 361 | 2.1 | 3.4 | 0.9 | ~0 | **6.4** |

**Вердикт:** у робочій смузі `near` один `sync()` коштує **2.9–3.9 мс**. Бюджет кадру 60 fps —
16.7 мс; порогу «гірше 30 fps» (33 мс) відповідає 33 мс. Тобто `sync()` на кожному кадрі пана
**технічно проходить** — він з'їдає близько чверті кадру 60 fps, а не ламає його.

⚠️ Це ціна `sync()` **окремо**, у циклі. Скільки коштує кадр, де `sync()` іде **разом** із
паінтом Pixi, цей вимір не каже — це окремий рядок 9 приймальної таблиці й окремий таск T87.10.

### 2.2 Без GPU — те саме

| Зум | З GPU | `SOFTWARE_GL=1` |
|---|---|---|
| 1.20 | 3.9 | 3.8 |
| 1.37 | 3.7 | 3.8 |
| 1.71 | 3.4 | 3.4 |
| 2.14 | 2.9 | 2.9 |

Різниці немає, і це не випадковість: у `sync()` немає жодної операції, яку виконує GPU.
Він увесь — обхід масивів, звірка прямокутників і DOM. **Для цієї роботи зеро-клієнт
не гірший за ноутбук**, і турбуватись за нього треба на боці паінту, а не тут.

### 2.3 Головне: дорога не геометрія, а резолв

| Що робиться над тими самими 639 боксами | p50, мс |
|---|---|
| взяти геометрію (`listNodeBoxes`) і відфільтрувати по екрану | **0.005–0.01** |
| те саме + резолв даних ноди (`listPromoteCandidates`) | **2.1** |

Різниця — понад два порядки. Уся ціна фази сидить у `resolveNodeRefInData` +
`resolveContextMenuNodeData`: до дев'яти лінійних `.find()` на кожен бокс
(`nodeRefs.ts:64,69,74`, `contextMenuPayload.ts:38-59`), і платяться вони за **всі 639**,
хоча на екрані 28–78.

Саме тому фаза **пласка**: 2.1 мс і при 28 картках, і при 78. Ціна не залежить від того,
скільки видно, — лише від того, скільки є в сцені.

**Контроль коректності:** кількість тих, що пройшли геометричний фільтр, збіглася з
кількістю тих, що пройшли повний шлях, **на всіх п'яти зумах** (28/54/66/78/361).
Фільтр по геометрії дає ту саму множину — переставляння порядку нічого не змінює у виводі.

## 2.4 Ціна в русі — T87.10, рядок 9

Стенд: `e2e/t87-motion.spec.ts`, коміт **`7b79211`** (видалений наступним; відновлюється звідти).
Жест — справжній drag по канвасу, **виконуваний усередині сторінки**, один рух на кадр.
Вкладка `Staff · 1M`, 1920×1080, зум 1.37, **66 карток** у шарі. Кожен вимір знято **в обох
порядках**.

### Справжній GPU (`--use-angle=metal`)

| Прогін | p50 | p95 | max | кадрів > 33 мс |
|---|---|---|---|---|
| пан **без** шару (порядок 1 / 2) | 10.9 / 12.1 мс | 13.2 / 13.8 | 16 / 14.9 | **0** |
| пан **із** шаром (порядок 1 / 2) | 13.4 / 14.0 мс | 15.5 / 19.6 | 17.8 / 19.9 | **0** |

🟢 **Вердикт рядка 9: проходить.** Шар додає **+2 мс** до медіани й **+4 мс** до p95, і
**жоден** кадр не перетнув 33 мс. Поріг «гірше 30 fps не проходить» не наближений.

### Перетин порога 1.2 туди-сюди

| | p50 | p95 | max | кадрів > 33 мс |
|---|---|---|---|---|
| без шару | 8.3 мс | 294 мс | 356 | 10 |
| із шаром | 8.5 мс | 193 мс | 504 | 9 |

Сплески на перетині є, але вони **не наші**: без оверлея вони такі самі (навіть більші).
Це перерендер сцени Pixi під новий LOD. Промоут до них нічого не додає.

### 🔴 Те, що вимір показав понад заявлену задачу

**Той самий пан на програмному рендері — 107 мс на кадр (≈9 fps), і теж однаково з шаром і
без.** Це не властивість фічі, а властивість сцени: **та сама** сцена дає 11 мс на апаратному
WebGL і 107 мс на SwiftShader. Різниця в **десять разів** — уся в середовищі.

⚠️ Це прямо стосується заявленої аудиторії — **зеро-клієнтів без GPU**. Я **не стверджую**, що
SwiftShader у headless дорівнює їхньому залізу: це різні програмні стеки. Але це сигнал, що
базова ціна панорамування на пристрої без GPU потребує окремого виміру **на цільовому залізі**,
і цей вимір не про промоут. Винесено в `work/tasks/T92-software-render-pan-cost.md`.

### `pad`: попередній висновок був неправильний, і ось чому

Тут спершу було написано, що `pad = 32` замало, бо за 150 мс дебаунсу камера проходить ≈54 px і
по ходу руху піде порожня смуга. ❌ **Обидві половини хибні.**

1. **Смуги не існує.** Нода, що входить у в'юпорт під час жесту, **ніколи не була схована**, тож
   Pixi малює її далі. Доведено тестом «a node entering the view during a gesture is never a
   hole». Порожнім місце буває лише коли ноду сховали й не дали картки — а тут відбувається
   протилежне: нова нода виглядає канвасною, доки камера не стане.
2. **Флінгу немає.** У камері **немає інерції** — `Viewport.endPan` лише скидає стан. Тобто
   виміру «максимальної швидкості флінгу», на який я посилався, немає що робити.

**Справжня роль `pad`** — зменшувати смикання на межі: нода, що наполовину виїхала, лишається
піднятою, і дрібний рух не змушує перебудовувати набір. 32 px для цього розумні.

Відкритим лишається **гістерезис** (різні пороги на вхід і вихід) — але сам по собі, а не як
частина невиведеного числа.

### 2б. Скільки нод справді за екраном — і чому це залежить від смуги

Спостереження користувача (2026-08-28): на вкладці 1M посади стоять «в купі», і за екраном
опиниться небагато. Перевірено числами, і воно **правильне — але для іншої смуги**, ніж та,
де працює фіча.

| Зум | Смуга | Видно | Поза екраном |
|---|---|---|---|
| **0.287** — так вкладка **відкривається** | far | 639/639 | **0%** |
| 0.52 | mid | 361/639 | 44% |
| 1.20 | near | 78/639 | 88% |
| 1.37 | near | 66/639 | 90% |
| 2.14 | near | 28/639 | 96% |

Причина щільності: 600 посад у сітці **24 колонки × 25 рядків**, комірка 272×72 світових px —
блок **6528×1800**, який на стартовому зумі влазить в екран цілком. Це навмисно: «вікно — це
стіна посад, а не сцена» (`tabConfigs.ts`).

**Наслідок для нашого порядку «фільтрувати → резолвити».** Виграш зберігається: у смузі `near`,
де режим працює, фільтр відкидає 88–96% нод перед резолвом. На `far` він не відкидає нічого —
але там режим **вимкнений**, тож і резолву немає.

🔴 **Наслідок для `T89` (кулінг), і він неприємний:** кулінг по в'юпорту дає найменше рівно там,
де об'єктів найбільше. На стартовому вигляді за екраном **нуль** нод — відкидати нічого,
а малюються всі 639. Тобто кулінг **не є** відповіддю на `T92` (9 fps на програмному рендері);
шукати треба в тому, що робить кожна нода, а не в тому, скільки їх поза екраном.

**3.0 Два способи виміру довелося викинути, перш ніж числа стали чесними.**
Спершу жест ішов через `page.mouse.move` — і дав рівно **60 кадрів понад 33 мс на 60 викликів
`move`**, причому **з шаром і без**. Тобто мірялася затримка CDP-раунд-трипу Playwright, а не
панорамування. Жест переїхав **усередину сторінки**, один рух на кадр.
Далі перший прогін показав, що «без шару гірше, ніж із шаром» — і це виглядало як приємний
результат. Перевірка **зміною порядку** показала, що різниця йде за позицією в прогоні, а не за
наявністю шару: знову прогрів, той самий, що в §3.1.

**3.1 Найгіршого зуму не існує — це був прогрів.** У перших прогонах зум 1.2 стабільно
показував 6.2 мс проти 2.1 у решти, і в звіт мало не пішло «поріг входу в near — найдорожча
точка». Прогін із **оберненим порядком** зумів показав, що 6.2 мс переїхали на зум 2.14, тобто
належать **першому виміряному зуму**, а не якомусь конкретному значенню. Це JIT. Усталене
число — 2.1 мс скрізь.

**3.2 Нуль може означати «зонд нічого не отримав».** Перший вимір геометрії дав `p50: 0`, що
однаково сумісне з «дуже швидко» і з «`listNodeBoxes` повернув порожній масив через приватне
поле, якого немає». Тому в результат додано `rawBoxes`/`survivors`: 639 і 28–361 відповідно —
вимір справжній. Плюс `performance.now()` у Chrome обрізаний до ~0.1 мс, тож геометричний
прохід міряється двадцятьма повторами з діленням назад.

**3.3 `Staff · 1M` не тримає мільйон у `data`.** Вкладка віддає **вікно**
(`tabConfigs.ts:209` → `buildScaleStaffWindow`), і лінійні `.find()` б'ють по вікну, а не по
мільйону. Якби вони били по мільйону, 2.1 мс були б секундами. Це і є причина, чому цифра
виявилась прийнятною, — і водночас межа її дії: **вимір нічого не каже про хост, який віддасть
у SDK невіконовані дані**.

## 4. Три рішення, які стояли на цьому вимірі

| Рішення | Вердикт | Чому саме так |
|---|---|---|
| **Дебаунс** | **лишається** | Це продуктове рішення користувача, і вимір його не спростовує. Але вимір знімає з нього роль «інакше не працює»: 3.9 мс на кадр — це працездатно й без дебаунсу. Дебаунс тут не рятує кадр, а прибирає **марну** роботу: 60 разів на секунду рахувати те саме, поки камера ще їде |
| **Трансформ шару** | **потрібен** | Випливає з дебаунсу, а не з числа: якщо перерахунок відкладено до зупинки, то під час пана шар мусить їхати цілком, інакше картки відстануть від канваса (`plan.md`, наслідок Q1) |
| **Кеш резолву** | **не потрібен** | 🔴 Заміняється **порядком**. 2.1 мс — не проблема кешу, а проблема того, що резолв іде до фільтра. Фільтруємо по геометрії (0.005 мс), резолвимо лише тих, хто вижив: 28–78 замість 639. Це дешевше за кеш, простіше за кеш і не має інвалідації, яку можна зіпсувати |

**Наслідок для плану:** T87.2 був про порядок «фільтрувати → ховати». Тепер він про порядок
**«фільтрувати → резолвити → ховати»**, і це вимагає дороги до геометрії без резолву —
або параметра-фільтра в `listPromoteCandidates`, або окремого дешевого доступу до боксів.
Очікуваний ефект: увесь `sync()` з 3.9 мс до ~1.8 мс.

## 5. Скріни

Не застосовно як «до/після»: у режимі за замовчуванням екранний вияв **не змінився** — це
свідоме рішення (див. §1, розходження №3 і коміт про регресію демо). Новий вигляд доступний
лише за `?promote=near-visible`, і його доводить e2e-твердження про три режими вмісту, а не
знімок: бейзлайн знімається на раннері CI і зафіксував би різницю середовища, а не поведінки.

## 6. Проходка по user flow

Пройдено **прогоном e2e** (`e2e/promote-near.spec.ts`), не руками.

1. Користувач відкриває `Staff · 1M` з `?promote=near-visible` — сцена на стартовому зумі
   (`far`), карток у DOM **нуль**, усе малює Pixi.
2. Наближає до 1.4 — перетинає `near`. У шарі з'являються картки **для всіх видимих нод**,
   і рівно ті самі ноди зникають з канваса (звірено: кількість дітей у DOM = кількість
   схованих у Pixi).
3. На вкладці `Staff · Figma` бачить **три різні** картки з одного компонента: зображення на
   всю ноду, зображення по центру з полями, і текст там, де фото немає.
4. Панорамує — картки їдуть **разом зі сценою** (шар несе один трансформ), позиції
   перераховуються на зупинці. Ноди, що вийшли за екран, **повертаються канвасними**.
5. Розтягує вікно — карток стає більше **на тому самому зумі**.
6. Відсуває камеру нижче 1.2 — картки зникають, канвас забирає їх назад; десять таких циклів
   не лишають жодного зайвого вузла.

## 7. Ризики й обмеження

- **Вимір ізольований.** Ціна `sync()` разом із паінтом Pixi не виміряна — це T87.10 і рядок 9.
- **Одна машина.** Числа зняті на цьому хості; `SOFTWARE_GL=1` емулює відсутність GPU, але не
  емулює повільніший CPU. Зеро-клієнт із суттєво слабшим ядром дасть інші числа, і саме тому
  рядок 9 має вердикт, а не припущення.
- **Вікно, а не мільйон** (§3.3): для хоста з невіконованими даними висновок не переноситься.
- **Стенди видалено.** Вимір `sync()` відтворюється з `e488a28`, вимір руху — з `7b79211`.
- **Два рядки таблиці були заявлені закритими без тесту** (7 і 12) і закриті лише на `code-review`.
  Код їх обробляв правильно — бракувало доказу. Це системний ризик самозвіту, а не разовий недогляд.
- **`setMaxPromoted` і `getPromotedNodeIds` приросли без замовника в плані** — обидва знадобились,
  щоб виразити тести, обидва тепер у `docs/USAGE.md`.
- **Конституція порушена свідомо:** док пішов не тим комітом, що зміна сигнатури (`tasks.md`, T87.11).
- **Гістерезис на межі** (різні пороги на вхід і вихід) не зроблений — набір може смикатись,
  коли камера дрижить біля межі. `pad` при цьому **закритий**: його роль з'ясована, 32 px
  обґрунтовані (див. §2.4).
- **Кулінг не врятує програмний рендер** (§2б): на стартовому вигляді за екраном нуль нод.

## Post-deploy

_(порожньо — заповнює `post-deploy-check` після викоту)_
