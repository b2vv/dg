# Сирі результати проби swap-seam — 26 рядків

**Контракт:** Dg 2, `work/reports/parity-recheck/swap-seam-contract.md` (`0e8e9cb`).
**Проба:** Dg 1, тимчасовий `packages/sdk/src/__swapSeamProbe.test.ts` — **видалений після виміру**;
джерело збережене поруч як `swap-seam-probe.test.ts.txt` (розширення не `.ts`, щоб не потрапляв
у збірку, typecheck і lint).
**Прогін:** `npx rstest run src/__swapSeamProbe.test.ts` → **12 passed, 14 failed**.
Повний набір і e2e не ганялись.

**Це сирі результати. Тлумачення «чи стане `dg` на місце» пише Dg 2 як не-автор проби.**

| # | Твердження | Результат | Що саме не виконалось |
|---:|---|---|---|
| 1 | канва в переданому контейнері | **стоїть** | — |
| 2 | повторний `setData` зберігає розгортання й виділення | **частково** | розгортання вціліло, **виділення стало порожнім** — `OrgHierarchyDiagram.ts:1062` |
| 3 | розгортання B не чіпає виділення {A} | **стоїть** | — |
| 4 | згортання батька зберігає виділення схованої дитини | **стоїть** | — |
| 5 | заміна виділення точна, ідемпотентна, без `onNodeClick` | **стоїть** | — |
| 6 | показ не змінює виділення; відкладений показ | **частково** | лист матеріалізувався, але виділення переїхало Root → Leaf: `revealPath` (`:1466`) кінчається `focusNode` (`:1548`) |
| 7 | крок зуму оборотний | **стоїть** | — |
| 8 | зворотний крок; нижня межа — no-op | **стоїть** | — |
| 9 | після `fitView` кожен видимий вузол у кадрі | **стоїть** | — |
| 10 | лічильник видимих виключає дітей згорнутого | **спростовано** | `3 → 3`: коли всі сибліни згорнуті, T113 перемикає на матрицю, а вона розкладає **всі** org — `rowTreeLayout.ts:239`, `matrixLayout.ts:35` |
| 11 | набір ключів збігається з лічильником і без дітей згорнутого | **спростовано** | лишились `root, branch, leaf` — та сама причина |
| 12 | виділене повертається ключами, придатними назад у `setSelection` | **спростовано** | повертає `NodeRef[]` (`:1371`, `interaction/types.ts:3`) — круговий обіг без коду хоста неможливий |
| 13 | `nodeCenter` у пікселях в'юпорта; відсутній → `null` | **спростовано** | віддав `(132,64)` замість `(361.4,191.8)`; для відсутнього — `undefined`, не `null`. Якорі свідомо віддають **світові** координати до камери — `nodeTestId.ts:90` |
| 14 | точка експандера тоглує, а не виділяє | **частково** | DOM-якір експандера **тоглює й зберігає виділення** ✅, але сира точка світова `(217,47)`, не в'юпортна `(505.9,162.9)` — перетворення живе в `createTestAnchorOverlay.ts:116` |
| 15 | інвалідація перемальовує вживане, no-op на невживаному | **стоїть** | — |
| 16 | повторний PNG-експорт не псує сцену | **стоїть** | — |
| 17 | зміна теми зберігає модель і розгортання | **стоїть** | — |
| 18 | підписка після створення й відписка | **спростовано** | колбеки фіксуються в `create` (`:350`), методу `on` немає |
| 19 | після `destroy` не стріляє жоден колбек, зокрема з польоту | **стоїть** | — |
| 20 | клік несе сирі модифікатори | **спростовано** | колбек отримує лише `NodeRef` (`callbacks.ts:92`), модифікатори спожиті до емісії (`:748`) |
| 21 | подвійний клік емітує ключ для org і людини | **стоїть** | — |
| 22 | клік по фону емітується назовні | **спростовано** | публічної події немає, виділення очищене всередині — `:739` |
| 23 | кожен тогл org емітує новий стан | **спростовано** | тогли віддали `true`/`false`, події немає: `onPositionExpandChange` лише про посади — `callbacks.ts:190` |
| 24 | контекстне меню несе вхідне ребро й точку | **частково** | точка `(37,41)` вціліла, `incomingLink` — `undefined` (`contextMenuPayload.ts:26`) |
| 25 | reparent несе `{nodeKey, newParentKey \| null}` | **частково** | працює, але навантаження `{positionId, fromManagerId, toManagerId}` (`callbacks.ts:33`), а від'єднання (`null`) **кидає** (`:1706`) |
| 26 | клік по ребру несе `structureId` + дати | **спростовано** | шар ребер неінтерактивний (`LayerManager.ts:38`), а тип лінії таких полів не має (`data/types.ts:145`) |

**Разом: стоїть 12 · частково 5 · спростовано 9.**

## Самоперевірка проби

🔑 **Умова Dg 2 виконана: зелених НЕ більше за його прогноз.** Він передбачав «радше встоять»
дев'ять рядків — 1, 3, 4, 5, 9, 15, 16, 19, 21 — і **всі дев'ять встояли**. Решта три зелені
(7, 8, 17) були в його списку «не знаю». Жоден рядок із «впадуть» чи «радше впадуть» не
позеленів. Тобто гіпотезу «асерти вимірюють існування» цей прогін **не** активує.

⚠️ **Єдиний рядок, що звівся до існування — 18.** Входу для пізньої підписки немає взагалі, тож
поведінку відписки не було на чому викликати. Рядки 22, 23 і 26 перед висновком робили
**справжні** кліки й тогли — це не перевірки існування.

⚠️ **Одне тлумачення, яке зробила проба, а не контракт, і Dg 2 має його зважити:** за
еквівалент `visibleNodeCount`/`visibleNodeKeys` взято `listTestAnchors()`. Це найближча публічна
поверхня, але вибір мій, і рядки 10–11 стоять на ньому.
