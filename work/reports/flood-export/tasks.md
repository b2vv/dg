# tasks — `cell-flood` в експорті

**План:** [`plan.md`](./plan.md) · **Гілка:** `cursor/flood-export-svg` · **Worktree:** `~/projects/dg-flood-export`
**Гейти:** GATE 1 ✅ (grilling, 4 рішення продукту) · GATE 2 ✅ (критика 4 лінзи — 5 blocking закрито;
захист 5/5, 0 дірок; конституція 6/6)

Кожен таск атомарний, має явні файли, критерій «готово» і рядки приймальної таблиці, які закриває.
`[P]` — можна робити паралельно з сусіднім `[P]`.

---

## T1 ✅ — Фікстура дефолтного рушія **до** будь-якої зміни коду

**Файли:** `packages/sdk/src/export/export.test.ts`
**Робимо:** зняти побайтову фікстуру SVG для сцени на дефолтному `contourEngine` і закріпити її
тестом (inline-snapshot або збережений рядок у тесті).
**Чому першим:** знята після рефакторингу, вона фіксувала б **уже змінену** поведінку й перевіряла
б сама себе (знахідка лінзи AI-slop).
**Готово, коли:** тест падає, якщо змінити будь-який байт виводу; проходить на поточному коді.
**Закриває:** `H2` (і `A2` зі спеки).
**Зроблено 2026-08-26:** snapshot `default-engine-svg` (4.7 КБ) + тест на чутливість самої фікстури.
Перевірено, що вона **вміє падати**: зсув контуру на 1 px ламає її; код повернуто.

## T2 — Канал діагностик наскрізь

**Файли:** `packages/sdk/src/export/svgExport.ts` (тип `SvgExportInput`), `packages/sdk/src/export/exportDiagram.ts`
**Робимо:** `SvgExportInput.onDiagnostic?: (message: string) => void`; `buildDiagramSvg` кличе його;
`exportDiagram` прокидає `options.onDiagnostic`.
**Чому другим:** без нього T4/T5 нема звідки брати причини — передумова, а не наслідок
(blocking від лінзи логічних дір).
**Готово, коли:** тест: `buildDiagramSvg` з `onDiagnostic` отримує рядок від штучної гілки; без
`onDiagnostic` не падає.
**Закриває:** передумова для `F1`–`F5`; сам по собі рядків не закриває.

## T3 — `resolveExportContourRings`

**Файли:** `packages/sdk/src/export/svgExport.ts`
**Робимо:** одна функція, що інкапсулює вибір рушія, збірку входів flood, виклик малювальника й
збірку `d`; кличуть її **обидві** контурні гілки (staff і grid), поглинаючи ~30 дубльованих рядків.
Вибір рушія — **строго `=== 'cell-flood'`**, як у `ContourPainter` (асиметрія з
`!== 'button-group'` — знахідка plan-defense). Склад входів — дослівно за `plan.md` §Кроки п.2
(`magnet`: padding 0, `corridorCellsForFlood`, smooth 0; `transform` з `staffMerged` pitch;
`cards` з `staffMerged.nodeWidth/Height`).
**Готово, коли:** `H1` зелений, `H2` (T1) досі зелений, дублювання між гілками зникло.
**Закриває:** `H1`, `F5`.

## T4 — Grid-гілка через резолвер

**Файли:** `packages/sdk/src/export/svgExport.ts`
**Робимо:** grid-гілка кличе резолвер; при `cell-flood` шар відділів **порожній** + один рядок
діагностики «flood потребує cell-transform, якого на сітці немає — так само й на канвасі».
**Готово, коли:** `F2` зелений; на дефолтному рушії grid-вивід не змінився (перевіряється T1).
**Закриває:** `F2`.

## T5 — `exportDiagram`: чесні причини замість попередження за фактом рушія

**Файли:** `packages/sdk/src/export/exportDiagram.ts`
**Робимо:** прибрати `reportSvgEngineMismatch` у теперішньому вигляді; назовні йдуть лише
діагностики, які реально повернув SVG-шлях. Дефолтний рушій — жодного повідомлення.
**Готово, коли:** `F1`, `F3`, `F4` зелені; тест «cell-flood + робочий flood → onDiagnostic мовчить».
**Закриває:** `F1`, `F3`, `F4`.

## T6 [P] — Граничні сценарії

**Файли:** `packages/sdk/src/export/export.test.ts`
**Робимо:** тести `B1` (сцена без відділів), `B2` (один відділ/одна посада), `B3`
(`minContourMembers` відсік усе — **без** діагностики), `B4` (`subtree` — лише свої відділи),
`B5` (`subtree` без посад → org-гілка, рушій не застосовується, діагностика мовчить).
**Готово, коли:** усі п'ять зелені й кожен здатен впасти (перевірити зламавши код тимчасово).
**Закриває:** `B1`–`B5`.

## T7 [P] — e2e на демо

**Файли:** `e2e/integration-paths.spec.ts`
**Робимо:** таб «Staff · Flood» → `#export-svg` → у завантаженому файлі є `<path data-dept` і
статус показує `export`.
**Готово, коли:** тест зелений локально й у контейнері `playwright:v1.62.1-noble`.
**Закриває:** `H3`.

## T8 [P] — Документи

**Файли:** `docs/USAGE.md` (§6, §10), `work/tasks/T80-contour-engines-ba-demo.md`,
`work/CTO-RESEARCH.md` (ризик №1)
**Робимо:** прибрати «SVG завжди button-group»; описати поведінку й деградацію (порожній шар +
діагностика); закрити ризик у брифінгу.
**Готово, коли:** у репо не лишилось твердження «SVG завжди button-group» (перевірка `grep`).
**Закриває:** `A5` зі спеки.

## T9 [P] — Версія і CHANGELOG

**Файли:** `packages/sdk/package.json`, `CHANGELOG.md` (новий)
**Робимо:** `0.1.0 → 0.2.0`; запис про зміну поведінки `export({format:'svg'})` при `cell-flood`.
**Готово, коли:** `CHANGELOG.md` існує й описує зміну одним абзацом без переказу плану.
**Закриває:** рішення GATE 1 №4.

## T10 — Ручна перевірка M1

**Робимо:** експортувати SVG із табу «Staff · Flood», відкрити поруч із канвасом, звірити форму
контурів навколо чужої картки.
**Готово, коли:** результат записано у звіт (`ship-report`) як пройдений ручний крок.
**Закриває:** `M1` — єдиний ручний рядок таблиці.

---

## Порядок

```
T1 → T2 → T3 → T4 → T5 → { T6 [P] · T7 [P] · T8 [P] · T9 [P] } → T10
```

T1 і T2 — строго перші: перший рятує від циркулярної фікстури, другий є передумовою для решти.
