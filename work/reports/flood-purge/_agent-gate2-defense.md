# GATE 2 — захист плану (flood-purge)

ДАЛІ: читаю plan.md і spec.md повністю, потім код репо (contourEngine, WASM loader, worker handlers, барель, демо-вкладка).

ФАКТ: план і спека прочитані повністю. Спека має розділ "Правки після GATE 1" (Г1-Г6, В1-В3).
План має явну секцію 5 "Rollback" (рядки 103-113) і сценарій акцептансу №18 (WASM недоступний).

ДАЛІ: перевіряю код — RenderConfig/contourEngine (де саме тип і чи є runtime-валідація зайвих полів),
WASM-лоадер (contour/bridge.ts) і worker-handlers, барель index.ts, minContourMembers/порожні сцени.

ФАКТ (Q1): `ContourPainter.ts:279` — `config.contourEngine ?? defaultRenderConfig.contourEngine ?? 'button-group'`.
Мердж конфігу в `OrgHierarchyDiagram.ts:331` — `{ ...defaultRenderConfig, ...config.render }`, чистий
object-spread, БЕЗ рантайм-валідації зайвих ключів (zod/ajv/ручної перевірки не знайдено). Після чистки
ця гілка й увесь `contourEngine`-читач зникають (крок 3/6 плану). Отже нетипізований хост (сирий JS/JSON,
без `npm run typecheck`), що передасть `contourEngine: 'cell-flood'`, отримає це поле **мовчки
проігнороване** — жодної помилки, жодного попередження, просто завжди button-group.
РІШЕННЯ: план (рядки 79, 144) стверджує лише «падає на типах, не тихо» — це вірно ЛИШЕ для
TS-збірки. Рантайм-обхід (JS/JSON-конфіг) планом не названо і не визнано свідомо поза скоупом.
🔴 Q1 не закрито.

ФАКТ (Q2): плану пункт acceptance-сценарій №18 (рядок 189) прямо каже: WASM недоступний →
button-group малює, row-tree падає з `WasmLoadError`, і визнає це **новим explicit тестом**
(поточне покриття — лише побічне через `variantBNotchPaint.test.ts`). Підтверджено кодом:
`contour/bridge.ts:83-103` (`initContourWasm` кидає `WasmLoadError`), `wasm/layoutBridge.ts` і
`worker/wasm-init.ts` — обидва йдуть через той самий лоадер для row-tree; button-group
(`paintMagneticGroups`) WASM не викликає взагалі.
✅ Q2 закрито рядком плану 189 (сценарій №18).

ФАКТ (Q3): порядок кроків (план §2, рядки 52-64) — TS-виклики контуру прибираються кроками 3-6
**до** Rust-кроку 7; крок 7 явно об'єднує «Rust-джерело + build:wasm + коміт pkg» в ОДИН крок/коміт
(рядок 59, підтверджено §4 рядок 95-96: «Крок 7 (Rust) обов'язково тягне npm run build:wasm і коміт
pkg — інакше свіжий клон зламається»). Це унеможливлює обидва боки гонки: (а) TS кличе те, чого
вже нема в Rust — TS перестає кликати РАНІШЕ, ніж Rust-джерело зникає; (б) pkg містить експорти,
яких нема в джерелі — джерело й pkg комітяться разом.
✅ Q3 закрито рядками плану 52-64 + 95-96.

ФАКТ (Q4): `packages/sdk/src/export/export.test.ts`, describe `'boundaries of the export contour
layer (T6 / B1–B5)'` (:396-471) — це ЗАГАЛЬНІ граничні кейси (сцена без відділу B1, один
сідок/один відділ B2, `minContourMembers` вище натовпу B3, subtree-фільтр B4-B5), але всі п'ять
використовують `floodCfg = { contourEngine: 'cell-flood', ... }` як єдиний носій — бо
`resolveExportContourRings` (`svgExport.ts:85`) гілкується `if (cell-flood) {...} else
{paintMagneticGroups(...)}`, а еквівалента для button-group-гілки на рівні SVG-export у файлі
НЕМАЄ (перевірено grep `minContourMembers` по всьому файлу — лише порівняльні/grid-тести мають
button-group-варіант, B1-B5 — ні). `paintMagneticGroups.test.ts:98` покриває minContourMembers
лише на рівні canvas-paint, не на рівні svgExport.
План (рядок 174) описує весь `export.test.ts` діапазон `:201-660` як «мінус ~15 flood-кейсів»,
без винятку для B1-B5, і прямо каже «Новий тест? ні». Тобто граничні кейси (порожній відділ,
один відділ, поріг minContourMembers, subtree) на рівні SVG-експорту втрачають ПОВНЕ покриття,
хоча функціонально йдуть через ту саму `paintMagneticGroups`, що й канвас (низький, але не
нульовий ризик регресії саме тому, що покриття зникає, а не тому, що код інший).
🔴 Q4 не закрито.

ФАКТ (Q5): план §5 (рядки 103-113) явно розписує rollback по трьох сценаріях і прямо називає
незворотне: «Rollback не повертає pkg автоматично: після revert Rust-кроку треба перезібрати
npm run build:wasm, інакше в дереві лишиться WASM без контурних експортів при коді, що їх кличе»
(рядок 111-112). Кожен крок — окремий комміт, що лишає дерево зеленим (§2/§4, рядок 95) — тобто
git revert окремого кроку безпечний за побудовою. Секція явно називає ціну повернення (переписати
гілки в ContourPainter/svgExport, рядок 107) і найнебезпечніший клас (row-tree loader, рядок 108).
✅ Q5 закрито рядками плану 103-113.

ДАЛІ: фіналізую таблицю й вердикт.

РІШЕННЯ: таблиця + дві дірки + одне речення-правка на кожну + вердикт дописані в
work/reports/flood-purge/plan.md як новий розділ "## 8. GATE 2 — захист плану (plan-defense)".
ВЕРДИКТ: закрито 3/5.
