# CR2 — Спека вісь: collapsed-matrix (T113)

ДАЛІ: прочитати spec.md, task file, git log діапазону

ФАКТ: перевірено весь діапазон 316930f..HEAD (12 комітів). A1-A4, A7, A8, A9, A10 покриті тестами
відповідно до формулювань. A5 навмисно closed сильніше (нуль перетинів замість дельти) — спека сама
це визнає чесним. Питання 3 і 4 закриті в першому ж коміті (a4b497a), до d73f4a4 (перший код) —
чесно "до коду". Rust-діф (ploeg_layout.rs) — лише тести в mod tests, жодної production-логіки;
межу WASM не рухає. out-of-scope (viewport/edit-mode/positions) — жодних слідів у коді, лише текст
нотаток. matrixLayout.ts/orgMode.ts не займані — A8 підтверджено.
ЗНАХІДКА (в): A6 вимагає "всі шляхи набору мають спільний сегмент-шину" — тест цього не перевіряє
(rowTreeLayout.test.ts:231-289 перевіряє лише reachability/endpoint-in-AABB, не спільний бас-сегмент).
ДАЛІ: фінальна відповідь.
