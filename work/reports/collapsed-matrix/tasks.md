# tasks — T113 org-половина

**План:** [`plan.md`](./plan.md) · **Спека:** [`spec.md`](./spec.md) · **Гілка:** `t113-collapsed-matrix`

Порядок — залежностями. `[P]` = можна паралельно з попереднім.
Кожен таск закриває названі рядки [приймальної таблиці](./spec.md#acceptance-criteria).

---

- [ ] **К1 — `planCollapsedSiblingGrids`, чисте рішення «які набори стають сітками»**
  - Файли: `packages/sdk/src/layout/collapsedSiblingGrid.ts` (новий),
    `collapsedSiblingGrid.test.ts` (новий)
  - Переюз: `resolveMatrixDimensions` з `matrixGrid.ts`; предикат «усі згорнуті» — за формою
    `siblingOrgGroups.ts:66-71`, але **до** розкладки
  - Готово, коли: набір із 9 дає `{cols:3, rows:3}`; один брат і «≥1 розгорнутий» дають «не сітка»;
    два набори на різних рівнях судяться незалежно; `n=2` дає 1×2 і тест **називає** це no-op'ом
  - Закриває: **A1, A2, A3, A4**

- [ ] **К2 — трансформація «набір → ланцюги» і зворотне розгортання**
  - Файли: ті самі два
  - Готово, коли: 9 членів → 3 ланцюги по 3 за `matrixOrder`; розгортання повертає ті самі id,
    **справжній `parentId`** і **справжній `depth`** (план §3.1, дірка 2), заповнює
    `matrixRow`/`matrixCol`; `matrixOrder` не в усіх → порядок із даних цілком; вхід не мутовано;
    синтетичний префікс не збігається з реальним id навіть на сцені, де org так названо
  - Закриває: **A7**

- [ ] **К3 — вплести в `computeOrgRowTreeLayout`**
  - Файли: `packages/sdk/src/layout/rowTreeLayout.ts`, `rowTreeLayout.test.ts`
  - ⚠️ Перед кодом: знайти тести, що чекають конкретні `width`/`height` row-tree (план §4 п.2)
  - Готово, коли: сцена як `Flat orgs` дає **2 ряди** замість 1 і **меншу ширину**; накладання як
    **дельта** (пари з перетином AABB після ⊆ до); гвардія глибини перевіряє **обидва** пороги й
    друге повідомлення називає сітку причиною (план §3.1, дірка 1)
  - Закриває: **A5**

- [ ] **К4 — ребра: хребет замість ланцюгових**
  - Файли: `rowTreeLayout.ts`, тест поруч
  - Переюз: `buildSpineBusEdgesForForest` (`spineBusEdges.ts`), як у `matrixLayout.ts:106`
  - Готово, коли: кожен член має шлях із кінцем у своєму AABB; усі шляхи набору мають спільний
    сегмент-шину; набір **без** сітки ребра не змінює
  - Закриває: **A6, A8**

- [ ] **К5 — `docs/USAGE.md` + машинний гейт** `[P]` з К6
  - Файли: `docs/USAGE.md`, `scripts/collapsedMatrixDocs.mjs` (новий),
    `scripts/collapsedMatrixDocs.test.mjs` (новий), `scripts/check-docs.mjs`
  - ⚠️ Окремим модулем, який `check-docs.mjs` **імпортує** — патерн `barrelSurface.mjs` /
    `seatCollisionDocs.mjs`; сам `check-docs.mjs` імпортувати не можна (`process.exit` на верхньому рівні)
  - Готово, коли: доку каже, **режим чого саме** називають `getOrgMode`/`onOrgModeChange`
    (GATE 1, варіант «а»); прибрати абзац → `check:docs` червоний; модуль має власні тести
  - Закриває: **A9**

- [ ] **К6 — e2e на `Flat orgs`** `[P]` з К5
  - Файли: `e2e/flat-orgs.spec.ts`
  - ⚠️ Вкладка дає чотири помилки сторінки на старті ([T114](../../tasks/T114-flat-orgs-null-on-first-paint.md)) —
    **не лікуємо тут**, але тест не має на них спиратись
  - Готово, коли: число рядів > 1, підтверджено знімком
  - Закриває: **A10**

---

## Ledger

Один виконавець (оркестратор), послідовно — паралельних агентів немає, тож окремий durable
ledger не заводиться; стан живе в цих чекбоксах і в живій ноті сесії.
