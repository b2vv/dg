# tasks — T113 org-половина

**План:** [`plan.md`](./plan.md) · **Спека:** [`spec.md`](./spec.md) · **Гілка:** `t113-collapsed-matrix`

Порядок — залежностями. `[P]` = можна паралельно з попереднім.
Кожен таск закриває названі рядки [приймальної таблиці](./spec.md#acceptance-criteria).

---

- [x] **К1 — `planCollapsedSiblingGrids`, чисте рішення «які набори стають сітками»** ✅ 2026-09-07
  - Файли: `packages/sdk/src/layout/collapsedSiblingGrid.ts` (новий),
    `collapsedSiblingGrid.test.ts` (новий)
  - Переюз: `resolveMatrixDimensions` з `matrixGrid.ts`; предикат «усі згорнуті» — за формою
    `siblingOrgGroups.ts:66-71`, але **до** розкладки
  - Готово, коли: набір із 9 дає `{cols:3, rows:3}`; один брат і «≥1 розгорнутий» дають «не сітка»;
    два набори на різних рівнях судяться незалежно; `n=2` дає 1×2 і тест **називає** це no-op'ом;
    вироджені входи оброблені **явно**: `n=0`, org без `parentOrgId`, батько поза payload
  - Закриває: **A1, A2, A3, A4**
  - ✅ 15 тестів. Мутаційна перевірка (червоне «модуля немає» валить усе з однієї причини, тож
    доводити довелось окремо): поріг `>1` знято → 1 червоний · перевірку згорнутості знято → 2 ·
    `matrixOrder` частково приймається → 1 · перевірку «батько в payload» знято → 1 ·
    порядок перевернуто → 3

- [x] **К2 — трансформація «набір → ланцюги» і зворотне розгортання** ✅ 2026-09-07
  - Файли: ті самі два
  - Готово, коли: 9 членів → 3 ланцюги по 3 за `matrixOrder`; розгортання повертає ті самі id,
    **справжній `parentId`** і **справжній `depth`** (план §3.1, дірка 2), заповнює
    `matrixRow`/`matrixCol`; `matrixOrder` не в усіх → порядок із даних цілком; дублікати,
    від'ємні й не цілі значення не кидають і дають детермінований порядок; вхід не мутовано
  - ⚠️ **Синтетичних id немає** — ланцюг переприв'язує `parentOrgId` реальних організацій.
    Перша редакція плану обіцяла префікс і тест на зіткнення; GATE 2 показав, що це спадок
    мертвого дизайну. Тест на префікс **не пишеться**
  - Закриває: **A7**
  - ✅ 13 тестів. Мутації: ланцюг по рядку замість колонки → 2 червоних · `parentId` не
    відновлено → 2 · `depth` не відновлено → 1 · `matrixRow`/`matrixCol` переставлено → 1 ·
    гвардію відсутнього батька знято → 1 · вхід мутовано на місці → 1

- [x] **К3 — вплести в `computeOrgRowTreeLayout`** ✅ 2026-09-07
  - Файли: `packages/sdk/src/layout/rowTreeLayout.ts`, `rowTreeLayout.test.ts`
  - ⚠️ Перед кодом: знайти тести, що чекають конкретні `width`/`height` row-tree (план §4 п.2)
  - Готово, коли: сцена як `Flat orgs` дає **2 ряди** замість 1 і **меншу ширину**; накладання як
    **дельта** (пари з перетином AABB після ⊆ до); `matrixRow`/`matrixCol` заповнені у членів;
    `visibleOrgsForRowTree` **повертає** максимальну глибину (план §3.1, дірка 3), і гвардія
    перевіряє **обидва** пороги, друге повідомлення називає сітку причиною (дірка 1)
  - Закриває: **A5**
  - ✅ 8 тестів. Перевірено **до** зміни, як вимагав план §4 п.2: крихких тестів на конкретні
    `width`/`height` немає — лише `> 0` у `flatOrgRootExpand.test.ts:40`. Воркерний шлях іде
    через ту саму функцію (`handleComputeOrgRowTreeLayout` → `computeOrgRowTreeLayout`), тож
    зміна одна на обидва.
  - ✅ Мутації: трансформацію знято → 3 червоних · розгортання знято → 1 · другу гвардію
    глибини знято → 1 · внесок сітки в глибину обнулено → 1 · `maxDepth` не рахується → 1

- [ ] **К4 — ребра: хребет замість ланцюгових**
  - Файли: `rowTreeLayout.ts`, тест поруч
  - Переюз: `buildSpineBusEdgesForForest` (`spineBusEdges.ts`), як у `matrixLayout.ts:106`
  - Ланцюгове ребро впізнається **за належністю до множини членів набору** (її рахує К1), не за
    префіксом чи глибиною — план §3.1
  - Готово, коли: кожен член має шлях із кінцем у своєму AABB; усі шляхи набору мають спільний
    сегмент-шину; набір **без** сітки ребра не змінює
  - Закриває: **A6**
  - ⚠️ **A8 звідси знято.** GATE 2: «Collapse all» іде через `computeMatrixLayout`
    (`matrixLayout.ts`), якого К4 не чіпає взагалі. A8 тримається тому, що той шлях **поза
    скоупом**, а не тому, що якийсь крок його береже — і сказати це чесніше, ніж приписати
    кроку заслугу, якої в нього немає. Перевіряється наявним тестом, що лишається зеленим **без
    правок**; якщо він зміниться — це сигнал, що скоуп поїхав

- [ ] **К5 — `docs/USAGE.md` + машинний гейт** `[P]` з К6
  - Файли: `docs/USAGE.md`, `scripts/collapsedMatrixDocs.mjs` (новий),
    `scripts/collapsedMatrixDocs.test.mjs` (новий), `scripts/check-docs.mjs`
  - ⚠️ Окремим модулем, який `check-docs.mjs` **імпортує** — патерн `barrelSurface.mjs` /
    `seatCollisionDocs.mjs`; сам `check-docs.mjs` імпортувати не можна (`process.exit` на верхньому рівні)
  - Готово, коли: доку каже, **режим чого саме** називають `getOrgMode`/`onOrgModeChange`
    (GATE 1, варіант «а»); прибрати абзац → `check:docs` червоний; модуль має власні тести
  - Закриває: **A9**

- [ ] **К6 — e2e на `Flat orgs`** `[P]` з К5
  - Файли: `e2e/flat-orgs.spec.ts`
  - ⚠️ Вкладка дає чотири помилки сторінки на старті ([T114](../../tasks/T114-flat-orgs-null-on-first-paint.md)) —
    **не лікуємо тут**, але тест не має на них спиратись
  - Готово, коли: число рядів > 1, підтверджено знімком
  - Закриває: **A10**

---

## Ledger

Один виконавець (оркестратор), послідовно — паралельних агентів немає, тож окремий durable
ledger не заводиться; стан живе в цих чекбоксах і в живій ноті сесії.
