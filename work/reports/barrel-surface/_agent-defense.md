
## plan-defense T105 (agent run)

ДАЛІ: читаю plan.md, spec.md, packages/sdk/src/index.ts, scripts/check-docs.mjs, packages/sdk/package.json — потім жену п'ять питань по черзі.

ФАКТ: `check-docs.mjs:100` читає фасад через `readFileSync` без try — прецедент «файл зник → аптрейс і ненульовий код» уже є; але тоді перевірки 1 і 3 не відпрацюють. План про це мовчить.
ФАКТ: `packages/sdk/src` не має жодного `export * from` (grep порожній) — тобто ризик К0-сліпоти до зіркового реекспорту сьогодні нульовий, але завтрашній `export * from './contour/bridge.js'` протягне обидва хуки повз текстову перевірку. §9 «Свідомо прийнято» звужує обіцянку лише до `__setLoader`-подібних імен, не до `export *`.
ФАКТ: `layout/index.ts:47` реекспортує `OrgHierarchyError`. Якщо реекспорт зникне — барель не скомпілюється, а К2 імпортує клас саме з барелю (`../index.js`), тож і typecheck, і тест червоніють. Відмова гучна, план її не називає, але вона закрита конструкцією.
ФАКТ: root `package.json:22` — `typecheck` = `typecheck -w sdk && build:types -w sdk && typecheck -w demo && tsc -p tsconfig.e2e.json`. Заява плану §5.4 («typecheck спершу робить build:types») **істинна**, порядок правильний: `dist/*.d.ts` перезбирається до тайпчеку демо.
ФАКТ: нова перевірка читає `src`, не `dist` — стану між прогонами не заводить; єдиний persist-стан у гейті — рукотворний `UNDOCUMENTED_BASELINE` (`:88`), нова перевірка його не торкається. Повторний прогін ідемпотентний.
ФАКТ: `ForTests` в `index.ts` трапляється лише в рядках 53–54 (самі експорти). Але К0 сформульовано як «читати текст і падати, якщо серед експортованих імен є `*ForTests`» — область сканування (увесь файл проти лише `export`-блоків) у плані не зафіксована, а К1/К3 природно захочуть написати в шапці «два хуки прибрано» з їхніми іменами → хибне спрацювання на власному коментарі.
ФАКТ: порожній `index.ts` (нуль експортів) дає вакуумний зелений — жодного sentinel-твердження план не має.
ФАКТ: публікації немає — ні `CHANGELOG`, ні workflow з `npm publish`/`NPM_TOKEN` (`.github/workflows/`: ai-review, ci, pages, post-deploy), root `package.json:5` `private: true`. Отже §7 «цього репо не знає» — не невідомість, а перевіряний факт.

РІШЕННЯ: 3 дірки — #1 (`export *` + зниклий/порожній файл), #4 (область сканування → хибне спрацювання на коментарі), #5 (публікація як факт, а не як «невідомо»). #2 і #3 закриті.

## Підсумок — 5 питань

| # | Питання | Закрито рядком плану | Вердикт |
|---|---|---|---|
| 1 | Кривий інпут | К0 («читати текст `index.ts`»), §9 п.5 | **дірка** |
| 2 | Відмова залежності | К2 (імпорт із барелю), §2 (`layout/index.ts:47`) | закрито |
| 3 | Гонка / повторний виклик | §5.4 + перевірка читає `src` | закрито |
| 4 | Порожні та граничні дані | К0 — область сканування не задана | **дірка** |
| 5 | Rollback | §7 | **дірка** |

Дірок: **3**. Гейт червоний.
