
## AI-slop lens

ДАЛІ: читаю spec.md, plan.md, packages/sdk/src/index.ts, docs/USAGE.md, .claude/standards.md — шукаю наповнювач: К3-тест-сторож, секція «сім груп», «решта внутрішня», A6.

ФАКТ: `packages/sdk/src/index.ts` — 33 явні re-export-блоки, **жодного `export *`**; `ForTests` лише на :53-54. Отже К3 червоний до К1 (валідний red-green), але далі ловить тільки навмисно вписане ім'я.
ФАКТ: `*.contract.test.ts` — наявна конвенція репо (10 файлів), тож ім'я нового файлу не церемонія.
ФАКТ: `packages/demo/src` та `e2e` — нуль згадок `ForTests`. A6 не має чим впасти.
ФАКТ: `OrgHierarchyError` живе в `packages/sdk/src/layout/orgTree.ts:3`; тест гвардії `layout/rowTreeDepthGuard.test.ts:37,48,77` — сусід цього модуля, тож `instanceof` пройде з імпорту `./orgTree.js` **без** експорту з барелю.
ФАКТ: `docs/USAGE.md` — 605 рядків, 15 секцій; групи з К4 (~40 імен) уже розсипані по §3–§13. `check:docs` (AGENTS.md) звіряє лише методи `OrgHierarchyDiagram`.

РІШЕННЯ: три знахідки — A3 (blocking), A6 і сайд-ефект 3 + таблиця семи груп (non-blocking). К3 і секція «Що є публічним API» по суті не slop.

[AI-slop] A3 не перевіряє ту половину, заради якої писався
де: spec A3 / plan К2
чому: `instanceof` у `rowTreeDepthGuard.test.ts` пройде з локального імпорту `./orgTree.js`; експорт із барелю (те, що бачить хост, С4) лишиться неперевіреним.
що зробити: у К2 вказати, що клас у цьому тесті імпортується з барелю (`../index.js`) — інакше рядок нічого не ловить.
вага: blocking

[AI-slop] A6 — рядок, який не може впасти
де: spec, A6 «Нічого не зламано», сценарій С2
чому: демо й e2e не мають жодної згадки `ForTests` (виміряно), а К2 лише додає експорт — typecheck/e2e зелені за побудовою. До того ж A6 приписаний С2, який про **текст документації**, і доводиться A5, не збіркою. A2 вже покриває «сюїта зелена без правок тестів».
що зробити: злити A6 в A2 («сюїта + typecheck + e2e без правок») або зняти.
вага: non-blocking

[AI-slop] «Без К3 нове правило нічим не тримається» — К3 тримає інше правило
де: plan §5 сайд-ефект 3
чому: К3 форсить «жодного `ForTests` у барелі»; «решта кореневих експортів внутрішня» (К4) він не торкається взагалі й машинно не форситься нічим. Заява виправдовує новий тест чужою користю.
що зробити: переписати чесно: К3 тримає лише конвенцію `ForTests`; межа з К4 — письмове зобов'язання без гейта, і це усвідомлений вибір (гейт на 253 імені = список, який заборонено спекою).
вага: non-blocking

[AI-slop] Таблиця семи груп — другий список імен без гейта
де: spec «Групи, які оголошуються публічними» / plan К4
чому: ~40 імен, які вже описані в §3–§13 USAGE; `check:docs` їх не звіряє, тож копія дрейфуватиме — саме та хвороба, про яку AGENTS.md пише «документ перестає бути правдою». Несуча частина — одне речення A5.
що зробити: у К4 лишити групи як рубрики з посиланням на наявні секції USAGE, а поіменний інвентар — тільки там, де він і так є.
вага: non-blocking
