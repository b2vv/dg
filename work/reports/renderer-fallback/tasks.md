# tasks — вибір рушія рендеру (T83)

**План:** `plan.md` (GATE 2 зелений: 0 blocking, 0 дірок, 0 порушень конституції)
**Гілка:** `cursor/renderer-canvas-fallback` · **PR:** у `main`

Порядок — за залежностями. `[P]` = можна паралельно з попереднім.
Кожен таск називає, які рядки приймальної таблиці (`plan.md §8`) він закриває.
За `work/TDD.md` тест пишеться **перед** кодом.

---

## T83.1 — Нормалізація значення опції `renderer`

**Файли:** `packages/sdk/src/render/PixiHost.ts` (експортована чиста функція),
`packages/sdk/src/render/PixiHost.test.ts` (наявний файл — там уже живе тест `resolvePixiResolution`)

Чиста функція `resolveRendererPreference(value: unknown)` → `{ preference, failIfMajorPerformanceCaveat, diagnostic }`
за таблицею К2; будь-що поза юніоном → поведінка `'auto'` + текст діагностики, який називає
**і отримане значення, і застосоване**.

- [x] тест: `'auto'` / `'webgl'` / `'canvas'` дають рівно опції з таблиці К2
- [x] тест: `undefined` === `'auto'` — ⚠️ позеленів без червоного: гілку вже закрив попередній зріз
- [x] тест: `'vulkan'` → опції `'auto'` **і** непорожній `diagnostic`
- [x] код

**Закриває рядки:** 4, 9 (частково — решта в T83.5)

## T83.2 — Опція доїжджає до рушія

🔴 **Шов змінено після узгодження з користувачем.** Таск писався з критерієм «тест доводить, що
значення дійшло до опцій `app.init`». Користувач обрав **три шви** й окремо відхилив четвертий —
опції `app.init` через стаб Pixi, бо він ламається на апгрейді бібліотеки. Тому перевірка йде
через шов 2 (публічний API): змонтувати з `renderer: 'canvas'` і спитати `getRendererKind()`.
Через це T83.2 і T83.3 закриваються **одним** зрізом.

**Файли:** `packages/sdk/src/render/PixiHost.ts`, `packages/sdk/src/OrgHierarchyDiagram.ts`

`PixiHostOptions.renderer`; `OrgHierarchyDiagram.create` передає `{ renderer: config.renderer }`
у `PixiHost.create` (зараз `:217` кличе без другого аргументу); `renderer?` у `OrgHierarchyConfig`.

- [x] тест (шов 2): монтування з `renderer: 'canvas'` → `getRendererKind() === 'canvas'`
- [x] код

**Закриває рядки:** 2 (передумова), 4
⚠️ **Нічого, крім `renderer`, у `PixiHost.create` не додаємо** — `resolution`/`background`/`signal`
у публічному конфігу відсутні, і виносити їх — інша задача (§2 плану).

## T83.3 — `getRendererKind()` із контрактом нульового стану

**Файли:** `packages/sdk/src/render/PixiHost.ts`, `packages/sdk/src/OrgHierarchyDiagram.ts`

`PixiHost` читає `app.renderer.type` (`RendererType.WEBGL=1 / CANVAS=4`);
`getRendererKind(): 'webgl' | 'canvas' | null`, `null` до монтування й після `destroy()`.

- [x] тест: після `destroy()` віддає `null` і **не кидає**
- [x] тест: до завершення монтування — `null` (той самий геттер: `app` ще не створено)
- [x] код

**Закриває рядки:** 1, 2, 3 (спостережувана поверхня для них)

## T83.4 — Рушій у діагностиці, контракт каналу розширено

**Файли:** `packages/sdk/src/OrgHierarchyDiagram.ts`, `packages/sdk/src/callbacks.ts`

Рядок `Renderer: <kind> (requested: <value>)` дописується **поверх**
`renderer.getLayoutDiagnostics()` у `OrgHierarchyDiagram` — `DiagramRenderer` про рушій не знає.
jsdoc каналу (`callbacks.ts:29` і геттер `:817`) переписується з «soft layout warnings» на
«діагностика останнього рендеру, включно з рушієм».

- [x] тест: рядок присутній **після другого** рендеру (не лише після першого)
- [x] тест: рядок про підміну невідомого значення (T83.1) доїжджає в той самий список
- [x] код + jsdoc **тим самим комітом**

**Закриває рядки:** 9, 12

## T83.5 — Відмова монтування не лишає воркерів

**Файли:** `packages/sdk/src/OrgHierarchyDiagram.ts`

`try/catch` навколо `PixiHost.create`: у `catch` — наявний `instance.destroy()`
(`:1093-1097` уже звільняє `searchService` і `workerPool`), далі кидаємо **обгорнуту** помилку,
яка називає задане значення опції.

- [x] тест: відмова монтування → воркери хоста термінуються
      ⚠️ **без стаба:** у jsdom `renderer: 'webgl'` падає по-справжньому (мок WebGL без
      `getContextAttributes`), тож шлях відмови справжній, а не підроблений
- [x] тест: тричі поспіль — кожна спроба звільняє свої воркери
- [x] тест: текст помилки містить задане значення `renderer`
- [x] код

**Закриває рядки:** 11, 11а

## T83.6 [P] — e2e: вибір рушія на машині з GPU

**Файли:** `e2e/renderer-choice.spec.ts` (**єдиний новий файл**),
`packages/sdk/src/render/twoDiagrams.contract.test.ts` (наявний)

- [x] рядок 1: ⚠️ **перефразовано** — не «=== 'webgl'», а «те, що каже SDK, збігається з тим,
      який контекст має канвас». Причина: headless Chromium залежно від машини або віддає
      софтверний WebGL, або ні; жорстке `'webgl'` тестувало б раннер, а не SDK
- [x] рядок 2: `renderer: 'canvas'` → `'canvas'` **і** `getContext('webgl2') === null`
- [x] рядок 5 (**половина**): дві діаграми (`'canvas'` + `'auto'`) у наявному
      `twoDiagrams.contract.test.ts` — прибита лишається канвасною поруч із сусідкою
- [ ] рядок 5 (**друга половина**): що сусідка на машині з GPU віддає саме `'webgl'` — **лише e2e**

🔴 **Дефект плану, знайдений при імплементації.** Рядок 5 обіцяв, що друга діаграма віддасть
`'webgl'`, і призначав перевірку юніт-тесту. У jsdom це недосяжно: `vitest.setup.ts` мокає WebGL
без `getContextAttributes`, тож `isWebGLSupported` завжди `false` і `'auto'` **завжди** дає канвас.
Юніт-тест, який стверджував би `'webgl'`, перевіряв би мок, а не поведінку. Рядок розділено:
юніт володіє ізоляцією прибитої діаграми, e2e — гілкою `'webgl'`.
- [ ] рядок 6: **строго послідовне** монтування `'auto'` → `'webgl'`; друга не отримує WebGL тихо
      — ⚠️ **не закрито**, див. «Що лишилось» унизу
- [x] рядок 7: `Staff · 1M` під `'canvas'` сходить

**Закриває рядки:** 1, 2, 5, 6, 7

## T83.7 — Бейзлайн канвас-режиму на зумі-аут (рівно один)

**Файли:** `e2e/renderer-choice.spec.ts`, знімок у `*-snapshots/`

- [x] знімок `Staff · 1M`, зум 0.19, `renderer: 'canvas'` — окремий бейзлайн **linux**
      (згенеровано в контейнері процедурою з `work/tasks/MOCKUP-styles-review.md`)
- [x] прогін зелений; бейзлайн **відтворюваний** — повторний прогін без `--update` зелений

**Закриває рядок:** 8
⚠️ Другого повного набору бейзлайнів мокап-вкладок **не заводимо** (§3 К5).

## T83.8 — Прогін у контейнері без GPU

**Файли:** `e2e/renderer-nogpu.spec.ts` (проєкція того ж коду під контейнер)

Середовище: `docker run --platform linux/amd64 mcr.microsoft.com/playwright:v1.62.1-noble`.

- [x] рядок 3: Firefox без GPU, `Staff · 1M` — сходить, `'canvas'`, **59 fps**
- [x] рядок 10: `webgl.disabled` → діаграма сходить на канвасі, не порожній екран
- [x] рядок 11: `webgl.disabled` + `renderer: 'webgl'` → явна помилка з причиною, видима на сторінці

🔴 **Відхилення від плану.** План обіцяв `e2e/renderer-nogpu.spec.ts` — спеку репо. Так не вийшло:
`node_modules` зібрані під хост-платформу, а середовище перевірки — Linux без GPU. Тому це
**`scripts/nogpu-check.mjs`** — самостійний скрипт із задокументованою docker-командою всередині.
Наслідок чесно названий: у CI він **не ганяється**, це ручний прогін.

**Закриває рядки:** 3, 10, 11

## T83.9 — `docs/USAGE.md`

**Файли:** `docs/USAGE.md`

- [x] опція та семантика трьох значень
- [x] `'auto'` — **best-effort**, із причиною (той самий Chromium дав різний вердикт на macOS і в Linux-контейнері)
- [x] blacklisted-драйвер теж поїде на канвас
- [x] рушій **не змінюється** після монтування; перемикання потребує перезавантаження сторінки
- [x] паралельне монтування двох діаграм із різними значеннями — **недетерміноване**

**Закриває:** acceptance criterion 3 і 6 спеки
🔴 **Відхилення від конституції, визнане.** Док мав їхати **тим самим комітом**, що й зміна
сигнатури (T83.2). Він поїхав на три коміти пізніше. Гілка ще не в `main`, тож PR приходить
цілісним, але правило порушене — і це має бути в звіті, а не замовчане.

---

## Покриття приймальної таблиці

| Рядок | Таск | | Рядок | Таск |
|---|---|---|---|---|
| 1 | T83.6 | | 8 | T83.7 |
| 2 | T83.2, T83.6 | | 9 | T83.1, T83.4 |
| 3 | T83.8 | | 10 | T83.8 |
| 4 | T83.1, T83.2 | | 11 | T83.5, T83.8 |
| 5 | T83.6 | | 11а | T83.5 |
| 6 | T83.6 | | 12 | T83.4 |
| 7 | T83.6 | | | |

**Усі 13 рядків мають таск. Тасків без рядка немає.**
