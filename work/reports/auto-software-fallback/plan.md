# plan — автофолбек `auto` → Canvas2D на програмному WebGL

**Спека:** [`spec.md`](./spec.md) · **Задача:** T98 варіант 1 · закриває T92
**Гілка:** `t98-auto-software-fallback` · **База:** `0f47928`
**GATE 1:** `grilling` 2026-09-03 · **GATE 2:** `plan-critique` 2026-09-03 — 4 лінзи,
8 blocking, дизайн змінено (проба знята). Розбір у §9.

---

## 1. Розміщення

Pixi-рендер ⇒ **`packages/sdk`**. Rust не чіпається, межа WASM ↔ TS не рухається, демо не чіпається.

| Що | Куди |
|---|---|
| Маркери + класифікація + читання рядка + мемоізація | `packages/sdk/src/render/detectSoftwareRenderer.ts` — **новий** |
| Тести | `packages/sdk/src/render/detectSoftwareRenderer.test.ts` — **новий** |
| Вплітання у вибір рушія | `packages/sdk/src/render/PixiHost.ts` |
| Тести вплітання | `packages/sdk/src/render/PixiHost.test.ts` — доповнення |
| Публічний опис поведінки | `docs/USAGE.md` § `renderer` + § обмежень |

**Чому окремий файл, а не поруч із `resolveRendererPreference`:** це єдине місце в `packages/sdk`,
що торкається DOM і WebGL напряму; `PixiHost.ts` працює на рівні Pixi-застосунку. Межа проходить
тут, і вона ж робить модуль тестованим без Pixi.

## 2. Reuse-first

**Переюзуємо:**

| Що | Де | Чому годиться |
|---|---|---|
| `resolveRendererPreference` | `PixiHost.ts:24` | **єдина** точка, де значення опції стає парою `{preference, fIMPC}`. Сходинка вбудовується сюди, інакше з'явиться друге місце істини |
| Канал `rendererDiagnostic` | `OrgHierarchyDiagram.ts:263,412` → `:1267` | уже несе рядок саме про вибір рушія й уже доходить хосту |
| `getRendererKind()` | `PixiHost.ts:206` | стенд ним уже звіряє, що сторінка отримала просимий рушій |
| Стенд `e2e/t92-software-pan.spec.ts` | `npm run measure:renderer` | уже міряє три гілки в обох порядках і вже документує поточний провал |
| **Спосіб узяти й віддати контекст** | `pixi.js/lib/utils/browser/isWebGLSupported.js` | Pixi сам створює тимчасовий канвас, бере `webgl`, звільняє через `WEBGL_lose_context`. Робимо так само — це не новий прийом у цьому застосунку |

**Нове:** `detectSoftwareRenderer.ts` — у `packages/sdk` немає жодного коду, що читає
`WEBGL_debug_renderer_info` (`grep` по `WEBGL_`/`getContext` дає лише `pngExport.ts:86-89`, і той
бере `'2d'`).

## 3. Кроки

### К1. Модуль детекту — два експорти, не шість

```ts
export type RendererDetector = () => 'software' | 'unknown';
export function createSoftwareRendererDetector(
  readUnmaskedRenderer: () => string | null,
): RendererDetector;            // мемоїзоване замикання
export function readUnmaskedRendererFromDom(): string | null;   // єдине місце з DOM/GL
```

Усередині файлу, **не** експортовано: `SOFTWARE_RENDERER_MARKERS` (рядок + причина поруч) і
зіставлення. Продакшн тримає один детектор у модулі:
`const detectRenderer = createSoftwareRendererDetector(readUnmaskedRendererFromDom)`.

**Мемоізація — замиканням, не «кешем».** Пробник захоплюється у фабриці, а не приймається в
метод, тож спитати той самий детектор двома різними пробниками неможливо; слово «кеш» не
запрошує `set`/`clear`. Тест створює власний детектор — публічного `reset` не з'являється.

⚠️ Обґрунтування Р-4 першої редакції («інакше в барелі з'явиться третій `*ForTests`») було
**хибним на фактах**: у SDK п'ять модульних `*ForTests` (`worker-bridge.ts:40`, `bridge.ts:106,112`,
`wasm-init.ts:28`, `searchWorker.ts:57`), а в барелі — **два** (`index.ts:53-54`). Модульний стан
барель не забруднює сам собою; T105 — про ре-експорт. Форма обрана не заради барелю, а тому що
замикання простіше за фабрику об'єкта.

**Правила зіставлення** (закриває blocking про форму маркерів): підрядок, **без урахування
регістру**, по нормалізованому рядку — реальні значення майже завжди загорнуті
(`ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)…))`,
`ANGLE (Microsoft, Microsoft Basic Render Driver Direct3D11 vs_5_0 ps_5_0)`,
`Mesa/X.org, llvmpipe (LLVM 15.0.7, 256 bits)`).

Маркери: `swiftshader`, `llvmpipe`, `softpipe`, `microsoft basic render`, `software rasterizer`,
**`software adapter`**, **`apple software renderer`**. `Software Adapter` доданий на GATE 2 —
це рівно RDP-сесії й тонкі клієнти на Windows, тобто заявлена аудиторія.

✅ **`warp` перевірено (З0) і викинуто зі списку.** Гіпотеза була, що Windows-термінали видадуть
токен `WARP`. Насправді WARP — це софт-драйвер Direct3D 11, і в `UNMASKED_RENDERER_WEBGL` він
показується **як «Microsoft Basic Render Driver»**, тобто вже покривається наявним маркером
`microsoft basic render`. Окремий `warp` був би водночас **спекулятивним** (реального зразка з
таким токеном не знайдено) і **надлишковим** — а саме він і вимагав окремого правила з межами
слова через ризик хибного спрацювання всередині чужого імені. Прибравши його, ми прибрали й це
правило: усі маркери, що лишились, достатньо довгі й специфічні для звичайного підрядка.

Джерела: [WARP (Wikipedia)](https://en.wikipedia.org/wiki/Windows_Advanced_Rasterization_Platform) ·
[значення renderer](https://deviceandbrowserinfo.com/learning_zone/articles/webgl_renderer_values).
Апаратний рядок для контрасту: `ANGLE (NVIDIA, NVIDIA GeForce RTX 4070 Direct3D11 vs_5_0 ps_5_0, D3D11)`.

**Таблиця рішень — повна, без недовизначених гілок:**

| Вхід | Вердикт |
|---|---|
| рядок містить маркер | `software` |
| рядок є, маркера немає | `unknown` |
| рядок `null` / порожній | `unknown` |
| **значення не є рядком** (`typeof !== 'string'`) | `unknown` |
| розширення `WEBGL_debug_renderer_info` немає | `unknown` |
| `getContext` віддав `null` | `unknown` |
| читання кинуло | `unknown` (виняток не виходить назовні) |

`unknown` завжди означає **поточну поведінку**. Гілки «не знаю → здогад» у дизайні немає.

Рядок про не-рядок доданий `plan-defense` §10 п.1: `getParameter` типізований як `any`, і драйвер
за нашою межею може віддати що завгодно. Без цієї гілки `String.includes` кинув би на нечіткому
значенні, і виняток пішов би вгору саме з того шляху, який мусить бути найтихішим.

🔒 **Інваріант, від якого залежить мемоізація:** читання рядка **синхронне** — один `getContext`
плюс один `getParameter`, без `await` між перевіркою мемо й записом у нього. Тому двоє
паралельних монтувань не можуть обидва побачити порожнє мемо. Якщо колись між ними з'явиться
`await`, мемо мусить зберігати in-flight `Promise`, а не лише результат. Записано, бо порушити
це можна однією правкою й не помітити (`plan-defense` §10 п.3).

### К2. Читання рядка з DOM

`readUnmaskedRendererFromDom()`: від'єднаний `<canvas>` (у DOM не потрапляє),
`getContext('webgl2') ?? getContext('webgl')`, розширення `WEBGL_debug_renderer_info` →
`getParameter(UNMASKED_RENDERER_WEBGL)`. Контекст звільняється через `WEBGL_lose_context`, якщо
розширення є. Уся функція в `try`.

⚠️ Якщо `WEBGL_lose_context` відсутній, контекст живе до GC. Це **записане обмеження**, а не
«звільнено»: A8 сформульований так, щоб не стверджувати недоказовного.

Це синхронно й дешево: один `getContext` + один `getParameter`, без малювання. Верхньої межі
часу не потрібно — вимірювання в дизайні немає.

### К3. Вплітання у вибір

```ts
export function resolveRendererPreference(
  value?: unknown,
  detect: RendererDetector = detectRenderer,
): ResolvedRendererPreference
```

Порядок:

1. `'canvas'` / `'webgl'` → повертається **як зараз**, `detect` не викликається (A5);
2. невідоме значення → діагностика як зараз, далі як `auto`;
3. `auto` → `detect()`; `unknown` ⇒ **байт у байт** поточний результат (A3);
   `software` ⇒ `{ preference: ['canvas'], failIfMajorPerformanceCaveat: false }`.

Дефолт другого параметра — той самий екземпляр детектора, тож обидва наявні виклики резолвера
(`OrgHierarchyDiagram.ts:412` і `PixiHost.ts:225`) бачать **один** вердикт: мемоізація робить
другий виклик безкоштовним і тотожним. Дуплікацію виклику навмисно **не** прибираємо — вона
наявна, мемоізація робить її нешкідливою, і прибирання розширило б дифф на фасад без потреби (KISS).

⚠️ **Тестовий шлях до `PixiHost.init`.** `init:225` викликає резолвер без другого аргументу, а
`PixiHostOptions` не має куди детектор прокласти. Щоб тести, які йдуть через `PixiHost.create`,
не ходили в модульний детектор, `PixiHostOptions` отримує **внутрішнє, не документоване**
поле `detectRenderer?: RendererDetector` — воно не потрапляє в `docs/USAGE.md` і тому не стає
публічним API за визначенням порога. Без нього `twoDiagrams.contract.test.ts` зафіксував би
вердикт модуля на весь файл.

### К4. Діагностика

Коли й тільки коли детект змінив вибір, `diagnostic` називає впізнаний рушій. Нічого не змінив —
рядка немає (A9).

### К5. Документація

`docs/USAGE.md` § `renderer`: дві сходинки, список за іменами, і прямо — **це не гарантія** (A10).
Рядок `:149` («не покладайтесь на `'auto'`») переписати: порада передавати `'canvas'` явно для
парку терміналів лишається, але тепер як **визначеність**, а не як «інакше буде погано».

§ обмежень (`:496-503`) — уточнити конкретно (перевірено по коду Pixi, не припущено):
`isWebGLSupported.js:7-9` кешує вердикт при першому виклику й **ігнорує** переданий далі
`failIfMajorPerformanceCaveat`; `autoDetectRenderer.js:27,41` іде по `preference` по порядку, і
гілка `canvas` безумовна. Отже коли ми віддаємо `['canvas']`, `isWebGLSupported` **не
викликається взагалі** — демотована `auto`-діаграма більше не засіває глобальний кеш Pixi, і
описаний там недетермінізм для сусіда **зменшується**.

⚠️ `check:docs` цього кроку не перевіряє: `check-docs.mjs:104-113` звіряє лише **імена** публічних
методів `OrgHierarchyDiagram` проти тексту `USAGE.md`, а ця зміна нових методів не додає. Крок
верифікує рев'ю, і так він і записаний в A10.

### К6. Вимір і звіт

`npm run measure:renderer`, гілка `auto`; числа поруч зі старими в `report.md` (A1, A2).

## 4. Приймальні сценарії

Складено `acceptance-spec`, переглянуто після GATE 2. **Спостережуване** — що віддає публічний
`getRendererKind()`, які кадри показує стенд, що приходить хосту в діагностику.

### Happy path

| # | Сценарій | Передумова | Дія | Спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 1 | Зеро-клієнт, хост мовчить | SwiftShader, `renderer` не передано | відкрити `Staff · 1M`, панорамувати | `getRendererKind()` = `'canvas'`; кадрів > 33 мс — **0 з 57** | e2e `t92-software-pan.spec.ts` |
| 2 | Машина з GPU, хост мовчить | апаратний WebGL | те саме | `getRendererKind()` = `'webgl'` — як до зміни | ⚠️ **вручну** + юніт A3 |
| 3 | Хост читає, чому canvas | SwiftShader, `auto` | прочитати діагностику | серед рядків є той, що називає впізнаний рушій | юніт `PixiHost.test.ts` |

### Межі

| # | Сценарій | Передумова | Дія | Спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 4 | Маркер усередині ANGLE-обгортки | рядок `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)…))` | відкрити діаграму | `'canvas'` | юніт, параметризований на 4 реальні рядки |
| 5 | Маркер в іншому регістрі | `LLVMpipe` / `SwiftShader` у різних регістрах | відкрити діаграму | `'canvas'` | той самий юніт |
| 6 | Апаратний рядок | `ANGLE (Apple, Apple M1 Pro…)` | відкрити діаграму | `'webgl'`; діагностика про рушій **порожня** | юніт |
| 7 | Дві діаграми, послідовно й **паралельно** | обидві `auto`, та сама сторінка | змонтувати обома способами | однаковий `getRendererKind()`; читач рядка викликано **один** раз | юніт на мемоїзований детектор |
| 8 | Windows-термінал | рядок містить `WARP` або `Software Adapter` | відкрити діаграму | `'canvas'` | юніт |

### Відмови

| # | Сценарій | Передумова | Дія | Спостережуваний результат | Чим перевіряється |
|---|---|---|---|---|---|
| 9 | Рядок недоступний | порожній / `null` / розширення немає / `getParameter` кинув / `getContext` віддав `null` | відкрити діаграму | **поточна поведінка** (`['webgl','canvas']`, `fIMPC: true`); виняток назовні не виходить | **один параметризований** юніт, 5 входів |
| 10 | `'webgl'` на програмному стеку | `renderer: 'webgl'`, SwiftShader | відкрити діаграму | `'webgl'` — пін не зламано; читач не викликано | юніт (наявні `PixiHost.test.ts:48-53` лишаються зеленими) |
| 11 | `'canvas'` явно | `renderer: 'canvas'` | відкрити діаграму | `'canvas'`; читач не викликано | юніт (наявні `:42-46`) |
| 12 | Невідоме значення | `renderer: 'vulkan'` | відкрити діаграму | поведінка `auto`, діагностика називає `vulkan` — **як зараз** | юніт (наявні `:31-39`) |
| 13 | `WEBGL_lose_context` відсутній | розширення немає | відкрити діаграму | вердикт повернуто, помилки немає; `loseContext()` викликано лише за наявності розширення | юніт |

**Ручних рядків: 1** — рядок 2, і він лишається ручним **назавжди**: стенд бігає в headless
Chromium на SwiftShader і про апаратний GPU відповісти не може в принципі. У звіті цей рядок
називається пройденим вручну, а не зеленим тестом.

## 5. Сайд-ефекти

1. **`auto` тепер може обрати canvas там, де раніше обирав webgl** — мета зміни, але це зміна
   дефолту для всіх хостів. Хто хоче старе — `renderer: 'webgl'` (уже існує).
2. **Один зайвий GL-контекст на сторінку** — створюється й одразу звільняється, один раз завдяки
   мемоізації. Pixi робить рівно те саме у `isWebGLSupported`.
3. **Затримки виміру немає.** Один `getContext` + один `getParameter`, без малювання — тому
   стелі часу в дизайні не існує (на відміну від знятої проби).
4. **Недетермінізм із `USAGE.md:496-503` зменшується**, а не зростає — перевірено по коду Pixi,
   див. К5.
5. **Експорт не зачіпається окремо:** PNG/PDF знімаються з живого канвасу, тож вони вже слідують
   за рушієм — це той самий шлях, що при явному `'canvas'`.
6. **`AbortSignal` на публічному шляху не прокидається:** `OrgHierarchyDiagram.ts:414` не передає
   `signal` у `PixiHost.create`. Для цієї зміни ціна скасованого монтування — один `getContext`,
   тобто мізер; названо, щоб не виводилось читачем.

## 6. План міграції

Міграції даних немає. Для хостів це зміна поведінки, не API: жодна публічна сигнатура не
міняється, нових документованих опцій не з'являється.

Порядок: К1 → К2 → К3 → К4 → К5 → К6, за TDD, кожен крок лишає сюїту зеленою.

## 7. Rollback

1. **Хост сам:** `renderer: 'webgl'` фіксує WebGL уже сьогодні, без релізу SDK.
2. **`git revert`** гілки: дифф ізольований у двох файлах `render/` + `docs/USAGE.md`, спільного
   стану з рештою немає.

Рівень «прапорець компіляції» з першої редакції **викреслено**: у JS це не механізм, а в коді
воно лишало б мертву гілку, яку доведеться тримати зеленою, і дешевшим за `revert` не було б
ні для кого.

**Незворотного немає, і це перевірене твердження, а не сподівання:** зміна не мігрує даних, не
пише в сховище, не робить мережевого виклику й не має зовнішнього ефекту. Єдиний стан, який вона
створює, — мемоїзований вердикт у пам'яті вкладки, і він зникає з перезавантаженням сторінки.
Отже після `revert` не лишається нічого, що треба було б чистити окремо.

## 8. Конституція (`.claude/standards.md:98`)

- [x] **Тести перед кодом** — К1–К4 через `tdd`; рядки 9–13 приймальної таблиці — failure-шляхи.
- [x] **KISS → SOLID → …** — дві сходинки, і друга куплена конкретною аудиторією (корпоративний
      Chrome/Edge на тонких клієнтах, де рядок відкритий). Третю сходинку GATE 2 зняв саме за
      KISS: вона несла п'ять дефектів і давала те, що дає її відсутність.
- [x] **GoF лише за ≥2–3 повтореннями** — жодного патерна. Мемоізоване замикання — не Singleton.
- [x] **Межа WASM ↔ TS** — не чіпається.
- [x] **Публічний API не змінюється мовчки** — сигнатури ті самі, але **поведінка** дефолту інша,
      тож К5 обов'язковий і входить у ту саму гілку (A10). Внутрішнє поле `detectRenderer` у
      `PixiHostOptions` свідомо не потрапляє в `docs/USAGE.md` — і тому за визначенням порога
      публічним API не стає.
- [x] **Pocock — тільки TS** — дифф увесь TS.

## 9. GATE 2 — що зробили зі знахідками

**Рахунок: 4 запущено / 4 повернулось.** Лінза «логічні діри» впала на API-помилці й
перезапущена на нижчому тірі; той прогін ішов без класифікатора безпеки, вивід перевірено руками.
Врожай тонкий (2 знахідки, одна дублює AI-slop) — записано, бо гейт із підміненим тіром не
дорівнює гейту в повному складі.

**Blocking — 8, усі закриті:**

| # | Знахідка | Як закрито |
|---|---|---|
| 1 | Обґрунтування сходинки 2 хибне: список тримає лише програмні маркери, апаратний рядок падає далі | §5 і §8 переписані; «безкоштовно для апаратної гілки» прибрано |
| 2 | Стеля недосяжна — синхронний `gl.finish()` не переривається | **проба знята** |
| 3 | Немає прогрівального кадру | **проба знята** |
| 4 | Семпл під власним навантаженням, вердикт липкий | **проба знята** |
| 5 | `performance.now()` загрублений саме в замаскованих браузерів | **проба знята** |
| 6 | Фонова вкладка: тротлінг міряється замість GPU | **проба знята** |
| 7 | `null` / втрата контексту / ліміт контекстів без правил | таблиця рішень К1 повна, `unknown` як єдина «нічия» |
| 8 | Шість експортів там, де досить двох | К1: два експорти |

**Non-blocking — 11, прийнято 10, свідомо відхилено 1:**

- прибрано rollback-прапорець (2 лінзи), параметр `limits` (2 лінзи), фабрику кешу на користь
  замикання (3 лінзи), A9-снапшот барелю, `check:docs` як верифікатор прози;
- зведено приймальні рядки «рядок недоступний» у один параметризований (5 входів);
- переформульовано A8/рядок 13 на спостережуване (не стверджуємо звільнення без розширення);
- додано `WARP`, `Software Adapter`, `Apple Software Renderer` і правило зіставлення;
- додано внутрішній шов `detectRenderer` у `PixiHostOptions` для тестів через `create()`;
- §5 п.4 переписано з гіпотези на перевірений висновок.

**Відхилено свідомо:** прибрати дуплікацію виклику резолвера (`OrgHierarchyDiagram:412` проти
`PixiHost:225`). Мемоізація робить її нешкідливою, а прибирання розширює дифф на фасад без
виграшу для користувача — KISS. Записано тут, щоб наступний читач не вважав це недоглядом.

## 10. GATE 2 — `plan-defense`

| # | Питання | Закрито рядком | Вердикт |
|---|---|---|---|
| 1 | кривий інпут | §3 К1 таблиця рішень, рядок «значення не є рядком» + §3 К1 правило `warp` як токен; для значення опції — §3 К3 п.2 і приймальний рядок 12 | закрито (**дірка знайдена й закрита**: не-рядок від `getParameter`; хибне спрацювання `warp` підрядком) |
| 2 | відмова залежності | §3 К1 таблиця рішень — усі п'ять відмов (`null` контекст, немає розширення, кинуло, порожньо, не-рядок) ⇒ `unknown` ⇒ поточна поведінка; §3 К2 «уся функція в `try`» | закрито |
| 3 | гонка й повторний виклик | §3 К1 🔒-інваріант синхронності + §3 К3 про два виклики резолвера; приймальний рядок 7 (послідовно **і** паралельно) | закрито (**дірка знайдена й закрита**: інваріант тримався мовчки) |
| 4 | порожні та граничні дані | §3 К1 (порожній рядок), приймальні рядки 4, 5, 8 (обгортки, регістр, Windows-термінал). Колекційні межі — **поза скоупом, бо** фіча не має колекцій: її вхід це одне рядкове значення, а вихід — один із двох варіантів | закрито |
| 5 | rollback | §7 — два рівні (хост ставить `'webgl'` без релізу; `git revert`) + абзац «Незворотного немає» з переліком, чого зміна не робить | закрито |

**Дірок лишилось: 0** — гейт зелений, план іде в таски.

Три дірки знайдені й закриті правкою файлу, не відповіддю в чаті: не-рядок від `getParameter`
(п.1), `warp` як підрядок (п.1), незаписаний інваріант синхронності (п.3).
