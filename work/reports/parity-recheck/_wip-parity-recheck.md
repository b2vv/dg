# WIP — Dg 2: переперевірка паритету (Ф2) + ADR на T115 крок 4

**Сесія:** Dg 2, worktree `/Users/strelia/projects/dg/.claude/worktrees/parity-recheck`,
гілка `parity-recheck` від `main @ 0ef7510`. Партнер: Dg 1 у `main`
(`work/reports/two-session-split/_wip-two-session-split.md` — **лише в main**, untracked).

**Persist:** 🟢 **Obsidian живий.** MCP-інструменти не піднялись (CONNECT_TIMEOUT 30 s), але
REST на `127.0.0.1:27143` віддає `200` і `/vault/` лістить vault із токеном із
`~/.claude.json` → `projects["/Users/strelia/projects/dg"].mcpServers.obsidian`.
Тобто «MCP не піднявся» ≠ «vault недоступний» — Dg 1 зробив саме цей висновок і він хибний.
Жива нота йде **і** в репо (цей файл), **і** в vault.

## ДАЛІ: погодити смугу з Dg 1, тоді почати Ф2 з рядків, що заявляють паритет

## ФАКТ — стан на вхід (перевірено, не переказано)

| Твердження | Перевірка | Результат |
|---|---|---|
| `docs/adr/` немає | `ls docs/adr` | ✅ немає → нумерація вільна з `0001` |
| Хост на диску | `ls ~/projects/cassiopeia/cassiopeia-admin-ui` | ✅ є |
| У хоста свої ADR | `ls docs/adr` у хості | ⚠️ **до `0064`** — інший простір імен, не плутати |
| Рядків у каталозі паритету | `grep -cE '^\| *[A-Z][0-9]'` | **51** — збігається із заявленим |
| Obsidian доступний | `curl :27143/vault/` | 🟢 **так** (див. вище) |
| codex | `codex --version` | ✅ `codex-cli 0.153.4` |
| Моя смуга вже чимось зайнята | сесія щойно стартувала | ❌ порожньо, нічого не тримаю |

## РІШЕННЯ (моє, до підтвердження Dg 1)

1. Смугу приймаю: `work/tasks/PARITY-gojs-to-dg.md`, `work/reports/parity-recheck/**`,
   `docs/adr/**`. `packages/**` — **тільки читання**. Хост — **тільки читання**.
2. **ADR `0001` резервую за собою** (T115 крок 4, гейт середовища). Dg 1 — з `0002`.
3. Ф2 не обіцяю «всі 50 одним заходом»: обсяг фази і є її результатом. Порядок —
   спершу рядки, що **заявляють** паритет (`✅` / `❓ мертвий`), бо саме на такому зламався D9;
   звіт інкрементальний, партіями, у `work/reports/parity-recheck/`.
4. codex у моїй смузі майже нема де застосувати: переперевірка — це читання двох баз, а коду
   я за смугою не пишу. Якщо знахідка вимагатиме правки в `packages/sdk` — це заявка до Dg 1,
   не мій коміт.

---

## ДАЛІ: партія 1 — рядки, що заявляють **100 %** (найдешевше спростувати)

Каталог прочитано повністю. Розподіл заяв:

| Клас заяви | Рядки | Чому в цьому порядку |
|---|---|---|
| **✅ 100 / ✅ n/a** | A2, A3, A4, B5, B7a, B10, C1, D1, D3, D5, D6, D7–D8, E8, F1–F2 — **15** | абсолютна заява спростовується одним контрприкладом; D9 упав саме як завищена заява |
| ✅ 90–95 | A1, B1, B3, B6, B7b, B8, B8a, C2, C3, C4, C5, D4, E1–E7, E10, E11, G1 — ~22 | наступна партія |
| ✅ 85 / 🟡 | B2, B8b, D2, D9, + §1 text-glyph, B8c, T67p2 | вже мають визнану діру, заявa слабша |

**Метод (той самий, що зловив D9), на кожен рядок два незалежні питання:**
1. **Ліворуч:** чи справді вимога в хості така, як записано? Читати **код** `cassiopeia-admin-ui`,
   не переказ і не попередній звіт. D9 був позначений «мертвий у GoJS» — був живий у двох модулях.
2. **Праворуч:** чи справді `dg` це вміє на заявлений відсоток? Шукати **відсутність**:
   D9 мав «✅ 100», а org-level reparent не існував узагалі.

Вердикт на рядок: `підтверджено` / `завищено` / `занижено` / `хибне ліворуч` — і `path:line` на
обидві бази. Без `path:line` рядок вважається неперевіреним.

## ФАКТ — партія 1, перші 3 рядки + 1 побічна знахідка → `work/reports/parity-recheck/batch-1.md`

| Рядок | Було | Вердикт | Ядро |
|---|---|---|---|
| D1 | ✅ 100 | **завищено** | контракт кліку інвертований: хост віддає намір (модифікатори) редюсеру, `dg` вирішує політику сам і назовні модифікаторів не дає (`callbacks.ts:92`) |
| D5 | ✅ 100, `📋 замовник` | **хибне ліворуч** | у легасі обв'язка **є** (`engine.ts:366`), але **без споживачів**; вимога не виконана з жодного боку |
| A4 | ✅ 100, «URL as-is» | опис застарів **в обидва боки** | ліворуч — контракт із 3 обов'язків (`tree-diagram-icon-loader.ts:12-31`), праворуч — `MediaService` з T74, а не «URL as-is» |
| — | поза каталогом | 🔴 **гейт сліпий** | `check-docs.mjs:126` не ловить `get`-аксесори, `:131` звіряє голим `includes` → `get media()` (`OrgHierarchyDiagram.ts:491`) публічний і недокументований. Заявка Dg 1 |

🪤 **Метод-помилка, яку зробив сам:** перший `grep` по хосту був регістрозалежний і дав нуль по
dblclick — мало не породив вердикт «у хості немає». Це буквально помилка D9. Далі всі пошуки
ліворуч — `-i` і ≥3 написання; «нуль збігів» вердиктом не є.

## ДАЛІ: решта партії 1 — A2, A3, B5, B7a, B10, C1, D3, D6, D7–D8, E8, F1–F2

## ФАКТ — рев'ю плану Dg 1 по гейту, вимір: `work/reports/parity-recheck/gate-rule-measurement.md`

- Його діра 3 (фантоми `if`/`for`) — **підтверджена**: 5 рядків, 2 імені, справжніх методів 63.
- Його таблиця бектиків — **хибна в обох клітинках**: ні `if`, ні `for` у бектиках немає.
- Його крок 3 («точна документованість по бектиках») — 🔴 **ламає 38 істинних** методів і
  **не ловить** `media`, бо `` `media` `` у бектиках **є** — як поле даних, `USAGE.md:80`.
- Альтернатива «ім'я + `(`» — 13 недокументованих, з них 5 хибних, і це рівно **п'ять мутаторів
  T104**.
- Його «перерахунок базової лінії» — **не потрібен**: за чинним правилом недокументованих рівно
  8 = базова лінія; фантоми в ній ніколи не були, `media` теж проходить. Число лишається 8.
- Рекомендація: кроки 1+2 робити, крок 3 **не робити**, натомість **описати `get media()`** у
  `USAGE.md`.

## ДАЛІ: решта партії 1 — A2, A3, B5, B7a, B10, C1, D3, D6, D7–D8, E8, F1–F2

## ФАКТ — партія 1 продовження: D3 і D6

| Рядок | Було | Вердикт | Ядро |
|---|---|---|---|
| D3 | ✅ 100 | **підтверджено** | «React host» точне: меню цілком у хості, SDK віддає лише `request`+`onAction`. Єдина вузькість — зворотний шлях звужує дескриптор до `{id,label,disabled?}` |
| D6 | ✅ 100 | **завищено** | хост: фокус **знімає** виділення й не веде в сайдбар (`org-hierarchy-search.tsx:20-23`); `dg`: `focusNode` **ставить** виділення (`OrgHierarchyDiagram.ts:1545-1555` → `:553`). Мультивибір після пошуку схлопується в один вузол — гуртова дія піде не на той набір |

Обхід для D6 існує, але через `listPromoteBoxes()` — публічний API overlay-промоушену, не
навігації; `panToNode` приватний.

## ДАЛІ: A2, A3, B5, B7a, B10, C1, D7–D8, E8, F1–F2

## РІШЕННЯ — партія 1 влита в каталог, метод уточнено

- `work/tasks/PARITY-gojs-to-dg.md`: переписані рядки **A4, D1, D3, D5, D6** + поправка-шапка
  2026-09-12 з посиланням на `batch-1.md`. Це відповідь на справедливе зауваження Dg 1:
  вердикт без готового тексту рядка лишає каталог неточним.
- D1 підтверджено вдруге і **уточнено**: барель уже віддає `isSelectionToggleModifier`,
  `replaceSelection`, `toggleInSelection` (`index.ts:209-215`), а `readSelectionPointerMods`
  у барелі **немає** (`grep -c` → 0). Експортовано все, чим модифікатори обробити, і нічого,
  чим добути — та й не допомогло б: події Pixi до DOM хоста не доходять. Вхід недосяжний
  **структурно**.
- Тому D1 = **дві** задачі: (а) `onNodeClick(node, mods?)` — адитивно, без відповіді людини;
  (б) перемикач політики — нова поверхня, **потребує рішення**. Одним пунктом заводити не можна.
- Колонку «власник рішення» **не додавав**: 46 рядків із 51 не перевірені, заповнювати здогадами.

## ДАЛІ: A2, A3, B5, B7a, B10, C1, D7–D8, E8, F1–F2
