# Незалежна перевірка `031224f` і `201fd2b` — прогоном, не читанням

Перевіряв Dg 2 (не автор), на гілці `parity-recheck`, ребейзнутій на `main`.
Метод: кожну заяву — командою; там, де заява негативна («не зросло», «кидає»), — мутацією.

## `031224f` — T106

| Заява Dg 1 | Команда | Результат |
|---|---|---|
| `DiagramRenderer.ts` 1285 → **1204** | `wc -l` | ✅ **1204** |
| ярус-1 методи зникли | `cohesion.mjs` | ✅ `orgTreeOptions`, `orgStaffCardOptions`, `normalizeSelected` у виводі **відсутні** |
| `renderOrganizations` 7→6 | там само | ✅ **6** |
| `addStaffOrgCards` 6→5 | там само | ✅ **5** |
| жоден метод не виріс | звірка з його таблицею «до» | ✅ `renderPositionGrid` 7, `addStaffPersonCards` 4, `renderStaffCanvas` 9, `paintStaffDepartments` 5 — усі як були |
| `render` не чіпано | там само | ✅ **18**, як і було |

Прогнав **його** скрипт на **його** результаті — це слабша перевірка, ніж незалежний вимір, і я
це називаю: скрипт і артефакт від одного автора. Сильнішим тут є не скрипт, а те, що таблиця
«до» була опублікована **до** правки, тож підганяти її заднім числом вже не можна.

## `201fd2b` — гейт

| Заява | Команда | Результат |
|---|---|---|
| тести скриптів 19 → **30** | `node --test scripts/*.test.mjs` | ✅ `tests 30 · pass 30 · fail 0` |
| `check:docs` зелений | `npm run check:docs` | ✅ зелений |
| модуль **кидає** без заголовка класу | мутація копії: подав джерело без `export class` | ✅ `THREW: The OrgHierarchyDiagram class header was not found…` |
| фантоми зникли | `publicSurface()` | ✅ `has if/for: false` |
| аксесор видно | там само | ✅ `has media: true`, поверхня **64** |
| базова лінія не зрушила | правило A | ✅ **8**, ім'я в ім'я |
| правило B коштує **47** | правило B | ✅ **47** |
| `media` задокументовано (§8) | `docs/USAGE.md:333-351` | ✅ `diagram.media`, приклад `refresh({kind:'organization', id:'org-7'})`, названо `OrgHierarchyDiagram: media service not initialized` |

### Дві дрібниці, які варто знати, поки вони дешеві

1. 🟡 **Ціна правила C виросла з 5 до 6 — через його ж §8.** `AGENTS.md` тепер каже, що
   `name(` «flags the five mutators T104 documented in prose». Вимір на постфіксному сканері:
   правило C дає **14** недокументованих, і серед хибних не п'ять, а **шість** —
   `movePersonToCell`, `placeOrgAtMatrixCell`, `reorderOrg`, `setPromotedNodeIds`, `shiftBlock`
   **і `media`**. Бо `diagram.media` — властивість, у прозі вона без дужок. Висновок («крок 3
   не робити») від цього не міняється, число в документі — так.
2. 🟡 **Брифінг відстає на 15 комітів із 25 дозволених** (сказав сам `check:docs`). Гейт зелений,
   але запас — десять комітів, а `AGENTS.md` вимагає свіжий брифінг **перед** імплементацією.
   Знахідки Ф2 туди все одно доведеться внести; що більше партій, то ближче межа.

### Чого ця перевірка **не** доводить

Шість мутацій у тести T106 прогнав автор. Я їх не переганяв: мутувати чужі тести — це другий
прохід тієї самої роботи, а не незалежна перевірка. Незалежним тут є інше й слабше: 1016 тестів
зелені, і моя мутація **його** гейта (без заголовка класу) впіймана. Якщо потрібна незалежність
саме по T106 — її дасть не повтор мутацій, а сторонній тест на поведінку винесеного модуля.
