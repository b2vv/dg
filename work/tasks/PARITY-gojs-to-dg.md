# Parity `gojs-diagram` → `dg`: вимога → можливість

**Статус:** 🟢 живий довідник, не задача — тримається синхронним із кодом, не закривається.
Міграційна черга закрита (`🔴 міграційні: 0`); відкритого лишилось ~2 🟡 (B2 matrix edge cases,
text-glyph padding) і один 🔴 поза міграцією — B8c → [T61](./T61-group-recursion-tier3.md).  
**Редакція:** 2.2 (синхронізовано з cutover queue complete, 2026-08-23)  
**Ліворуч:** прод `gojs-diagram` / `modules/{org-hierarchy,positions}`  
**Праворуч:** `b2vv/dg`  
**Індекс задач:** [T71-gojs-to-dg-migration-plan.md](./T71-gojs-to-dg-migration-plan.md)

🔴 **Поправка 2026-09-06 — D9 перерахований, «🔴 міграційні: 0» вище не перевірене повторно.**
Свіже читання **реального** коду хоста `cassiopeia-admin-ui` (не переказу) показало: D9
(«D&D reparent») був позначений і хибно (`❓ мертвий у GoJS` — насправді живий і в org, і в
positions), і завищено (`✅ 100% у dg` — org-level reparent відсутній повністю, position-reparent
не захищає корінь, move/merge/clone-structure нема). Рядок D9 виправлено нижче; нові тікети —
[T116](./T116-org-level-reparent-missing.md), [T117](./T117-position-reparent-root-guard-and-cross-org-actions.md).
Повний метод і решта знахідок (не лише D9) — окремі звіти:
[`org-diagram-parity.md`](../reports/host-integration/org-diagram-parity.md),
[`positions-diagram-parity.md`](../reports/host-integration/positions-diagram-parity.md).
🟢 **Поправка 2026-09-12 — Ф2, партія 9 (вигляд картки): E1, E2, E3, E4 — усі чотири стоять.**
Уперше за прохід чотири підтверджені поспіль, і це не випадковість класу: «звичайні» рядки
писались про те, що **видно на екрані**, а такі твердження важче завищити, ніж твердження про
власника рішення чи про семантику слова. E2 — найміцніший рядок каталогу: хост тримає інваріант
двома 🔴-тестами на два виміри, ми — двома тестами на два **шари** плюс третій режим понад
заявлене. Звіт: [`../reports/parity-recheck/batch-9.md`](../reports/parity-recheck/batch-9.md).

🆕 **Сьома вада форми — джерело в майбутньому.** E9 не є рядком паритету через **порожню**
колонку джерела; **D2** — через джерело `📋 наступні задачі`, тобто вимогу, якої **ще немає**.
Невимірність однакова, але друга **виглядає легітимною** (колонка заповнена, посилання
осмислене) і тому небезпечніша. Ознака: джерело називає не документ, а **намір**. Перевірка
та сама — чи існує **сьогодні** спостереження, що зробило б рядок хибним.

🔑 **Що об'єднує всі сім вад — одне речення, і воно з'явилось лише наприкінці проходу.**
Рядок завищується там, де він говорить про **механізм або повноваження**, і тримається там, де
говорить про **спостережуване на екрані**. E1–E4 — чотири підтверджені поспіль, і всі чотири про
те, що видно оком (є підпис чи ні, однаковий розмір, що замість знака, де годинник). А D1, D5,
D6, A3, D2 хибні всі однаково, і всі — про те, **хто вирішує**. Звідси практичне: рядок, що
описує власника рішення, семантику слова чи набір входів, потребує **виходу в чужу базу**;
рядок про намальоване перевіряється дешево й рідко бреше.

🪤 **Обмеження на весь набір критеріїв (з B9):** негативний чи умовний кваліфікатор у тексті
вимоги — **ознака, а не доказ**. Розрізняє одне питання: **чи звужує кваліфікатор вимогу під
те, що ми вміємо.** «Без вигаданого підпорядкування» — ні, це сама вимога. «Не edge-click» — так.

🔴 **Поправка 2026-09-12 — Ф2, партія 8 (B8a за власною заявкою).** Пунктир у хоста несе **два**
значення; `dg` відтворює **одне**. На картці збігається; на контейнері наш `dashed` — прапорець
**теми**, один на весь стиль, тож «або всі зони пунктирні, або всі суцільні», і сигнал «запасне
відро» виразити нічим. Це не окрема знахідка, а **B6 з іншого боку**: коробки без департаменту
немає, отже й пунктиру навколо неї. Звіт:
[`../reports/parity-recheck/batch-8.md`](../reports/parity-recheck/batch-8.md).

🔴 **Поправка 2026-09-12 — Ф2, партія 7: клас «зелений за побудовою» закрито.** Вісім
кандидатів зі скринінгу пройдені. Звіт:
[`../reports/parity-recheck/batch-7.md`](../reports/parity-recheck/batch-7.md).
**B6 → 🟡 70** (посади без департаменту в нас не малюються; у хоста це коробка з підписом і
пунктиром, названа вимогою замовника), **D2 → 🟡** (джерело «наступні задачі» — вимоги ще немає,
плюс власник політики), **B9 підтверджено — підозра не справдилась**.

🪤 **Урок про сам критерій, вартий більше за рядок:** негативний кваліфікатор у тексті вимоги —
**ознака, а не доказ**. B9 «без вигаданого підпорядкування» **і є** вимогою (замовник просив не
показувати неіснуючий зв'язок), на відміну від D4 «не edge-click», який **звужував** вимогу під
можливість. Питання не «чи є заперечення», а «чи звужує воно вимогу під можливість».

⚠️ **Нова заявка на перечитання: B8a** — у хоста пунктир прив'язаний саме до коробки без
департаменту, якої в нас немає, отже наш пунктир може бути про інше.

🔴 **Поправка 2026-09-12 — Ф2, партія 6 (кандидати типу 5).** A1 → 🟡 (контейнерний, як A2),
B1 **підтверджено**, **B2 `✅ 85` → 🟡 50**: координати BE читаються лише в обмеженій сітці, а
типове `matrixShape: 'auto'` дає необмежену — тобто **за замовчуванням координати ігноруються**,
і опція, що вмикає їх, у `docs/USAGE.md` не описана. Звіт:
[`../reports/parity-recheck/batch-6.md`](../reports/parity-recheck/batch-6.md).
Умова «сітка лише коли більше одного» при цьому **збігається** з хостом.

🔴 **Поправка 2026-09-12 — Ф2, партія 5: критерій застосовано до всіх 24 рядків, що лишались.**
Звіт: [`../reports/parity-recheck/batch-5-screening.md`](../reports/parity-recheck/batch-5-screening.md).

**Як перевірити рядок на шосту ваду** — два питання: (1) чи існує спостереження, яке зробило б
рядок хибним? якщо ні — рядок **не є твердженням**; (2) чи можна це спостереження зробити, **не
виходячи з каталогу**? якщо так — вада 1–4, видно порівнянням; якщо ні — вада 5–6, рядок
**зелений за побудовою**.

Результат скринінгу: **2** рядки взагалі не є рядками паритету (**E9** — порожня колонка
джерела; **B8c** — сам виводить себе зі скоупу, лишаючи відсоток), **8** кандидатів на зелень за
побудовою (A1, B1, B2, B6, C2, C4 — клітинка = ім'я символу; B9, D2 — кваліфікатор у тексті
вимоги), **14** — звичайна перевірка, вада видна порівнянням. З перевірених: **C2 підтверджено**
(у легасі розгортання посад справді немає — важливо після D5, де таке саме твердження було
хибним), **C4** тримається, але власник рішення не той.

🔴 **Поправка 2026-09-12 — Ф2, партія 4 (рядки 85–95 %).** C3, D4, G1 — і **два нові типи
хиби, гірші за попередні, бо роблять рядок зеленим за побудовою**:
**збіг імен** (C3 — клітинку заповнено однойменним методом про інше: наш `expandToDepth` про
посади, хостовий — про дерево організацій) і **перепис вимоги** (D4 — дужка «не edge-click»
звужувала вимогу під можливість, а відсоток лишився від старої). Звіт:
[`../reports/parity-recheck/batch-4.md`](../reports/parity-recheck/batch-4.md).

⚠️ **Вад форми цього каталогу тепер шість.** Перші чотири робили рядок **неточним**; останні
дві роблять його **непідданим спростуванню** — він не червоніє при жодному читанні, бо
тримається або на збігу тексту, або на власному означенні. Саме тому їх не знайшов жоден
попередній прохід.

🔴 **Поправка 2026-09-12 — Ф2, партія 3 (штатка).** A2, B5, B7a, B10 — інша вісь: розкладка,
не шов. Підтверджено **один** (B7a). Звіт:
[`../reports/parity-recheck/batch-3.md`](../reports/parity-recheck/batch-3.md).
Головне: **B10 не `n/a`** — мапер не може виразити переїзд подвійної посади, поки членство в
блоці виводиться з `organizationId`; і **дві мовчазні відмови** в `canvasLayout` (стеля
розгортання відкидає `slice`-ом без діагностики; нуль голів у керівній org пропускає ярус 1
без жодного слова). Перша — буквально та помилка, яку хост у себе вже полагодив і описав
червоним (`use-subordinate-blocks.ts:23-30`).

🔴 **Поправка 2026-09-12 — Ф2, партія 2.** Ще п'ять рядків: A3 (`✅ 100` → 🟡 75), C1
(відсоток тримається, **значення слова «згорнуто» розходиться**), E8 і D7–D8 (**підтверджено**),
F1–F2 (зум підтверджено, експорт — інша форма). Звіт:
[`../reports/parity-recheck/batch-2.md`](../reports/parity-recheck/batch-2.md).
**A2, B5, B7a, B10 навмисно не чіпані** — це розкладка штатки, окрема область, і чесна її
перевірка коштує як уся партія 1.

🔑 **Головне за день — поза каталогом:** у хоста є **записаний контракт рушія-замінника**
(`tree-diagram-engine.ts:44-52`, 19 членів + 7 подій), якого цей файл не знав. Прогін:
12 стоїть · 5 частково · 9 спростовано →
[`../reports/parity-recheck/swap-seam-verdict.md`](../reports/parity-recheck/swap-seam-verdict.md).
Дев'ять спростованих зводяться до **однієї** причини — хост тримає виділення у своєму редюсері,
`dg` тримає його в собі.

🔴 **Поправка 2026-09-12 — Ф2, партія 1 (рядки, що заявляли 100 %).** Перевірено 5:
A4, D1, D3, D5, D6 — вистояв **один** (D3). Звіт із `path:line` на обидві бази:
[`../reports/parity-recheck/batch-1.md`](../reports/parity-recheck/batch-1.md).

⚠️ **Три з п'яти хибні однаково, і це вада не рядків, а форми каталогу: він міряє наявність
можливості, а не власника рішення.** «Клік є» (D1), «dblclick є» (D5), «пошук є» (D6) — усе
правда; хто вирішує, що при цьому стається з **виділенням**, не записано ніде, і саме там
розходження. Коли прохід завершиться, каталогу потрібна **колонка «власник рішення»**, а не
п'ять точкових правок. Зараз це гіпотеза на трьох випадках, тому колонки ще немає.

Решту рядків (окрім D9) **не** перераховано цим проходом — «🔴 міграційні: 0» в §3 нижче
може бути так само застарілим, це не перевірялось.

> Порівнюємо **«що зобов’язані показати користувачу»**, не механізм із механізмом.  
> GoJS-обходи → §1 (не мігрують).  
> **Нумерація тікетів `dg`:** T53–T56 уже зайняті зданими тікетами; gaps = **T63–T70**, B8c = **T61**.

**Походження:** 📋 замовник · 🏛 ADR · 🧬 легасі · 🎨 макет · ❓ не встановлено  
**%:** ✅ ≥85 · 🟡 30–80 · 🔴 <30

---

## §1. Обхідні шляхи GoJS — не мігрують

| Що в GoJS-проді | Чому існує | Доля в `dg` |
|---|---|---|
| `formMatrix` / `revertMatrix` / `matrixParents` / `shouldFormMatrix` | TreeLayout володіє розміщенням; підмножину іншому layout — лише через Group | **зникає** — `matrixRow/matrixCol` напряму |
| Структурний лінк батько→група + `GROUP_LINK_KIND` | поставити групу під батька в дереві | **зникає** |
| `go.Group` як зона окремої розкладки | єдиний спосіб дати підмножині свій layout-прохід | **зникає** — `orgBlockLayout` + `StaffTierBand` (SPEC §2.2.1) |
| «Сітка лише коли всі сиблінги схлопнуті» | розгорнутій дитині потрібна вертикаль | **зникає** — anchor child under parent (`orgBlockLayout`) |
| `addChildren` + `expandedKeys` | перф-стеля canvas | **зникає** — LOD + viewport / windowing |
| `allowSelect:false` + редюсер | flaky modifier-click GoJS | **зникає** (вимога multi-select лишається — D2 / T67) |
| `isActionable` / expander click-guard | click після `standardMouseSelect` | **зникає** |
| «Непідпорядкована = ОКРЕМИЙ КОРІНЬ» + Horizontal arrangement | TreeLayout ставить лише top-level | **зникає** — orphan під head лише для WASM root; ребра з `reportLines` |
| `pendingReveal` на LayoutCompleted | async layout без bounds | **зникає** — `focusNode()` async |
| Один `nodeTemplate` | обмеження DiagramAdapter | **зникає** — фіксовані профілі + promote |
| `text-glyph-padding.ts` | GoJS ріже кирилицю | ⚠️ виміряти на Pixi Text |
| Raw-hex §31.2 | canvas ≠ CSS vars | ⚠️ лишається — Pixi `NodeTheme` `0xRRGGBB` |

---

## §2. Вимоги → `dg`

### A. Дані і полотно

| # | Вимога | Дж. | dg | % | Тікет |
|---|---|---|---|---|---|
| A1 | Ієрархія org зі знімка BE | 🧬📋 | `create` + `setData` — ⚠️ **контейнерний рядок** (як A2): власного спростовного змісту не має, підсумовує B1/B2/C1/A3. Конкретне до нього одне й уже знайдене: `setData` **гасить виділення** (A3) | 🟡 | T01/T12 |
| A2 | Штатка організації | 📋 4245 | `layout/staff` — ⚠️ **контейнерний рядок**: власного спростовного змісту не має, відсоток успадкований від B5/B6/B7a/B8 | 🟡 80 | T08 |
| A3 | Новий знімок / фільтр (у хості **фільтр = новий знімок**) | 📋 | ⚠️ два методи з **різною** семантикою виділення: `setData` його **гасить** (`:1062`), `appendData` — **зберігає**. Отже кожне застосування фільтра знімає виділення | 🟡 75 | T12/T21 · корінь = причина 1 вердикту |
| A4 | Іконки: контракт із трьох обов'язків — `load` (+дедуп) / `invalidate` (байти міняються, посилання ні) / `dispose` (інакше течуть усі object-URL) | 🧬 | **не «URL as-is»** (застаріло з T74): `MediaService` — рефкаунт, `invalidate`, `refresh`, `destroy`; шов для хоста — `get media()` | ✅ 100 | T23/T74 |

### B. Розміщення

| # | Вимога | Дж. | dg | % | Тікет |
|---|---|---|---|---|---|
| B1 | Дерево згори вниз | 🧬 | WASM row-tree — **підтверджено**; найслабше твердження каталогу і єдине, яке від цього виграє: воно не претендує на більше, ніж може | ✅ 95 | T03/T48 |
| B2 | Підлеглі в сітці за BE coords | 📋 BE — хост читає координати **завжди**, плюс правило для виродженого випадку «усі однакові → розсунути» (`matrix-decision.ts:21-42`) | 🔴 `org.matrixRow/matrixCol` читаються **лише** в обмеженій сітці (`matrixGrid.ts:143-177`). За замовчуванням `matrixShape: 'auto'` → `bounded: false` → розстановка **за індексом**, координати BE ігноруються. Саму опцію `matrixShape` у `docs/USAGE.md` **не описано**, а `matrixRow/Col` там названі **виходом** (`:277`) | 🟡 50 | T03 |
| B3 | Spine: стовбур + шина + райзери | 📋+🧬 | `spine-bus` org-matrix | ✅ 90 | **T63** ✅ |
| B5 | Три яруси штатки | 📋 4245 | `canvasLayout` — ярус 2 збігається; **ярус 1** інший за джерелом (хост: `leadingBlocks` зі зв'язків org, їх кілька · ми: один `parentOrgId` + рівно одна голова, нуль голів → **тиша**); **ярус 3** інший за вмістом (хост: блок позицій на кожну підлеглу org, із вкладенням для груп · ми: **картки**, розгортання капнуте `maxExpandedOrgCards: 1` і зайві **відкидаються мовчки**) | 🟡 60 | T08 |
| B6 | Департамент групує посади. ⚠️ Посада **без** департаменту в хості — **власна коробка з підписом (назва org) і пунктирною рамкою**, і це записано як вимога замовника (`position-tree-to-diagram.ts:133,159-168`) | 📋 4245 | DepartmentBlob + G1–G8 — але місце без департаменту **не малюється**: `NO_DEPARTMENT_ID`, «their bucket is never painted» (`contourPaintFilter.ts:3`). Рішення свідоме (магнетизм M2), але відповідь інша | 🟡 70 | T07/T49 |
| B7a | Посади в власній зоні розкладки | 📋 4245 | `orgBlockLayout` локальна СК — **підтверджено**, і з тієї самої причини, що в хості: контейнер без власної розкладки обрамляв би те, чого ніхто не розставив | ✅ 100 | — |
| B7b | Dept зона всередині блоку org | 📋 4245 §3 | cells всередині блоку | ✅ 90 | — |
| B8 | Іменована **зона відображення** (фон/radius/рамка/підпис) | 🎨📋 4245 §3–4 | `staffZoneChrome` paint | ✅ 90 | **T64** ✅ |
| B8a | Пунктир навколо сітки. ⚠️ У хості пунктир — **інформація, і її дві**: на картці = відчеплений вузол; на контейнері = «це **запасне** відро» (суцільний для департаменту, пунктирний для коробки без департаменту — `positions.adapter.ts:415`) | 🧬 | картка ✅ збігається (`PersonNode.ts:434-436`); зона — **прапорець теми**, один на весь стиль (`StaffZoneChromeStyle.dashed`, «default solid»), не властивість конкретної зони → **або всі пунктирні, або всі суцільні**, і сигнал «запасне відро» невиразний | 🟡 (картка ✅ 90 · контейнер 🔴) | **T64** ✅ · та сама причина, що B6 |
| B8b | Керівник вище людей у зоні | 🏛 ADR-0055 | hybrid по reportLines | ✅ 85 | **T64** ✅ |
| B8c | Рекурсія зон груп орг | 📋 4245 §1 | Group = caption на картці | ⬜ **не рядок паритету**: кваліфікатор «не ціна міграції» виводить рядок зі скоупу, а відсоток лишався. Або вимога — і тоді 10 % щось означає, або не вимога — і тоді не тут. Зміст лишаємо, відсоток знято | **T61** |
| B9 | Посада без керівника без вигаданого підпорядкування | 📋 модалка | ребра тільки з reportLines ✅; side-column detached (T65) ✅ | ✅ | **T65** done (не блокер) |
| B10 | Подвійна посада → ярус 2 (**переїзд**, не дублювання) | 📋 | 🔴 **не `n/a`**: мапер це виразити **не може**. Членство в блоці = `p.organizationId === orgId` (`orgBlockLayout.ts:292`), тож переїзд вимагав би переписати власника. Єдиний шов для чужої позиції — `externalManagersFor` — лише для керівників, пришпилює **над** блоком і в лічильники не входить | 🔴 | **новий** |

### C. Розкриття

| # | Вимога | Дж. | dg | % | Тікет |
|---|---|---|---|---|---|
| C1 | Expand/collapse org | 📋 | `expandOrg` / … — команда точна й **не чіпає виділення** (проба, рядки 3–4). ⚠️ але «згорнуто» означає різне: у GoJS це **спосіб не малювати**, у `dg` після T113 — **спосіб перемкнути розкладку**, і матриця розкладає всі org (`matrixLayout.ts:35`) | ✅ 100 **за командою** | T03 · T113 |
| C2 | Expand/collapse **посади** | 📋 замовник — **перевірено**: у легасі справді немає (пошук по всьому модулю позицій, `-i`, 5 написань; єдиний релевантний збіг — seed-collapsed для чужої org, тобто C4) | `togglePositionExpand` | ✅ 95 **підтверджено** | **T66** ✅ |
| C3 | Початкова глибина N — **дерева організацій**, поле вхідних даних `initialDepth` (`tree-diagram.model.ts:141`), застосовується на `setData` і **зсуває режим розкладки** | 📋 замовник | 🔴 **збіг імен**: наш `expandToDepth` про **посади** всередині однієї org (`positionExpand.ts:123`, `USAGE.md:288`). Глибини для **дерева org** немає: `initialExpand` приймає `rootOrgId` + `revealNodeId`, і **жодного `depth`** | 🔴 | **новий** |
| C4 | Чужий блок згорнутий | 🧬📋 — у хості це **рішення мапера** (`position-tree-to-diagram.ts:58`) | `collapsed` — читається з даних (`data/types.ts:56`), але **й виставляється розкладкою самостійно** (`orgMode.ts:15,42`, `orgTree.ts:121`); хост, що засіє `collapsed: false`, може виявити, що розкладка думає інакше | ✅ 90 ⚠️ власник | T03 |
| C5 | Афорданс на картці | 🎨 | +/− ▼/▲ ⋮ | ✅ 90 | T52 |

### D. Взаємодія

| # | Вимога | Дж. | dg | % | Тікет |
|---|---|---|---|---|---|
| D1 | Click → sidebar. Модифікатори віддаються **сирими**, політику виділення вирішує **редюсер хоста** | 📋🧬 | `onNodeClick(node)` — **без модифікаторів**; політику застосовує SDK, хост бачить лише `onSelectionChange` (підсумок). Барель уже віддає `isSelectionToggleModifier` / `replaceSelection` / `toggleInSelection`, але **вхід** до них хосту недосяжний | 🟡 70 | T04 · гапи → **нові** |
| D2 | Multi-select → bulk | 📋 **наступні задачі** — тобто вимоги ще **немає**, мішень не зафіксована | Set API + ctrl toggle ✅ (механіка є); але **власник політики** інший: у хості вся політика мультивибору в редюсері, рушій віддає модифікатори сирими, у нас вони назовні не виходять. Marquee — найменша з трьох проблем рядка | 🟡 | **T67** Phase1 ✅ · корінь = причина 1 |
| D3 | Context menu | 📋 | React host: SDK віддає `request` + `onAction`, меню й правила цілком хостові. Зворотний шлях звужує дескриптор до `{id,label,disabled?}` | ✅ 100 | T10/T52 |
| D4 | Період підпорядкування на організації — ⚠️ дужка «~~не edge-click~~» **звужувала вимогу під можливість**: у хості клік по ребру живий і спожитий (`use-org-hierarchy-diagram.ts:156`), і дає **дві** речі — поповер дат і захист сфокусованого ребра від видалення (`build-org-hierarchy-menu-items.ts:38`) | 📋 замовник | відображення періоду зроблено; кліку по ребру **немає за рішенням** (`LayerManager.ts:39`), `incomingLink` у меню теж немає | 🟡 (відображення ✅ 95 · взаємодія 🔴) | **T68** ✅ · гап → **новий** |
| D5 | Dblclick → sidebar | 🧬 ~~📋 замовник~~ — у легасі обв'язка **жива** (`ObjectDoubleClicked` → `nodeDoubleClick`), але **споживачів нуль**: подія емітиться в порожнечу | `onNodeDoubleClick` розведено на org і person; host must subscribe | ✅ 100 **шва**; сама вимога не жива **з жодного боку** | **T69** |
| D6 | Search + focus. Вибір центрує камеру, **знімає** попереднє виділення і **не** відкриває сайдбар (він лишається click-only) | 📋 | пошук є (worker + revealPath, більше ніж просить хост — той шукає сам у React). Але `focusNode` **ставить** виділення замість знімати: мультивибір схлопується в один вузол. «Центруй без виділення» публічно лише через `listPromoteBoxes()` + `panTo` | 🟡 70 | T18 · гап → **новий** |
| D7–D8 | Fullscreen / timeline | 📋🧬 | host — перевірено: `use-fullscreen.ts`, `timeline/org-hierarchy-timeline.tsx`; у `gojs-diagram/` збігів на fullscreen **нуль**. Хост володіє хромом, рушій дає команди | ✅ n/a | — |
| D9 | D&D reparent | 📋🧬 живий і в org, і в positions | 🟡 часткова — org-level reparent взагалі відсутній; position-reparent не захищає корінь, немає move(cross-org)/merge/clone | 🟡 60 | T04/T17, **гапи → T116, T117** |

### E. Вигляд картки (+ §4 зображення)

| # | Вимога | Дж. | dg | % | Тікет |
|---|---|---|---|---|---|
| E1 | Лише знак, без підпису | 📋 4231 | `showShortName: false` box modes | ✅ 90 | **T70** ✅ |
| E2 | Однаковий розмір вузла з/без підпису | 📋 4231 | fixed card AABB tests | ✅ 90 | **T70** ✅ |
| E3 | Без знака → `fullName`. ⚠️ У хості картка **з підписом** без знака лишається на **легасі-плейсхолдері** (антирегресія) | 📋 4231 №2 | text fallback — повне ім'я заміщує коротке; поле знака лишається **порожнім**, бо плейсхолдери це робота **хоста** (`mediaPlaceholders`), не SDK | ✅ 95 | **T70** ✅ |
| E4 | Годинник тимчасової на org — правило хоста це **заборона розміщення**: «NOT inside the row that the caption rule hides» | 📋 4231 | **T** badge — `tempBadge`/`tempBadgeLabel` окремі об'єкти сцени (`OrganizationNode.ts:47-48`), не діти рядка підпису, тож правило виконується **за побудовою**. ⚠️ На картці **людини** годинник гейтиться на `nameText.visible` (`PersonNode.ts:537`) — та сама форма, яку хост забороняє для org | ✅ 90 **підтверджено** | **T70** ✅ |
| E5 | Бейдж `N [M]` | 🧬 скріни | counts badge | ✅ 90 | **T70** ✅ |
| E6 | Unit-code | 🎨 | caption line | ✅ 90 | **T70** ✅ |
| E7 | Вакансія / чип періоду / detached | 📋 4245 | vacant label + period chip | ✅ 90 | **T70** ✅ |
| E8 | Theme + symbol light/dark | 📋 | `setTheme` + URLs — **підтверджено**: обидва боки зберігають виділення, розгортання й камеру (хост — ThemeManager без перебудови, `engine.ts:475-485`; `dg` — `setTheme` без `applySelection`, `:1083`) | ✅ 100 | T28 |
| E9 | HTML/React overlay | **—** | promote | ⬜ **не рядок паритету**: колонка джерела порожня, тобто вимоги немає й спростувати нічим. Це опис **нашої** фічі в каталозі **чужих** вимог — зміст лишаємо, відсоток знято | T26 |
| **E10** | Знак org: **contain**, не розтяг; 3 режими коробки; intrinsic 400×200 | 📋 4231 №3 | `fitContain` + box modes | ✅ 95 | **T70** ✅ |
| E11 | Прелоад light+dark → миттєвий theme flip | 🧬 | inactive URL prefetch | ✅ 90 | **T70** ✅ |

### F / G

| # | Вимога | % | Тікет |
|---|---|---|---|
| F1–F2 | Zoom / export — зум підтверджено (проба 7–9); експорт **інша форма**: у хоста `exportPng(): Promise<Blob>`, у нас `export(options): Promise<Blob \| string>` — union + обов'язковий аргумент | ✅ 100 зум · 🟡 експорт | T15/T05 |
| G1 | Playwright адресація | ✅ ~90 **підтверджено** | **T55 done** — «еквівалент» тут правильне слово: шлях через DOM-якорі **кращий** за координати (Playwright чекає стабільності), а ціна еквівалентності задокументована — якорі лише для того, що в кадрі; сирі координати сценові (`worldBoxToScreen` публічна); `interactive: true` гейтить хост ([ADR-0001](../../docs/adr/0001-test-anchor-overlay-gate-belongs-to-the-host.md)) |

---

## §3. Підсумок

| | к-сть |
|---|---|
| ✅ | **~35** (cutover queue) |
| 🟡 | **~2** (B2 matrix edge cases; text-glyph padding) |
| 🔴 міграційні | **0** (queue closed) |
| 🔴 не міграція (B8c) | 1 → **T61** (макет Figma) |
| ⏸ optional | **T67 Phase 2** marquee (product go) |

**Cutover P0:** ✅ T64, T66, T70 Phase 0.  
**P1/P2 queue:** ✅ T63, T68, T69, T70p1/p2, T65, T67 Phase 1.  
**Залишок:** T61 (макет); T67 Phase 2 (optional); host cutover (видалити gojs-diagram).

### Mapping «нов. T5x» з чернеток → `dg`

| Чернетка | Файл у `dg` |
|---|---|
| T53 spine | **T63** |
| T54 зони | **T64** |
| T55 orphan placement | **T65** |
| T56 expand position | **T66** |
| T57 multi-select | **T67** |
| T58 period | **T68** |
| T59 dblclick | **T69** |
| T60 chrome + images | **T70** |
| T61 groups recursion | **T61** |

---

## §4. Зображення (стисло)

**Сумісно:** object-URL / IndexedDB, `symbolUrlLight/Dark`, person initials + LOD hide photo (бонус).  
**Діра:** `OrganizationNode.showSymbol` — квадратний stretch (суперечить 4231 №3); немає 3 режимів коробки / intrinsic detect / fullName fallback. Деталі й acceptance → [T70](./T70-position-card-chrome.md).
