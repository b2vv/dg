# T97 — Ініціалізований стан діаграми організації

**Пріоритет:** P1 · **Статус:** 🔵 не почато (вимоги отримані, спека не написана)
**Підстава:** формулювання користувача 2026-08-31.

---

## Вимоги (дослівно за користувачем, не переказ)

### В1 — базова глибина на відкритті

Це **мінімум, який має побачити користувач**, і він залежить від того, чи має «наша» організація
керуючу над собою:

| Випадок | Рівні, що показуються |
|---|---|
| Наш рут **має** керуючу організацію на діаграмі | **0** — root діаграми · **1** — наша рут-організація · **2** — її children у матриці |
| Наш рут **збігається** з root діаграми | **0** — рут · **1** — children |

Тобто в першому випадку видно **три** рівні, у другому — **два**.

### В2 — розкриття до цілі при вході з пошуком

Якщо користувач заходить на діаграму й URL містить пошук, то **після ініціалізації** діаграма має
розкритись до тієї організації й одразу показати **розгорнуте дерево до неї**, а не згорнутий
вигляд, який потім стрибне.

### В3 — зображення тільки для видимого

Організацій багато, тож зображення першочергово вантажаться **лише для тих, що користувач бачить**,
і далі — в міру розкриття.

> «для цього всього мають бути механізми за допомогою яких можна це буде зробити» — тобто SDK має
> віддати хосту механізми, а не сховати поведінку в демо.

---

## Що вже є (звірено з кодом)

| Потреба | Механізм | Стан |
|---|---|---|
| В2 — розкрити предків і навести камеру | `OrgHierarchyDiagram.revealPath(nodeId)` (`:1101`) — `revealOrgPath` + `focusNode`, з рендером між ними | **є, публічний** |
| В1/В2 — масове згортання/розгортання без перемальовування на кожну org | `setOrgsCollapsed(ids, collapsed)` (`:663`), `collapseAllOrgs()` (`:674`) | **є** |
| В3 — кеш і дедуплікація завантажень | `MediaService.loadTexture` (`media/MediaService.ts:77`), `prefetch` (`:144`) | **є** |

## Чого немає (це і є задача)

1. **Розкриття org-дерева на глибину.** `expandToDepth` (`:878`) працює **лише зі штаткою**:
   він бере `data.positions` + `reportLines` у межах однієї організації. Для дерева **організацій**
   аналога немає — `collapsed` приходить від хоста, і ніщо не рахує «глибина ≤ N».
   **В1 без цього не реалізується.**

2. **Правило «наш рут проти root діаграми».** У моделі немає поняття «наша рут-організація»:
   є `parentOrgId` і є корені як орги без батька. Хто саме «наш рут» — вирішує хост, і йому
   потрібен спосіб це сказати (конфіг? параметр `initialFocusOrgId`?). Від цього залежить, два
   рівні показувати чи три.

3. **Ініціалізація як момент.** `revealPath` існує, але викликається **після** `create()`, тобто
   після першого рендера: користувач побачить згорнуте дерево, і воно стрибне. Потрібен шлях
   «розкрити **до** першого показу» — конфігом при створенні, а не викликом після.

4. **Завантаження зображень за в'юпортом.** Сьогодні `loadTexture` викликається рендерером для
   **кожної картки, яку він будує** (`OrgHierarchyDiagram.ts:517`), а кулінга по в'юпорту немає —
   T89 закрито з висновком «вигравати нема чого». ⚠️ Але T89 міряв **геометрію**, а не медіа:
   мережа й пам'ять GPU — інший ресурс, і його ніхто не міряв. Тобто В3 упирається в питання,
   яке вважалось закритим, і його треба переміряти на медіа, а не на вузлах.

---

## Розвилки, які має вирішити GATE 1 (не тут)

- **Хто рахує глибину** — SDK за `parentOrgId`, чи хост присилає `collapsed` уже розставленим?
  Друге дешевше, але тоді В1 не механізм SDK, а домовленість.
- **Що таке «рівень 2 у матриці»** — children у matrix-режимі показуються як картки в сітці;
  «розгорнути до рівня 2» означає розгорнути рівень 1, щоб його діти стали видимі. Термінологію
  треба закріпити, бо «рівень» тут і про дерево, і про матрицю.
- **В3: гейт по в'юпорту чи по розкриттю?** «Що бачить користувач» і «що розгорнуто» — різні
  множини: розгорнута гілка може бути за межами екрана. Від вибору залежить, чи потрібен кулінг.
- **Чи В2 переважає В1** — якщо URL веде до організації на 7-му рівні, показуємо три рівні
  **плюс** розкритий шлях, чи тільки шлях?

## Порядок робіт

- [ ] GATE 1: закрити чотири розвилки вище.
- [ ] `acceptance-spec` — сценарії з очікуваним результатом на кожну з В1/В2/В3.
- [ ] Вимір для В3: скільки зображень вантажиться сьогодні на реалістичній кількості org
      і скільки з них поза екраном. Без числа В3 — здогад.
- [ ] Аж тоді план і код.

## Готово, коли

- [ ] Відкриття діаграми показує рівно ту глибину, що описана в В1, для обох випадків.
- [ ] Вхід за URL із пошуком дає одразу розгорнуте дерево до цілі, **без** проміжного згорнутого
      кадру.
- [ ] Зняте число: скільки зображень вантажиться на першому кадрі до і після.
- [ ] Механізми — публічні на `OrgHierarchyDiagram`, а не приховані в демо.
