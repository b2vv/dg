# T117 — reparent посади не захищає корінь; move/merge/clone-structure відсутні

**Пріоритет:** P1 · **Статус:** 🔵 не почато
**Джерело:** `work/reports/host-integration/positions-diagram-parity.md` п.24, п.29 — звірка
positions-diagram GoJS-хоста (`cassiopeia-admin-ui`, режим редагування) з
`@org-hierarchy/sdk`, 2026-09-06. Два окремі гапи, одна задача — обидва про edit-mode
переміщення посад і обидва відсутні в дзеркальний спосіб (SDK **дозволяє** те, що хост
структурно **забороняє**, і SDK **не вміє** те, що хост **робить**).

## Гап 1 — reparent не захищає кореневу посаду

Хост типізує кореневу посаду окремо (`HierarchyRoot`) і структурно вимикає для неї
move/merge/delete в меню — `build-position-menu-items.tsx:61-131`, три дії задизейблені саме
на корені.

`@org-hierarchy/sdk`: `checkReparent` (`packages/sdk/src/interaction/positionReparent.ts:44-60`)
відмовляє лише на чотири речі — `self`/`cycle`/`unchanged`/`unknown-id` — і **нічого не знає про
`isHead`**. `grep isHead` по `interaction/**` не дає жодного збігу поза одним використанням в
`OrgHierarchyDiagram.ts:1227` (пошук голови для `expandToDepth`, не guard). Тобто сьогодні нічого
не заважає перетягнути `isHead`-посаду під нового керівника — API це прийме.

## Гап 2 — move (cross-org) / merge / clone-structure відсутні

Хост має три дії, яких немає в SDK взагалі:

- **Move** — модалка переносу посади в **іншу** організацію (не reparent усередині тієї самої);
- **Merge** — злиття двох структур;
- **Клонувати структуру** — SaveBar secondary action, копіює **всю** структуру.

Усі три комітяться **одразу поза draft-чергою** й навмисно **не входять у undo**
(`use-positions-editing.ts:40`, задокументовано раніше в `questions.md` §D15). `reparentPosition`/
`movePersonToCell`/`shiftBlock` — усі три рівно про **одну** посаду в межах **наявної**
структури; жоден не рухає посаду між організаціями, не зливає дві структури й не копіює піддерево.

## Чому одна задача, а не дві

Обидва гапи — про те саме питання «що дозволено робити з посадою в edit-режимі», обидва виявлені
в тому самому проході, обидва зачіпають той самий контракт (`reparentPosition` + майбутні нові
мутатори). Розділяти на реалізацію все одно доведеться (root-guard — маленька, cross-org дії —
велика фіча), але задача одна, щоб рішення по одній частині не приймалось без огляду на другу
(наприклад: **move під новий батько в іншій org** — це вже майже те саме, що «Move»-дія хоста,
тож дизайн API для Гапу 2 варто звіряти з тим, чи не закриває він заразом Гап 1).

## Що зробити

**Гап 1 (мала, безпекова):**
1. `checkReparent` — новий варіант відмови `'root'`, коли `positionId` має `isHead: true`.
2. `docs/USAGE.md` — назвати гарантію явно (кореневу посаду `reparentPosition` не чіпає).

**Гап 2 (велика, потребує продуктового рішення — не заводити код без нього):**
1. Чи потрібен SDK взагалі «move в іншу org» / «merge» / «clone» як **свої** мутатори, чи це
   лишається виключно хостовою логікою (host сам робить кілька `reparentPosition`/`setData`-
   викликів і склеює historyю)? Це відкрите питання, не рішення цієї задачі.
2. Якщо так — форма патчів, транзакційність (T104-правило: або все на екрані, або нічого),
   і чи ці дії взагалі мають комітитись через `onLayoutChange`, а не як SDK-what-you-give-is-what-
   you-get (§1 USAGE.md: SDK не ходить у мережу і не зберігає стан сам).

## Готово, коли

- [ ] Гап 1: `reparentPosition(headPositionId, anyManagerId)` → відмова з `refusal: 'root'`,
      дані не змінені, помітно (не мовчазний catch — той самий урок, що в T111).
- [ ] Гап 2: продуктове рішення зафіксоване (можливо — «SDK свідомо цього не робить, host
      складає з примітивів» — теж прийнятний результат, якщо назва вголос у `docs/USAGE.md`).

## TDD (Гап 1, готово до red-green одразу)

| Кейс | Тест |
|---|---|
| success | reparent звичайної посади далі працює як раніше |
| failure | reparent `isHead`-посади → `refusal: 'root'`, дані байт у байт |

## Межі

Гап 2 без продуктового рішення в код не йде (`spec-flow`, якщо рішення — «додати мутатори» —
бо `movePersonToCell`/`reparentPosition` вище порога пайплайна, `docs/USAGE.md`).
