{
  "name": "org-hierarchy",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/sdk",
    "packages/demo"
  ],
  "scripts": {
    "build:wasm": "cd packages/core && wasm-pack build --target web --out-dir ../sdk/src/wasm/pkg",
    "build": "npm run build -w @org-hierarchy/sdk",
    "build:demo": "npm run build -w @org-hierarchy/demo",
    "dev": "npm run dev -w @org-hierarchy/demo",
    "test": "npm run test -w @org-hierarchy/sdk && npm run test -w @org-hierarchy/demo",
    "test:rust": "cd packages/core && cargo test",
    "typecheck": "npm run typecheck -w @org-hierarchy/sdk && npm run typecheck -w @org-hierarchy/demo"
  },
  "engines": {
    "node": ">=20"
  }
}
