{
  "name": "org-hierarchy",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/sdk",
    "packages/demo"
  ],
  "scripts": {
    "build:wasm": "cd packages/core && wasm-pack build --target web --out-dir ../sdk/src/wasm/pkg && cargo test export_rust_types --features ts-export",
    "build": "npm run build -w @org-hierarchy/sdk",
    "build:demo": "npm run build -w @org-hierarchy/demo",
    "dev": "npm run dev -w @org-hierarchy/demo",
    "test": "npm run test -w @org-hierarchy/sdk && npm run test -w @org-hierarchy/demo",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "compare:nodes": "playwright test e2e/node-compare.spec.ts",
    "test:verify": "npm run typecheck && npm test && npm run test:e2e && npm run compare:nodes",
    "test:rust": "cd packages/core && cargo test",
    "typecheck": "npm run typecheck -w @org-hierarchy/sdk && npm run build:types -w @org-hierarchy/sdk && npm run typecheck -w @org-hierarchy/demo"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
